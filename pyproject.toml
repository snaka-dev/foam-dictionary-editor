[tool.ruff]
target-version = "py310"
line-length = 110

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.10"
files = ["foam", "model", "app_config", "schemas", "services", "ui/app_state.py"]
ignore_missing_imports = true
warn_unused_ignores = true
