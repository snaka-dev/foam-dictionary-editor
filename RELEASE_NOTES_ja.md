# リリースノート

## 未リリース

## v1.8.0 — 2026-07-15

### 新機能

- **ケースルートの `All*` スクリプトをファイル一覧に表示** — `Allrun`、`Allrun.pre`、`Allclean` などケースルート直下の `All*` スクリプトが、ファイル一覧の最後に新設された **case root** グループへ自動的に表示されるようになりました。Tools メニューが実行するスクリプトを、アプリを離れずに確認・編集できます。一覧に加わるのは `All*` ファイルのみで、ログ・`*.foam` ファイル・計算結果は従来どおり表示されません。スクリプトはシェルスクリプトであり辞書ではないため、エディタはシェル用のシンタックスハイライト（`#` コメント、文字列、`$変数`、`runApplication`/`runParallel` などの OpenFOAM ヘルパー、および通常のキーワードリストによるユーティリティ・ソルバー名）に切り替わり、ツリービューは空のまま、**Apply Text to Tree** は無効、比較モードの対象外です。編集・保存しても実行権限は保持されます。副次的な効果として、**ケースの複製** / **新しいケースとして保存** にスクリプトが含まれるようになり、複製したケースも **Tools > Run Allrun Script** から実行できます（従来は複製時に暗黙的に失われていました）。関連する 2 つの改良: 隠しファイル（アプリ自身の `.foam-editor-files.json` などのドットファイル）は追加ディレクトリのスキャンおよび Add files ダイアログから常に除外されるようになり、追加ディレクトリ経由で一覧に含まれた `log.*` 実行ログはグレー表示され、サイズにかかわらず辞書パーサーを通さずプレーンテキストとして開かれます（100 KB 超の警告ダイアログの「ツリービューは利用できません」という文言が実際に守られるようになりました）。また **case root** ヘッダーにも他のディレクトリヘッダーと同じ右クリックメニュー（**New file in 'case root'...** / **Add files from 'case root'...**）が表示されるため、実行ログや `README` などの特定のルートファイルを、ルートディレクトリ全体を登録することなく個別に一覧へ追加できます。
- **BlockMesh 3-D パネル: 折り返しツールバーと並列表示の自由なリサイズ** — パネル上部のコントロール（Vertices ▾、Blocks ▾、各オーバーレイメニュー、Refresh、STL ▾、Scale ▾、ラベルサイズ、ビューボタン）が 1 本の折り返し式ツールバーにまとまりました。パネルが広いときは 1 行に収まり、狭いときはボタンが複数行に流し込まれるため、従来のように約 700 px の最小パネル幅を強制しません。並列表示（サイドバイサイド）モードでは、スプリッターをその最小幅より狭くドラッグしても 3-D ペインが折りたたまれて消えることがなくなりました。両ペインとも折りたたみ不可となり、3-D ビューは実用的な最小幅（150 px）まで滑らかに狭められます。マウス操作ヒント行と Preview バナーもリサイズを妨げず折り返して表示されます。
- **BlockMesh 3-D パネルへの setFieldsDict 領域の表示** — `setFieldsDict` を読み込んだり編集したりすると、既存の `topoSetDict`/`snappyHexMeshDict` オーバーレイと並んで、その `regions ( … )` リストが 3-D パネルにオーバーレイ表示されるようになりました。領域ソースは topoSet と同じジオメトリキーワードを使うため、同じシェイプ（box、複数 box、rotated box、sphere、cylinder、cone — `$変数`/`#eval{ 式 }` の解決も同様）が描画されます。各シェイプはオレンジ色で描画され、エントリの `fieldValues` の要約（例: `alpha.water=1`）がラベルとして表示されます。新しい **setFields ▾** メニューは topoSet/snappyHexMesh のメニューと同じ構成（マスタートグル、Show/Hide all、シェイプごとのチェックボックス、`zoneToCell` のようなエントリ用の Non-geometric sources サブメニュー）を持ち、シェイプは 3 つ目のグループとして **Export Shapes as STL…** にも加わります。また、パネルの ⊞ 並列表示トグルは `setFieldsDict` がアクティブなファイルの場合にも有効になります。
- **ブロックメッシュより大きいオーバーレイシェイプのクリップ表示** — damBreak の `box (0 0 -1) (0.1461 0.292 1)` のような `setFieldsDict` 領域はブロックメッシュをはるかに超えて広がり、3-D ビューでメッシュが極端に小さく見えてしまっていました。すべてのオーバーレイシェイプ（topoSet、snappyHexMesh、setFields）は、ブロックメッシュのバウンディングボックスを各軸 10% 拡大した範囲に（表示上のみ）クリップされるようになり、クリップされたシェイプのシーンラベルには「✂ clipped」マークが付くため、切り取られた形状を実際の大きさと見誤ることはありません。ブロックメッシュの完全に外側にあるシェイプはクリップせずに「⚠ outside block mesh」とラベル付けされ、シーン全体を包み込むシェイプ（内側に表面が一切見えないもの）は消えてしまう代わりにバウンディングボックスの重なり部分として描画されます。**Export Shapes as STL…** は常にクリップ前の完全なジオメトリを書き出します。
- **Run setFields / Run checkMesh** — Terminal タブで実行する 2 つの Tools メニューアクションを追加しました。**Run setFields** は `setFields` を実行して `log.setFields` に tee します。setFields は `0/` の場ファイルを直接書き換えるため（設定済みの場に再実行すると値が重ねて適用されます）、`0.orig/` があるケースでは **0/ を復元してから実行**（`rm -rf 0 && cp -r 0.orig 0 && setFields …`）・**そのまま実行**・**キャンセル**の三択を提示し、`0.orig/` がないケースでは通常の確認ダイアログを表示します。**Run checkMesh** はメッシュを検証して `log.checkMesh` に tee します。メッシュを読むだけなので確認なしで実行されます。どちらのログも汎用ログパーサー経由で **View Log Summary** に表示されます。
- **ヘルプ > バージョン情報にバージョン番号を表示** — バージョン情報ダイアログのタイトル下にアプリケーションのバージョンが表示されるようになりました。番号は単一の情報源（`_version.py`）から取得します。git チェックアウトでは、直近のタグからのコミット数とコミットハッシュ（作業ツリーが汚れている場合は `*`、例: `1.7.0 (dev+12, ge75192d*)`）が付加され、バグ報告時にビルドを正確に特定できます。インストール済みのコピーではリリース番号のみを表示します。
- **`constant/polyMesh` の有無・古さインジケーター** — メッシュが存在する場合、ファイル一覧に `constant/polyMesh: N cells` 行が表示されるようになりました（セル数は `owner` ファイルの `FoamFile` ヘッダーの note フィールドから読み取り、メッシュ自体の解析は不要です）。`blockMeshDict` がメッシュ生成後より新しい場合、インジケーターはアンバー色になり「stale (blockMeshDict changed since last run)」と表示されます。
- **BlockMesh 3-D パネルへの `snappyHexMeshDict` ジオメトリの表示** — `snappyHexMeshDict` を読み込んだり編集したりすると、`blockMeshDict`/`topoSetDict` とは独立して、その `geometry {}` ブロックが 3-D パネルにオーバーレイ表示されるようになりました。対応するエントリタイプ: `box`、`sphere`（`radius` を軸ごとのベクトルで指定した場合はイグルー型ドームのようなだ円体として表示）、`cylinder`、`cone`、`triSurfaceMesh`/`distributedTriSurfaceMesh`（参照先の STL/OBJ ファイルを `constant/triSurface/` から自動読み込み）、box ベースの `collection`（searchableSurfaceCollection）メンバー（各インスタンスをスケール・回転・平行移動済みの個別のボックスとして描画）。各シェイプは `castellatedMeshControls.refinementSurfaces`/`refinementRegions`（`"iglo.*"` のような正規表現パターンのサーフェス名キーを含む）と照合して **surface**・**region**・**geometry のみ** のいずれかに分類され、それぞれ色分け表示されます（ティール／紫／グレー）。`locationInMesh` およびマルチリージョン用の `locationsInMesh` はラベル付きの保持点マーカーとして描画されます。**「snappyHexMesh ▾」** メニューでオーバーレイ全体または各シェイプを個別に表示・非表示にできるほか、カテゴリごとの色凡例と、解決できなかったジオメトリ（STL ファイルが見つからない、collection のメンバーが 1 つも box として解決できない、など）のグレーアウト項目も表示されます。トップレベルの `$変数` および `#eval{ 式 }` は `blockMeshDict`/`topoSetDict` と同じ仕組みで解決されます。
- **BlockMesh 3-D パネルで描画できる topoSetDict ソースの拡充と、スケーラブルな topoSet ▾ メニュー** — `topoSetDict` オーバーレイが実際の辞書で使われるソース構文をより広く認識するようになりました（OpenFOAM v2606 のほぼ全ソースを網羅した `pisoFoam/RAS/cavity` チュートリアル辞書では、67 アクション中 6 個だった描画数が 21 個になり、黙って脱落するエントリはなくなりました）。新たに認識されるもの: `boxToCell/Face/Point` の `min`/`max` 形式と複数ボックスの `boxes ( … )` リスト形式、`origin` で宣言された球（OpenFOAM の第一キーワード — 従来は互換エイリアスの `centre` のみ対応）と `innerRadius` による中空球、ラベル付きマーカーとして描画される点を持つソース（`nearestToCell`/`nearestToPoint` の `points`、`regionToCell` の `insidePoints`/`insidePoint`、`regionToFace` の `nearPoint`）、そしてシーンの大きさに合わせた円板として描画される `planeToFaceZone`。60 以上のアクションがあっても **topoSet ▾** メニューが使いやすいままであるように、**Show all shapes** / **Hide all shapes** アクションを追加し、描画可能なジオメトリを持たないエントリは長いグレーアウトリストの代わりに **Non-geometric sources (N)** サブメニューにまとめるようにしました。**snappyHexMesh ▾** メニューにも一貫性のため同じ 2 つの改善を適用しています。点マーカーと平面のシェイプは意味のある STL サーフェスを持たないため **Export Shapes as STL…** の対象外です。同梱の `tutorials/topoSetShapes` デモケースは新しい形式をすべて含むようになりました。
- **Run snappyHexMesh / Run topoSet** — 既存の「Run blockMesh」と同様の Tools メニューアクションを追加しました。それぞれコマンドを Terminal タブに送り、出力をケースディレクトリの `log.snappyHexMesh` / `log.topoSet` に tee します。ケースに既に数値の結果ディレクトリがある場合は、再実行がそれらを無効にする可能性があると事前に警告します。
- **Run Allrun Script / Run Allclean Script / Clean Case (foamCleanTutorials)** — Terminal タブで実行する 3 つの Tools メニューアクションを追加しました。**Run Allrun Script** はケースの `./Allrun`（ソルバーを含むワークフロー全体）を実行します。OpenFOAM の `runApplication`/`runParallel` ヘルパーは `log.*` ファイルが既に存在するステップをスキップするため、実行済みのケースでは何も起きないように見える代わりに、**クリーンしてから実行**（`foamCleanTutorials && ./Allrun` — 標準的な `./Allclean && ./Allrun` イディオム）・**そのまま実行**・**キャンセル**の三択を提示します。**Clean Case** は `foamCleanTutorials` を実行します。ケースに独自の `Allclean`/`Allwclean` スクリプトがあればそれを使い、なければメッシュ・時刻ディレクトリ・`processor*/`・`postProcessing/`・`log.*` ファイルを削除し、さらに `0.orig/` が存在する場合は `0/` も削除します（標準の `-auto` チュートリアル慣例）。確認ダイアログにはこれらのうちどれが起きるかが正確に表示されます。**Run Allclean Script** は、スクリプト自体を明示的に実行したい場合のために `./Allclean` を直接実行します。3 つとも破壊的な操作の前に必ず警告し、ケースまたはターミナルパネルがない場合は無効になります。
- **形状を STL としてエクスポート** — BlockMesh 3-D パネルに新しい **STL ▾ → Export Shapes as STL…** アクションを追加しました。読み込み済みの `topoSetDict`/`snappyHexMeshDict` の描画可能な各シェイプ（パラメトリックな box/sphere/cylinder/cone/rotated box に加え、`triSurfaceMesh`/collection 由来のものも含む）をチェックボックス付きで一覧表示し、初期状態は 3-D ビューでの現在の表示/非表示に対応します。出力先フォルダを選ぶと、チェックした各シェイプがそれぞれ個別の `.stl` ファイルとして名前（衝突時は重複排除）付きで書き出されます。描画可能なジオメトリを持たないシェイプはエラーにせずスキップとして報告されます。
- **Find OpenFOAM Examples** — キーワードや設定の実際の使用例を `find`/`grep` なしで探せる、新しい非モーダルの Tools メニューダイアログを追加しました。ローカルの OpenFOAM インストールのチュートリアルケース（`tutorials/`）と `etc/caseDicts/` 配下の厳選テンプレート（`#includeFunc` で利用できる `postProcessing/` の function object テンプレートを含む）の両方を検索します。インストールは環境変数（`WM_PROJECT_DIR`/`FOAM_TUTORIALS`/`FOAM_ETC`）が設定されていればそこから、なければ一般的なインストール場所（`/usr/lib/openfoam/`、`/opt/`、`~/OpenFOAM/`）から自動検出され、バージョンセレクタで切り替えられます。任意のディレクトリを参照して指定することもでき、その選択はセッションをまたいで記憶されます。検索結果はチュートリアルケース／テンプレートフォルダごとにグループ化され、ファイル名（`controlDict`、`fvSchemes` など）で絞り込みでき、シンタックスハイライト付きの読み取り専用プレビューが最初の一致行までスクロールして表示されます。そこからファイル全体または選択した部分をクリップボードにコピーできるほか（**ファイルをコピー** / **選択範囲をコピー** の 2 つのボタン）、一致箇所がチュートリアルケース内にある場合は **このケースと比較** をクリックすると、そのケースが既存の「ケースと比較」ビューにそのまま読み込まれ、右クリックの **この値を使用** で個々の設定を自分のケースに取り込めます。
- **View Log Summary** — `log.*` ファイル（blockMesh、snappyHexMesh、topoSet）を生テキストの代わりに短いレポートに要約する、新しい非モーダルの Tools メニューダイアログを追加しました。フェーズごとの最終メッシュ統計と反復回数、最終的なパッチ別レイヤー厚テーブル、topoSet のセル・面・点セットごとの 1 行要約、件数付きで重複排除された警告（致命的エラーは常に全文表示）を表示します。「Raw Log」タブでは加工前のテキストもすぐ確認できます。非モーダルであるため、辞書の設定内容とログの結果を見比べながら、ツリーやエディタの横に表示したまま作業を続けられます。ダイアログを閉じても次回開いたときに表示されなくなる不具合を修正し、別のケースを開くとアプリの他のパネルと同様にそのケースの最新ログへ自動的に切り替わるようになりました。
- **シンタックスハイライトのキーワード抽出を強化し、インストールを選択可能に** — **Settings > Generate OpenFOAM Keywords…** の背後にあるキーワードスキャナが、`src/` と `applications/` のソースに含まれる辞書読み取り呼び出し（`lookup("…")`、`get<…>("…")`、`readEntry("…")` など）も grep するようになりました。`controlDict` の `application`、`writePrecision`、`timePrecision` のような自由形式の辞書キーが実際に名前として現れるのはここであり、これまでスキャンしていた caseDicts テンプレートや `TypeName` マクロには一切登場しませんでした。同梱のキーワード一覧も新しいスキャンで再生成した（約 3,100 → 約 5,200 キーワード）ため、これらのキーは最初からハイライトされます。ダイアログには Find OpenFOAM Examples と同じ自動検出付きの **Installation** セレクタ（記憶されるディレクトリも共有）が追加され、OpenFOAM 環境を source しなくても任意のインストール済みバージョンからキーワード一覧を構築できます。CLI では `tools/generate_foam_keywords.py --dir <インストールルート>` が対応します。生成される JSON には来歴情報（`source`、`version`、`generated`、ライセンスに関する `note` — ファイルにはキーワード識別子名のみが含まれます）が記録されるようになりました。
- **同梱デフォルトのキーワード一覧をユーザー生成分から分離** — 同梱ベースラインは `app_config/foam_keywords.default.json`（git 管理、現在は OpenFOAM v2512 から生成）に移り、**Generate OpenFOAM Keywords…** は従来どおり `app_config/foam_keywords.json` に書き込みますが、このファイルは gitignore 対象になり、存在する場合にデフォルトより優先されるだけになりました。一覧を再生成しても git チェックアウトが汚れることはなく、ユーザーファイルを削除すれば同梱デフォルトに戻ります。

### 不具合修正

- 擬似 2D ケース（例: damBreak）で **Boundary faces** をチェックしても何も起きないように見える問題を修正しました。このようなケースは薄い側面の壁だけを `boundary` に列挙し、大きな前面・背面は blockMesh の暗黙の `defaultFaces` パッチに属するため、これまで一切描画されていませんでした。どのパッチにも割り当てられていない外部ブロック面が `defaultFaces` として抽出され、**Boundary faces** がチェックされているときに薄いグレーの面として描画されるようになったため、2D ケースでもチェックボックスの効果が目に見えるようになりました。
- **View Log Summary…** が **Tools** メニューに加えて **View** メニューにも表示されるようになりました（両方とも同じアクションです）— 「View」で始まる項目は View メニューで探されがちなためです。
- Tools メニューの欠けていた日本語訳（メニュー名、各アクションのラベルとツールチップ、Restore 0/・Run blockMesh/snappyHexMesh/topoSet・Open Mesh in ParaView の確認/警告ダイアログ）に加え、**Settings > Generate OpenFOAM Keywords…** と BlockMesh 並列表示トグルのツールチップの訳を追加しました — これらは日本語モードでも英語で表示されていました。
- 比較モードの **Use this value** が `#includeFunc …` ディレクティブのような名前を持たないエントリを正しく扱えない不具合を修正しました。ディレクティブにはキー名がないため、このアクションは囲むブロック自体を対象にしてしまい、`functions {}` 辞書全体を 1 行のディレクティブで上書きする可能性がありました。名前のないエントリは内容で照合して正しいブロックの末尾に追加され、同一のエントリが既にある場合は複製せずスキップされます。また、エントリを囲むブロックが現在のケースに存在しない場合に適用を拒否しなくなりました。不足している親辞書（例: `functions {}`）は自動的に作成されるため、参照ケースや例のケースで見つけた設定を、そのブロックを持たないケースにも取り込めます。
- `save_file()` / `save_all_files()` がファイルをその場で書き換えた後、ディスク由来のファイル一覧インジケーター（新しいメッシュ staleness フラグを含む）を更新しない不具合を修正しました。`QFileSystemWatcher.directoryChanged` はディレクトリエントリの追加・削除にのみ反応し、既存ファイルのその場での書き換えには反応しないため、両方の保存パスが `_reload_file_list()` を直接呼び出すようにしました。
- BlockMesh 3-D パネルの **⊞** サイドバイサイド切り替えボタンが `blockMeshDict` がアクティブなファイルのときしか有効にならない不具合を修正しました。パネルはすでに `topoSetDict`/`snappyHexMeshDict` のジオメトリも同じビューにオーバーレイ表示しているため、これら 2 つのファイルタイプでもボタンが有効になるようにしました。
- 別のケースを開いたときに `topoSetDict`/`snappyHexMeshDict` のオーバーレイ状態（シェイプ、シェイプ単位のメニューチェックボックス、保持点マーカー、**Export Shapes as STL…** アクションの有効/無効状態）がクリアされない不具合を修正しました。新しいケースディレクトリを開く処理は、パネル自身の `clear()` を呼ぶ代わりに内部のシェイプ一覧だけを直接リセットしていたため、新しいケースの `topoSetDict`/`snappyHexMeshDict` がたまたま読み込まれるまで、前のケースのメニュー項目・`locationInMesh`/`locationsInMesh` マーカー・3-D オーバーレイが表示されたままになっていました。
- エディタの数値ハイライトが識別子の内部にはみ出す不具合を修正しました。単語にくっついた数字 — `wall0` のようなパッチ名の `0`、`inlet-1` の `1`、`0wall` の先頭の `0` — が、独立した数値であるかのようにティール色に着色されていました。数値ルールは前後両側で境界判定されるようになり、本来の数値（`0.05`、`-1e-05`、ベクトル成分）には影響ありません。
- **Settings > Generate OpenFOAM Keywords…** をただ開いて閉じるだけで、現在のファイルが誤って編集済み（タイトルの `*`）とマークされる不具合を修正しました。ダイアログの後に実行される再ハイライトは、書式しか変わっていなくても Qt の `textChanged` を発火させます。この処理はエディタパネルのプログラム的更新ガード（Highlight ツールバートグルが既に使っていたものと同じ）を通るようになり、実際の編集だけがファイルを編集済みにします。
- xterm モードから Simple ターミナルに切り替えた直後、作業ディレクトリがケースディレクトリではなくアプリケーション自身のディレクトリになる不具合を修正しました。差し替え後のターミナルウィジェットにケースディレクトリが伝えられる時点ではシェルプロセスがまだ起動中で、プロセスが `Running` 状態でないために `cd` コマンドが黙ってスキップされていました。プロセスが `NotRunning` でない限りコマンドを送るようにしました — `QProcess` は起動中の書き込みをバッファし、シェルの起動完了後に流し込みます。
- **Apply Text to Tree** が `snappyHexMeshDict` の 3-D オーバーレイを更新しない不具合を修正しました。適用パスはファイル名→ビューアのディスパッチを独自に複製しており、そこに snappyHexMesh のケースが欠けていたため、テキストエディタで編集したジオメトリはファイルを保存するかツリー経由で編集するまで 3-D ビューに反映されませんでした。読み込み・保存・ツリー編集・適用のすべてのパスが単一のディスパッチヘルパーを共有するようになりました。

### ドキュメント

- **マニュアルでの機能の見つけやすさを改善** — `USER_GUIDE.md` の「目的別ガイド」テーブルを 27 行から 41 行の目的別の項目に拡充しました（例: `topoSetDict` ジオメトリの 3-D 表示、フィールドファイルを開かずに全境界条件を一覧、形状の STL エクスポート、パッチの一括リネーム、foamMonitor での残差プロット）。目次にも、これまで抜けていた Tools 各セクション、**Generate OpenFOAM Keywords**、**同梱サンプルケース**、BlockMesh パネルのサブセクションを追加しました。`README.md` の「主な機能」は、各グループ見出しがユーザーガイドの対応セクションへディープリンクするコンパクトな概要に整理し、ターミナルを必要としない Tools アクションを「統合ターミナル」から独立した **ツールメニュー** グループへ移動、抜けていた Allrun/Allclean/Clean Case も追加しました。`DEVELOPER.md` にはドキュメントマップ（各ドキュメントの役割と、ユーザーに見える機能を追加したら「目的別ガイド」と目次にも追加するというルール）を追加し、プロジェクト構成ツリーに `tutorials/` と開発者ガイド・リリースノートのファイルを載せました。
- **SoftwareX 掲載論文の書誌情報を追加** — README の引用セクションとトップの告知が、掲載済み論文の正式な書誌情報 — *SoftwareX*, Volume 35, 2026, 102852, [doi:10.1016/j.softx.2026.102852](https://doi.org/10.1016/j.softx.2026.102852) — を記載するようになり、「採録済み・詳細は近日追記」のプレースホルダーを置き換えました。

### 内部変更

**リファクタリング: JSON 設定ヘルパーの共通化**

- `app_config/json_io.py`（`load_json`/`save_json`）を追加し、`AppConfigManager` と `CaseFilesConfig` がそれぞれ個別に実装していた読み込み・破損時のフォールバック・書き込みロジックをこれに置き換えました。機能変更はありません。

**リファクタリング: `snappyHexMeshDict` スキーマをパッケージへ分割**

- `schemas/snappy_hex_mesh_dict.py`（776 行、70 キー）を `schemas/snappy_hex_mesh_dict/` パッケージへ変換し、サブドメイン別（geometry、castellated mesh、snap controls、layers、mesh quality）のサブモジュールを `__init__.py` で統合するようにしました。スキーマレジストリが使用する `schemas.snappy_hex_mesh_dict` のインポートパスは変わりません。機能変更はありません。

**リファクタリング: `AppState` の diff フィールドを `DiffState` サブデータクラスへ**

- `AppState.diff_case_dir` と `diff_parsed_roots` を、既存の `FoamMonitorState` と同様のパターンで `DiffState` サブデータクラス（`AppState.diff`）にまとめました。機能変更はありません。

**`ruff`/`mypy` のスコープを拡大**

- `ruff` と `mypy` の対象に、既存の `foam/` と `model/` に加えて `app_config/`、`schemas/`、`services/`、`ui/app_state.py` を追加しました。クリーンな状態にするため、これらのディレクトリに残っていた既存の lint/型の問題（スキーマの説明文字列の行長調整、型注釈の不足2件）を修正しました。`ui/` の残りの部分は引き続き対象外です。

**リファクタリング: UI 周りの重複排除と、抽出・整形ヘルパーの共通化**

- BlockMeshPanel の `topoSet ▾` / `snappyHexMesh ▾` メニュー — 従来は約 25 個の属性が 2 系統並列に存在し、再構築・可視判定メソッドも鏡写しに重複していました — を、単一の `_ShapeOverlayMenu` ヘルパークラスの 2 インスタンスに置き換えました。挙動の違いは 1 点のみ: snappy の保持点トグルの有効/無効が、次回の再構築時ではなくマスタートグルの切り替えに即座に追従するようになりました。
- Tools メニューの実行系アクションは `_run_in_terminal()` / `_confirm_rerun_over_results()` ヘルパーを共有し、メニュー構築はループ化され、繰り返されていた `QMessageBox.question` ブロックは共通の `_confirm()` はい/いいえヘルパーに置き換えました。
- `ui/mixins/_boundary_ops.py` に `_find_boundary_field` / `_append_new_patch` / `_set_patch_children` ヘルパーを追加し、5 箇所に複製されていた boundaryField 検索と、繰り返されていたパッチ作成・置換ブロックを置き換えました。
- リーフ値の整形を共通化しました。`foam/utils.py` の新しい `format_leaf_value()` をライター（直列化）とツリーモデルの Value 列（表示）の両方が使うため、表示と直列化が食い違うことはなくなりました。
- `foam/topo_set_extractor.py` と `foam/snappy_hex_mesh_extractor.py` の間で重複していた sphere/cylinder/cone のジオメトリ解決を、`foam/tree_utils.py` の共有リゾルバ（`resolve_sphere_geometry`、`resolve_cylinder_geometry`、`resolve_cone_geometry`）へ移動しました。
- カプセル化: 他モジュールから呼ばれている `FoamTreeModel.index_of_node`/`attach_parents` と `BlockMeshPanel.init_plotter` をパブリックにし、`FileListPanel` に `file_paths()` アクセサを追加して、diff の事前計算が内部のリストウィジェットへ直接アクセスしないようにしました。
- `i18n.tr()` は、呼び出しのたびに言語モジュールを再インポートする代わりに、`set_language()` 時にキャッシュした変換テーブルを参照するようになりました。

## v1.7.0 — 2026-07-04

### 新機能

- **`topoSetDict` の `actions` ブロック**を、不透明な生テキストではなく構造化ツリーとしてパースするようになりました。各無名 `{ … }` エントリが `action_entry` ノードになり、その子ノード（`name`、`type`、`action`、`source`、`box`、`set` など）をツリービューで個別に表示・編集できます。差分比較はアクションエントリを位置ベースで照合します。
- **テキストエディタのシンタックスハイライト** — プレーンテキストエディタで OpenFOAM 辞書ファイルのトークンが色付けされるようになりました: `//` および `/* */` コメント（グレー・斜体）、文字列リテラル（緑）、`#ディレクティブ`（紫・太字）、`$マクロ` 参照（オレンジ）、`FoamFile`・`true`/`false`・`uniform`・`nonuniform` などの予約済みキーワード（青・太字）、数値（ティール）。PySide6 組み込みの `QSyntaxHighlighter` を使用しており、新たな依存関係はありません。
- **テキストエディタのコード折りたたみ** — 複数行にまたがる `{ … }` ブロックに加え、2 行以上連続するコメント行（複数行の `/* … */` ブロックや連続する `//` 行）ごとに、行番号ガターにクリック可能な ▾/▶ 三角形が表示されるようになりました。三角形をクリックすると領域の折りたたみ・展開を切り替えられます。ファイルを読み込むと `FoamFile { … }` ヘッダーとファイル先頭のコメントバナーはどちらも自動的に折りたたまれます。折りたたみは表示のみの操作で、テキスト本文は変更されません。
- **BlockMesh 3-D パネルへの `topoSetDict` ジオメトリの表示** — `topoSetDict` を読み込んだり編集したりすると、各アクションのジオメトリソースが 3-D パネルにオーバーレイ表示されるようになりました。対応ソースタイプ: `boxToCell/Face/Point`（ボックス）、`sphereToCell/Face/Point`（球）、`cylinderToCell/Face/Point/AnnulusToCell`（円柱）、`coneToCell/Face/Point` および `coneAnnulusToCell`（円錐台。`radius2` が 0 のときは真円錐）。円柱・円錐では、新しい `point1`/`point2` と旧来の `p1`/`p2` の両方の軸点キー名を受け付けます。シェイプはアクション種別で色分けされます（`new` = スチールブルー、`add` = 緑、`subtract`/`delete` = 赤、`subset` = 紫、`invert` = ゴールド）。半透明で描画されるため、下地のメッシュも視認できます。パネルツールバーの **「topoSet ▾」** メニューでオーバーレイ全体の表示・非表示を切り替えられるほか、各シェイプ（アクションごとに `name · source` で一覧表示）を個別に表示・非表示できます。`topoSetDict` のトップレベルに定義した `$変数` および `#eval{ 式 }` は抽出前に解決されます（`blockMeshDict` と同じ仕組みです）。

### バグ修正

- キーワードリストを拡張スキャンで再生成した後、シンタックスハイライトがサイレントに失敗する問題（`QRegularExpression` が無効 — "regular expression is too large"）を修正しました。ハイライターが値キーワードの交替パターンを 1,000 キーワードずつのチャンクに分割し、それぞれを独立した `QRegularExpression` オブジェクトとしてコンパイルするようになりました。これにより、キーワードリストの規模にかかわらず PCRE2 のコンパイル済みコードサイズ制限に達しなくなります。
- `ui/mixins/_boundary_ops.py` の `_on_patch_edit_requested`、`_on_patch_paste_requested`、`_on_patch_delete_all_requested`、`_on_patch_add_all_requested` が存在しない `_extract_boundary` を `ui.panels.boundary_view_panel` からインポートしていた問題を修正しました。正しくは `model.boundary_model` の `extract_boundary` をインポートします。
- ケース比較の差分バーにある **Side by side** チェックボックスが、比較を開いても効果がなかった問題を修正しました。チェックボックスがシグナル接続前に checked 状態で初期化されていたため、常に `True` のまま固定されていました。`_compare_with_case` が `setChecked(True)` を呼び出しても既に checked であるためシグナルが発火せず、比較パネルが表示されませんでした。
- **OpenFOAM キーワード生成**で、`CoProcessor()` や `Pipeline:` などの正規表現特殊文字を含むトークンがリストに混入し `QRegularExpression` パターンが無効になる問題を修正しました。ジェネレーターは完全な識別子パターン（`^[A-Za-z]\w+$`）にマッチするトークンのみ保存するようになりました。ハイライターのローダーにも同じフィルターを追加したため、既に生成済みのファイルは次回起動時に自動的にクリーンアップされます（再生成は不要です）。
- BlockMesh 3-D パネルの **「Vertices ▾」**・**「Blocks ▾」**・**「Scale ▾」**・**「topoSet ▾」** ドロップダウンメニューが、クリックのたびに閉じてしまい、複数のチェックボックスを続けて切り替えにくい問題（例: topoSet の複数シェイプを個別に表示・非表示する場合）を修正しました。チェック可能な項目をクリックしてもメニューは開いたままになり、メニュー外のクリック・Escape キー・ツールバーボタンの再クリックで閉じる従来の挙動は維持されます。
- **ツリーパネル**で値を直接編集(インラインセル編集)しても、ファイルが未保存としてマークされず、**「ツリーからテキストを再読込」**を実行してもテキストエディタが更新されない問題を修正しました。詳細パネル経由で同じ値を編集した場合は正しく動作していました。ツリーモデルの `dataChanged` シグナルを、詳細パネル編集と同じ後処理(テキストエディタの再生成・未保存マーク)に接続したことで、インラインでのツリー編集も詳細パネル編集と同じ挙動になりました。

### 改善

- **topoSet シェイプの個別表示** — BlockMesh 3-D パネルの topoSet オーバーレイ操作を **「topoSet ▾」** メニューに変更しました。マスターの **Show topoSet geometry** トグルに加えて、読み込んだ `topoSetDict` の各シェイプ（`name · source` で表示）を個別のチェックボックスで表示・非表示できるため、特定のボックス・球・円柱・円錐ソースだけを表示したり隠したりできます。新しい `topoSetDict` を読み込むと一覧はすべて表示状態にリセットされます。メニューには **アクション色の凡例**（`new`・`add`・`subtract`・`subset`・`invert`）も表示され、各シェイプ行にはそのアクションに対応する色見本が付くため、メニューと 3-D ビューの色が直接対応します。
- **topoSet の中空環状体・`rotatedBox`・非ジオメトリソースの一覧表示** — BlockMesh 3-D パネルで、`cylinderAnnulusToCell` と `coneAnnulusToCell` が（`innerRadius` / `innerRadius1` / `innerRadius2` を反映して）中実ではなく本当に中空のチューブとして描画されるようになりました。`rotatedBoxToCell/Face/Point`（`origin` + `i`/`j`/`k` のスパンベクトルで定義される斜め方向のボックス）もオーバーレイ表示されます。描画対象のジオメトリを持たないソース（`cellToFace`、`zoneToCell`、`fieldToCell`、`surfaceToCell` など）は、**「topoSet ▾」** メニューに *"(no geometry)"* としてグレーアウト表示され、辞書内に存在することが分かるようになりました。
- **シンタックスハイライトのトグル** — エディタツールバーに **Highlight** ボタンを追加しました。ワンクリックでシンタックスカラーリングのオン／オフを切り替えられます。設定は `app_config.json` に保存され、次回起動時に復元されます。
- **シンタックスハイライトのキーワードリストを拡充** — **OpenFOAM キーワード生成** スキャナーが、C++ ヘッダーの `ClassName("…")` マクロと実装ファイルの `addNamedToRunTimeSelectionTable` / `addNamedToMemberFunctionSelectionTable` ルックアップ名も抽出するようになり、ベースラインのキーワード数が約 2,200 から約 3,100 に増加しました。辞書のキー名（`vertices`、`blocks`、`edges`、`boundary`、`dimensions`、`internalField`、`boundaryField`、`solvers` など）もダーク・シアンでハイライトされるようになりました。caseDicts スキャンがノード値に加えてノード名を収集するようになり、またハイライターが起動時にスキーマレジストリからキーセグメントを抽出するため、キーワードファイルを再生成しなくても一般的なキーがカバーされます。
- **パーサ／モデル層の型安全性強化** — `FoamNode.node_type`（`foam/nodes.py`）が単なる `str` ではなく、パーサ／ライタ／ツリーモデルが実際に生成するすべての値からなる `Literal` 型になりました。開発依存として追加した `mypy`（`foam/` と `model/` にスコープ）がこれを検証し、`tests/test_lint.py` によって通常のテスト実行の一部としてチェックされます。lint 用の `ruff` も同様です。機能・挙動の変更はありません。

**`tutorials/` の同梱チュートリアルセットを拡充**

- `tutorials/cavity/` を OpenFOAM v2512 標準チュートリアルの 3 つの icoFoam サブケースをまとめるコンテナに変更しました: `cavity/`（単一リージョンのワークフロー解説）、`cavityGrade/`（非一様 `simpleGrading`）、`cavityClipped/`（クリップ形状、`mapFieldsDict`）。コンテナレベルに `Allclean`/`Allrun` スクリプトも含まれます。
- OpenFOAM v2512 標準チュートリアルから `tutorials/damBreak/`（interFoam、層流）を追加しました。`defaultFieldValues`/`regions` ブロックを持つ `setFieldsDict`、`0.orig/`、および `sampling` 関数オブジェクト辞書のテストに使用します。
- cavity をベースにしたカスタム icoFoam `blockMeshDict` ケースを 4 件追加しました: `oneBlocks`（3-D 単一ブロック）、`oneBlocks-vars`（変数置換とコンパクト `(blockId faceId)` 面記法）、`nineBlocks`（3×3 マルチブロック、正規表現境界パッチ）、`nineBlocks-vars`（変数置換 + コンパクト記法）。
- `tutorials/topoSetShapes/` を追加しました。単一の 3×3×3 ブロック上に、BlockMesh 3-D パネルがオーバーレイ表示できるすべてのジオメトリソース（`boxToCell`、`rotatedBoxToCell`、`sphereToCell`、`cylinderToCell`、中空の `cylinderAnnulusToCell`、切頭円錐の `coneToCell` フラスタム、`radius2 0` の真円錐 `coneToCell`、中空の `coneAnnulusToCell`）に加えて非ジオメトリの `cellToFace` アクションを、`$変数` および `#eval` 解決とあわせて配置した `topoSetDict` のデモです。`system/topoSetDict` を開いて **「topoSet geometry」** にチェックを入れると、アクション別に色分けされた形状が表示されます。

---

## v1.6.1 — 2026-06-19

### バグ修正

- `tutorials/` の README を修正しました。GPLライセンスも追加しました。

## v1.6.0 — 2026-06-19

### 改善

**サンプルケースを `tutorials/` に同梱**

- 2 件の OpenFOAM ケースを `tutorials/` ディレクトリに収録しました: `cavity`（icoFoam、単一リージョンのワークフロー解説）と `snappyMultiRegionHeater`（chtMultiRegionFoam、マルチリージョン）。
- 各ケースは OpenFOAM v2512 の標準チュートリアルセットから取得し、GPL-3.0 でライセンスされています（FoDE ソースコードの AGPL-3.0 とは別）。詳細は `tutorials/tutorials_README.md` を参照してください。

**マルチリージョン：`0/<region>/` 配下のフィールドファイルをファイル一覧と境界条件パネルに表示**

- `0/<region>/` および `0.orig/<region>/` 内のフィールドファイル（例: `0/heater/T`、`0/bottomWater/p`）が、`0/heater` や `0/bottomWater` のようなグループヘッダーのもとにファイル一覧へ自動的に追加されます。
- **Boundary** パネルのディレクトリセレクターに、`0` と `0.orig` に加えてリージョンごとのサブディレクトリ（例: `0/bottomWater`、`0/heater`、`0/topAir`）が表示されるようになりました。リージョンを選択すると、そのリージョンのフィールドファイルに絞り込んだ境界条件テーブルが表示されます。
- 以前は `0/` および `0.orig/` の直下ファイルのみがスキャン対象で、リージョンサブディレクトリは無視されていました。

**BlockMesh ビューア：コンパクト境界面記法 `(blockIndex, faceIndex)` のサポート**

- OpenFOAM の新しいコンパクト境界面記法 — `(blockIndex faceIndex)` — が、従来の 4 頂点インデックス記法 `(v0 v1 v2 v3)` と並んでサポートされるようになりました。
- コンパクト記法は標準的な六面体ブロック面テーブル（面 0 = −x、1 = +x、2 = −y、3 = +y、4 = −z、5 = +z）を使用して 4 頂点インデックスリストに展開されます。両方の記法を同じファイルで混在させることができます。
- 以前は、コンパクト面エントリは `len < 3` ガードによって 2 整数タプルが除外されて無視されていたため、新しい記法を使用した境界パッチが 3D ビューに表示されませんでした。

**GUI：トップバーの再配置と Tools メニューの追加**

- トップバーに **Reload Case** ボタンを追加しました。**Save All Files** とセパレーターの間に配置されており、**Case > Reload Case** と同じ操作をワンクリックで実行できます。
- **foamMonitor…** ランチャーをトップバーから新しい **Tools** メニュー（**Settings** と **Help** の間）に移動しました。foamMonitor はファイル編集機能ではなくランタイム監視ツールであるため、編集系ボタンと分離することで役割の違いを明確にしました。実行中の状態表示（メニュー項目が **■ foamMonitor** に変わる）およびクリックで停止する動作は従来どおりです。

### バグ修正

- **BlockMesh ビューア：否定マクロ変数定義の解決** — `xMin -$xMax;` のような形式（`$参照` の前にマイナス符号）の変数定義は、パーサーがそれらを `macro` ノードではなく `word` ノードとして分類するため、`_build_var_map` で無視されていました。マクロ解決パスは `macro` 型のノードのみを処理していたため、`($xMin $yMin $zMin)` のような頂点が 3D ビューに表示されませんでした。新しい word/compound 算術評価パスを追加し、`xMax` が既知になった時点で `-$xMax` が正しく解決されるようにしました。

- **Compare Cases：サイドバイサイドパネルが即座に表示されるように修正** — "Side by side" チェックボックスはデフォルトでオンになっています。比較を開始したとき、チェックボックスの値がすでに `True` だったため Qt の `toggled` シグナルが発火せず、比較パネルが表示されませんでした。ユーザーが手動でチェックを外してから再度オンにするまで表示されないという問題がありました。`setChecked` の直後にトグルハンドラーを直接呼び出すことで修正しました。

### 内部変更

**リファクタリング: `ui/` をサブディレクトリへ整理**

- ダイアログ 14 ファイルを `ui/dialogs/`、パネル 7 ファイルを `ui/panels/`、低レベルウィジェット 3 ファイルを `ui/widgets/`、8 つの `_*_ops.py` Mixin ファイルを `ui/mixins/` へ移動しました。オーケストレータのコア（`main_window.py`、`app_state.py`、`layout_constants.py`）は `ui/` 直下に残ります。機能変更はありません。

**リファクタリング: `tests/` をソース構造に合わせてサブディレクトリへ分割**

- `tests/` 直下にフラットに置かれていた全テストファイルを、ソース構造に対応したサブディレクトリへ移動しました: `tests/foam/`、`tests/model/`、`tests/ui/`、`tests/services/`、`tests/app_config/`、`tests/schemas/`。`conftest.py` は `tests/` 直下に残ります。機能変更はありません。

**リファクタリング: 共有状態を `AppState` データクラスへ集約**

- `ui/app_state.py` を追加し、`MainWindow` の bare `self.X` 属性として散在していた 18 個の共有可変フィールド（現在のファイル・ツリー、ファイルバッファ、ダーティ追跡、内部フラグ、ケース設定、差分状態、foamMonitor 状態、パネル状態）を `AppState` データクラスにまとめました。`MainWindow.__init__` では `self.state = AppState()` の 1 行で初期化され、8 つの Mixin すべてが共有データを `self.state.<field>` としてアクセスします。これにより Mixin 間の依存関係が明示的かつ grep 可能になりました。機能変更はありません。

## v1.5.0 — 2026-06-10

### 改善

**BlockMesh ビューア：サイドバイサイドモード**

- `blockMeshDict` がアクティブなファイルで BlockMesh パネルが利用可能なとき、上部タブウィジェットの右上に **⊞** トグルボタンが表示されます。
- クリックするとツリーの右隣に 3D ビューが水平スプリッターで並列表示され、ツリーの編集と 3D ビューを同時に確認できます。サイドバイサイドモード中は独立した **BlockMesh** タブが非表示になり、モードを切ると復元されます。
- 3D ビューは自動更新されません。ツリーを編集した後は BlockMesh パネルの **Refresh** をクリックしてください。
- xterm ターミナルがアクティブな間はボタンが無効化されます（GPU/OpenGL 競合のため）。

**比較パネル：比較モード以外では非表示に**

- 参照ケースが読み込まれていないときは、参照ケースのツリーペインが完全に非表示になりました。従来はスプリッターの非表示スロットとして存在し、メインツリーと詳細ペインの間に細いスプリッターハンドルの隙間が残っていました。
- ペインは差分バーで **Side by side** にチェックを入れたときに表示され、チェックを外すか **Clear** をクリックすると非表示になります。

**BlockMesh ビューア：変数ベースメッシュのプレビューモード**

- `vertices` ブロックに変数参照（`$varName`）が含まれる場合、頂点テーブルの X/Y/Z セルはデフォルトで読み取り専用になりました（直接書き込みは無言で失敗しており、誤解を招いていました）。
- Vertices パネルの上部（グループボックス内、メインツールバーではない）に **⚙ Variable-based** チップと **Preview** トグルボタンが表示されます。**Preview** をクリックするとプレビューモードになり、テーブルセルが編集可能になって各変更が即座に 3D ビューへ反映されますが、ツリーとファイルは変更されません。プレビューモード中は黄色いバナーが表示されます。
- **Refresh** をクリックするとプレビューモードを終了してツリーの頂点座標に戻ります。
- 変数参照を含まないメッシュでは従来どおり、テーブルへの編集がツリーとエディタに即座に反映されます。

**BlockMesh ビューア：多段変数チェーンの解決**

- `blockMeshDict` 内の任意の深さの変数チェーンを完全に解決できるようになりました。従来は `z001 $z1;` のようなマクロ参照が `z1 #eval{$z0+$dz0};` で定義されていた場合、マクロパスが `#eval` 評価より先に実行されるため、数値ではなく生の式文字列が残っていました。
- 固定の 3 パス方式を、マクロ参照の解決と `#eval` 式の評価を交互に繰り返す反復ループに置き換えました。反復上限はトップレベルの変数定義の数であり、これは非循環依存グラフの理論的な最大深度に等しいです。
- 循環参照（`a $b; b $a;` など）は、無限ループやクラッシュを起こすことなく未解決のまま静かにスキップされます。

---

## v1.4.0 — 2026-06-09

### 新機能

**foamMonitor ランチャー**

- トップバーの **foamMonitor…** ボタンからダイアログを開き、`foamMonitor` を起動して残差などのデータを gnuplot でプロットできます。
- ダイアログではファイル（ソルバーログや `postProcessing/` の出力ファイル）の選択、オプション（対数スケール、グリッド、リフレッシュ間隔、アイドルタイムアウト）の設定、追加オプションの自由入力ができます。
- foamMonitor の実行中はボタンが **■ foamMonitor** に変わり、クリックするとプロセスを停止します（foamMonitor シェルと gnuplot ウィンドウの両方を終了）。新しいケースを開いたときも実行中のインスタンスは停止されます。
- 新しい gnuplot バージョンで非推奨となった `reread` コマンドを起動時に自動的にパッチし、モダンな `load ARG0` 相当に置き換えます。gnuplot のバージョンに関係なくウィンドウが正しくリフレッシュされます。
- 指定したファイルが存在しない場合や foamMonitor がエラーで終了した場合は警告ダイアログを表示します。

**BlockMesh ビューア：変数と `#eval` 式のサポート**

- `blockMeshDict` 冒頭の変数定義（例：`xMax 0.5;`、`nCell 20;`）が、3D ジオメトリ抽出前に `vertices` および `blocks` セクションで置換されるようになりました。`$varName` および `${varName}` の両参照形式に対応しています。
- 他の変数を参照するマクロ変数（`nx $nCell;`）を 1 レベル深まで解決します。
- 算術式（`zMax #eval{ $length / $nCell };`）は変数置換後に評価されます。対応演算子：`+`、`−`、`*`、`/`、括弧。
- 従来は未解決の `$variable` 参照により `float()` 変換が失敗し、頂点が欠落したり、最悪の場合は範囲外の頂点インデックスによる VTK クラッシュが発生していました。
- **レキサー修正：** 内部にスペースのない `#eval{expr};` 形式が、セミコロンを含む単一のディレクティブトークンとして解析され、パーサーが直前の変数の値として `vertices` ブロック全体を消費してしまう問題を修正しました。レキサーは `{` でディレクティブトークンの読み取りを停止するようになり、深さ追跡が正しく機能します。

**ドラッグ＆ドロップによるケースオープン**

- ファイルマネージャからケースディレクトリをウィンドウ上の任意の場所にドロップするとケースを開きます。ツリービュー、エディタ、ファイル一覧など、すべてのパネルがドロップ対象として有効です。
- 現在開いているファイルに未保存の変更がある場合は、新しいケースを読み込む前に確認ダイアログが表示されます。

---

## v1.3.0 — 2026-06-01

### 改善

**ケース比較の高速化・ノンブロッキング化**

- ファイル一覧の差分件数をイベントループ 1 ティックごとに 1 ファイルずつ段階的に計算するようになりました。UI はブロックされず、すべてのファイルの解析完了を待たずに `≠` マーカーが順次表示されます。
- `FoamFile` ヘッダーを持たない大きなファイル（`log.simpleFoam` や残差出力など）は、差分計算および境界条件パネルの読み込み時に自動的にスキップされます。100 KB 未満のファイルは常に解析を試みるため、`FoamFile` ブロックを持たない小さなカスタムソルバー辞書は引き続き正常に動作します。
- 大きな非ディクショナリファイルを開く際に確認ダイアログが表示されるようになりました。ツリービューが利用できないこと、および読み込み中にアプリケーションが応答しなくなる可能性があることを警告します。確認後はステータスバーにメッセージが表示されます。

### 新機能

**UI 言語選択（英語 / 日本語）**

- **Settings > Language** サブメニューを追加。**English** または **日本語** を選択するとアプリケーションが選択言語で再起動します。
- 言語設定は `app_config.json` に保存され、起動時に適用されます。デフォルトは英語です。
- 新規 `i18n/` モジュール: `tr()` 関数がアクティブな言語辞書を参照し、未翻訳のキーは英語文字列へ自動フォールバックします。
- 新しい言語（例: イタリア語）の追加は `i18n/<コード>.py` 翻訳ファイルを1つ作成するだけで完了し、他のコード変更は不要です。

---

## v1.2.0 — 2026-05-30

### 新機能

**Boundary ビュー — テーブルのコピー**

- Boundary パネルのツールバーに **Copy Table** ボタンを追加。ドロップダウンメニューで 2 つの形式を選択できます:
  - **Copy as Markdown** — GitHub Flavored Markdown のパイプテーブル。Lines per cell が 2 以上の場合、複数行セルは `<br>` タグで結合され GitHub 上で改行として表示されます。
  - **Copy as CSV** — RFC 4180 準拠の CSV。複数行セルは引用符付きフィールドとして保持され、Excel・LibreOffice Calc で正しく表示されます。
- どちらの形式も行ヘッダー列を含み、転置状態を反映します。

**ケース比較 — サイドバイサイド表示**

- 差分バーの **Side by side** トグルで中央パネルを左右に分割します。左ペインは編集可能な現在のケースのツリーを表示し、右に新しい **Reference** ペインが開いて対応するファイルを読み取り専用で表示します。
- 参照ケースのツリーでは、参照にしか存在しないキーを**薄緑色**でハイライト表示します。
- 参照ペインの任意のリーフノードを右クリックして **Use this value** を選択すると、対応するノードの値を現在のケースに即座に適用できます。適用後、差分ハイライトは自動的に更新されます。
- ファイル一覧の **Changed files only** チェックボックス: 差分のないファイルを非表示にし、参照ケースと異なるファイルのみを表示します。
- 差分マーカー（`≠N` / `≠0`）は、比較開始時に全ファイルに対して即座に計算されるようになりました（従来はファイルを開くたびに遅延計算していました）。

### 改善

**BlockMesh パネル**

- ツールバーを 1 行にコンパクト化しました。
- STL に加えて OBJ ジオメトリファイルの読み込みとオーバーレイ表示に対応しました（`Load STL / OBJ…` / `Clear STL`）。

**ファイル一覧**

- 追加ファイルのボタンを常に表示するようにしました（追加ファイルが登録されている場合は件数を表示）。

**ケース比較**

- 差分バーに **Side by side** トグルとカラーレジェンドを追加しました。

---

## v1.1.0 — 2026-05-23

本リリースでは、インタラクティブな BlockMesh 3D ビューア、差分オーバーレイによるケース比較、ケース全体にわたる境界パッチのリネーム、Boundary パネルからエディタへのナビゲーション、ツリー ↔ エディタのソース行同期など、複数の主要機能を追加しました。また、Windows 向けの起動バリアント（no-terminal）、追加ディレクトリの再帰スキャンオプション、snappyHexMeshDict スキーマの拡張、各種エディタおよびユーザビリティの改善も含まれています。

### 新機能

**BlockMesh 3D ビューア** *（`pyvista` および `pyvistaqt` が必要）*

- 新しい **BlockMesh** タブで `blockMeshDict` のジオメトリをインタラクティブに 3D プレビュー。頂点（赤い球）、ヘックスブロックのエッジ（ワイヤーフレーム）、境界パッチ面（パッチ種別で色分け: wall、inlet、outlet、symmetry など）を表示。
- 頂点、頂点ラベル、ブロックエッジ、ブロックラベル、境界面、軸、グリッド、寸法テキストの表示切替チェックボックス。
- **Block labels** — 各ヘックスブロックの重心にブロックインデックスを表示（濃青テキスト）。頂点ラベルと同様の機能。
- **Color blocks** — 各ヘックスブロックを定性的パレット（tab10）の異なる色で表示。ワイヤーフレームとソリッド面の両方に適用。
- **Solid blocks** — 半透明のソリッドヘックスブロック面（opacity 0.25）をワイヤーフレームに加えて表示。Color blocks のパレットを共有。
- **Label size** — 第 2 ツールバー行の共用スピンボックス（範囲 6–32、デフォルト 10）で頂点ラベルとブロックラベルのフォントサイズを一括調整。
- 視点方向ボタン（+X / −X / +Y / −Y / +Z / −Z / Iso）でカメラを素早く切り替え。
- パネル下部のマウス操作ヒントバー（ドラッグ = 回転、Shift+ドラッグ = 平行移動、スクロール = ズーム）。ホバーで詳細ツールチップ表示。**Help > Keyboard Shortcuts…** でも確認可能。
- 3D ビューと並んで表示される頂点テーブル（インデックス | X | Y | Z）。行クリックで頂点をハイライト、座標セルをダブルクリックで値を編集 — 変更は FoamNode ツリーとテキストエディタに即座に反映。
- STL ジオメトリファイルの読み込みとオーバーレイ表示（`Load STL…` / `Clear STL`）。
- **View > BlockMesh 3-D Panel**（チェック式）でターミナルモードとは独立してタブを表示・非表示に切り替え可能。xterm が有効なときはアクションが無効化され、GPU コンフリクトの理由を示すラベルに変わります。
- `no-terminal-blockmesh` バリアントでは常時表示。`standard` バリアントではターミナルがシンプルモードのときのみ表示。

**ケース比較**

- **Case > Compare with Case…** — 参照ケースのディレクトリを選択し、現在のケースと比較。
- アクションバー下の差分バーに参照パスとカラーレジェンドを表示。**Clear** をクリックすると比較モードを終了。
- ツリーオーバーレイ: 薄黄色 = 値の変更、薄青 = このケースのみに存在するキー。ハイライト行をホバーするとツールチップに参照値を表示。
- ファイル一覧のマーカー: `≠N`（アンバー）= 差分あり、`≠0`（グレー）= 確認済みで一致、マーカーなし = 未訪問。50 件超は `≠50+` に丸め。ファイルを開いたタイミングで遅延更新。

**境界パッチのリネーム（Rename Boundary）**

- **Boundary** パネルのセル・パッチ列/行ヘッダー、またはツリービューのパッチノードを右クリックして **Rename Boundary…** を選択。
- ダイアログにパッチ名が含まれるすべての読み込み済みファイル（`blockMeshDict` の境界エントリおよびフィールドファイルの `boundaryField` パッチキー）が一覧表示されます。更新するファイルを選択し、新しい名前を入力して **Rename** をクリック。選択したすべてのファイルが一括でメモリ上に反映され、ダーティマークが付きます。

**Boundary パネルのシングルクリックナビゲーション**

- Boundary パネルのセルをクリックすると、対応するフィールドファイルをエディタで開き、該当パッチエントリにスクロールしてアンバーハイライトで表示。**Auto-scroll editor** チェックボックスでこの動作を切り替えられます。
- クリックしたセルが現在開いているファイルと異なる場合、ファイル一覧とエディタが自動的に更新されます。

**ツリー ↔ エディタ ソース行同期**

- ツリーノードをクリックすると、対応するソース行がテキストエディタでアンバー背景でハイライトされ、スクロールします。
- **Find in Tree** ボタン（または `Ctrl+Shift+T`）で現在のエディタカーソル行のソース範囲をカバーする最も深いツリーノードを選択します。
- エディタテキストが未保存の間（行番号が古い状態）はジャンプが無効化され、チェックボックスのラベルとツールチップで状態を表示します。

**ツリーキーフィルター**

- ツリー上部のフィルターバーで大文字小文字を区別しないキーワード絞り込みが可能。子孫ノードが一致する場合は親ノードも表示されます。

**ケースの再読み込み（Case Reload）**

- **Case > Reload Case** — メモリ上の編集をすべて破棄し、ディスクからケースを再読み込み。未保存ファイルがある場合は件数を示す確認ダイアログを表示してから実行します。

**`0/` ディレクトリの削除**

- ファイル一覧の `0` グループヘッダーを右クリックして **Delete '0' directory…** を選択すると、`0/` ディレクトリをディスクから完全削除。`0.orig` が存在するときのみ表示されます。削除前にフルパスを示す確認ダイアログを表示します。

**追加ディレクトリの再帰スキャン**

- 追加ディレクトリをフラットスキャン（直下ファイルのみ、デフォルト）または再帰スキャン（`rglob` でサブディレクトリ含む）に設定できるようになりました。
- **Settings > Manage Extra Files & Directories…** > **Extra Directories** タブでエントリを選択し **Toggle Recursive** をクリックすると再帰スキャンを有効・無効に切り替えられます。有効なエントリは `[recursive]` と表示されます。
- サブディレクトリにファイルを含む結果ツリー（例: `validation/`）などに便利です。

**アプリケーションバリアント**

- `--variant` フラグで起動設定を選択: `standard`（ターミナル + BlockMesh）、`no-terminal`（Windows 向け）、`no-terminal-blockmesh`（BlockMesh 常時表示・ターミナルなし）。選択したバリアントは `app_config.json` に保存され、次回以降の起動時に自動適用されます。
- ターミナルモードの切替（xterm ↔ シンプル）は Terminal タブ内のチェックボックスで行います。シンプルモードに切り替えると BlockMesh タブが表示されます。

**ツリーのコピー＆ペースト**

- ツリーパネルで `Ctrl+C` / `Ctrl+V` によるツリーエントリのコピー＆ペーストが可能。ショートカットはツリーウィジェットにスコープされており、下部のテキストエディタとは干渉しません。

### 改善

**View メニュー**

- **View > Show Type Column** — ツリービューの Type 列を表示・非表示に切り替え。
- **View > BlockMesh 3-D Panel** — パネルの表示切替（上記参照）。

**エディタツールバー**

- **Find Prev**（Shift+F3）と **Find Next**（F3）ボタンを追加。
- **Find in Tree** ボタン（`Ctrl+Shift+T`）— 現在のエディタ行から対応するツリーノードにジャンプ。
- ツールバー右側に **Line: N** として現在のカーソル行番号を表示。

**キーボードショートカットダイアログ**

- **Help > Keyboard Shortcuts…** ですべてのキーボードショートカットを一覧表示。
- 新しい **BlockMesh 3-D viewer (mouse)** セクションで回転・平行移動・ズーム・カメラリセット・フライトゥーポイントの操作方法を確認できます。

**snappyHexMeshDict スキーマ**

- `snappyHexMeshDict` のスキーマカバレッジを拡張: `snapControls`、`addLayersControls`、`meshQualityControls` のキーを追加。
- ユーザー定義名のサブ辞書（ジオメトリエントリ、細分化サーフェス/リージョン、レイヤーパッチグループ）に対して祖父キーコンテキストを使ったスキーマ検索に対応。ユーザー定義名に関わらず正しいスキーマが表示されます。

**パーサー**

- 括弧ブロック内（頂点リストなど）のインライン `//` コメントが無音で破棄されていたバグを修正。コメントはブロック内の `unknown_raw_entry` ノードとして保持されるようになりました。

**Boundary パネル**

- セルおよび列/行ヘッダーの右クリックメニューに **Rename Boundary…** を追加。
- パッチ列/行ヘッダーを右クリックして全フィールドファイルへの一括パッチ追加・削除が可能。

**ファイル管理**

- パーサーが完全に解釈できなかったエントリ（`unknown_raw_entry` ノード）をツリーでアンバー色でハイライト表示し、通常エントリと区別しやすくなりました。

---

## v1.0.0 — 初回公開リリース

初回公開リリース。主要機能:

- OpenFOAM 辞書ファイルの構造化ツリービューおよびテキストエディタ。
- `TARGET_FILES`、`0/`、`0.orig/` を自動スキャンするファイル一覧。追加ディレクトリの登録も可能。
- 境界条件テーブル（閲覧・編集・作成・削除・コピー・ペースト・全ファイル一括追加/削除）。
- `controlDict`、`fvSchemes`、`fvSolution`、`blockMeshDict`、`snappyHexMeshDict` の組み込みスキーマヘルプ。
- フル PTY xterm.js ターミナル（Linux/macOS）。ケースを開くと自動でそのディレクトリへ移動。
- ケースライブラリ（**Case > Duplicate from Case Library**）でチュートリアルを作業ディレクトリにコピー。
- 現在のケースを新規ケースとして保存 / ケースの複製。
- 個人用参照リンク（**Help > Resources…** > **My Links** タブ）。
- AGPL-3.0-or-later ライセンス。
