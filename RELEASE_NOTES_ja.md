# リリースノート

## v1.15.0 — 2026-08-13

### 改善

- **上部アクションバーが本物のツールバーになりました** — `QPushButton` は常に縁取りのある矩形として描画されるため、この行はすぐ下の **Tree** / **Boundary** / **BlockMesh** のタブバーのすぐ上に並び、2 段目のタブ列のように見えていました。あるユーザーからは「ボタンがタブのように見える」と報告されていました。`addToolBar` で配置する `QToolBar` はこれを見た目の調整ではなく構造的に解決します。ボタンは既定で `autoRaise` となり、デスクトップの他のツールバーと同じく、ホバーするまでフラットに描画されます。各コマンドには小さなアイコンがラベルの横に付きました — `ui/assets/icons/` 以下に新規追加した手描き SVG 9 個です。サードパーティのアイコンセットではなくリポジトリ本体と同じ AGPL ライセンスなので、表示義務を別途管理する必要がありません。固定色で一度描画するのではなく有効なテーマに合わせて色を付けるため、Light・Dark・System のいずれでも判読できます。**Open Case** がツールバーに加わりました。以前はケースを開く操作を示す表示がドラッグ＆ドロップ以外になく、`Ctrl+O` は使えても画面上にその手がかりがありませんでした。ツールバーと **Case** メニューは、2 つ別々に作るのではなく 1 つの `QAction` を共有するようになりました。これが **Save All Files**（ボタン側）と **Save Case**（メニュー側）が同じ `save_all_files` コマンドに 2 つの異なる名前を付けていた理由です。アクションを共有することでラベルは 1 つに定まり、今後はどちらも **Save Case** です。エディタの Find 行も同じツールバー化を受けています。

- **Detail ペインが最終行を切り詰めなくなりました** — About / Resources ダイアログと同じ折り返し `QLabel` のバグ（ラベルの `sizeHint` は実際に与えられる幅ではなく Qt が推定した幅で測られる）ですが、こちらのほうが厄介でした。ツリーの選択が変わるたびに再描画され、隣接するスプリッタをドラッグするたびに幅も変わるため、ダイアログ側の一度きりの修正では足りません。今回、再描画のたびと、リサイズのたびに再フィットするようにしました。

- **ファイル一覧が長いファイル名を隠す代わりに省略表示するようになりました** — `constant/transportProperties` のようなパスは、狭いファイル一覧では水平スクロールバーが伸びるだけで、末尾を読むには横方向に一行ずつスクロールする必要がありました。今回、`…` で切り詰めて情報量の多い先頭部分を見えるようにしました。フルネームはもともと各行のツールチップにあり、ヘッダー行も狭いパネルで省略され得るようになったため同じツールチップを持つようにしています。

- **これまで翻訳されていなかった 4 つのパネルが翻訳されました** — BlockMesh 3D パネルには `tr()` の呼び出しが 1 つもなく、言語設定に関わらずすべてのメニュー・ボタン・ツールチップが英語のままでした。比較パネル、ターミナルパネル、エディタのツールバーにも同種の小さな漏れがありました。すべてラップし、日本語も用意しています。新しいテスト（`tests/ui/test_translatable_strings.py`）が、Qt の表示先へ到達する文字列をすべて走査し、`tr()` でラップされていないものをファイル名と行番号付きで失敗させるため、この種の漏れは再発できません。`ui/panels/block_mesh_renderer.py` と `ui/panels/shape_mesh.py` の 2 ファイルだけは意図的に翻訳対象外のままです。VTK 組み込みフォントで直接ラベルを描画しており、持っていないグリフに対しては誤訳ではなく**何も描画されない**ため、この 2 ファイルの日本語ラベルは文字化けではなく完全な消失になってしまいます。

- **メニューの整理** — 3 項目を、実際にその項目が対象とするメニューへ移動しました。**Manage Extra Files & Directories…** と **Reset File List** は開いているケース 1 つに対する操作であってアプリ全体の設定ではないため、**Settings** から **Case** へ。**Reset Window Size** は、その上にあるペイン切り替え項目と同様にウィンドウの見た目のサイズに関する項目なので、**Settings** から **View** へ。**Tools** メニューには区切り線が 2 本増え、**Run Allrun Script**（個々のユーティリティ実行ではなくワークフロー全体）と **Open Mesh in ParaView…**（実行ではなくビューア起動）がそれぞれ独立したグループになりました。そしてすべてのメニュー項目の省略記号(…)に 1 つのルールを徹底しました — **「…」は「何かを尋ねてくる」という意味であり、「確認を求める」という意味ではありません**。**Open Case…**、**Set Default Case Directory…**、**Manage Schema Modules…** には抜けていた省略記号を追加し、ピリオド 3 つ(`...`)で書かれていた 6 項目は本物の省略記号に置き換え、**About Foam Dictionary Editor (FoDE)**・**Keyboard Shortcuts**・**Resources** の 3 項目は省略記号を外しました。いずれも情報を表示するだけで、何かを尋ねてくることはないためです。

### バグ修正

- **インターネットに接続された Linux ビルドがウィンドウを開けませんでした** — `build_linux.sh` が実際にビルドする `foam-dictionary-editor.spec` は、同梱ファイルの一覧に `ui/xterm_terminal.html` を一度も含んでいませんでした。インターネットに接続されたマシンでは xterm.js のダウンロードが成功し、`ui/xterm/xterm.js` の存在チェックも通過したうえで、`_load_terminal()` が*存在しない* HTML テンプレートを読もうとして `FileNotFoundError` を送出します。しかもこれは無防備な状態で `MainWindow.__init__` の中から直接発生するため、ウィンドウが一度も現れません。インターネットに接続されていないマシンではこの行に到達せず、先に「Terminal unavailable」のフォールバック分岐を通ってそのまま正常に起動していました。インターネットに接続していることがかえって悪化要因になっていたため、これまで気づかれずにいたと考えられます。テンプレートを同梱することで修正しました。あわせて、`foam_dict_editor.spec` は `ui/xterm/` を一切同梱しなくなりました。xterm.js は MIT ライセンスで実行時に取得するものであり、「ビルドを行ったマシンにたまたまローカルキャッシュがあったときだけ」同梱するのでは、MIT の著作権表示義務が spec 自体の制御ではなくビルドマシンの状態に左右されてしまうためです。

- **3 つのステータスメッセージが、翻訳済みの日本語文の中に生の英単語を混ぜていました** — 「Parsed」「Saved」「Parsed and tree updated」はいずれも、それ以外は翻訳済みの行の中に `entry`/`entries` をそのまま埋め込んでいたため、日本語モードのユーザーには `3 件の未認識entries` のようにステータスバーへ表示されていました。`tests/test_i18n.py` は `ja.py` のキー重複しか検査していなかったため、日本語キーの補間の中に紛れ込んだ英単語には気づけませんでした。今回、単数形と複数形をそれぞれ完全に日本語のキーとして分離しました。

### 内部変更

リポジトリ全体を対象とした 6 段階のリファクタリングです。アプリケーションの動作は一切変わりません。テストは 2235 件から 2269 件になり、すべて成功、`ruff` と `mypy` も全段階を通じてクリーンです。

- **ドキュメントに書かれていた 3 つの不変条件が、破られたときに失敗するようになりました。** `ui/mixins/_protocol.py` は結合後の `MainWindow` に対する mypy 用のスタンドインなので、ここに宣言のない Mixin メソッドは、それを `self` 経由で呼ぶ *他の* Mixin すべてから型チェックを黙って失わせます — 7 個が欠けていました。`tests/ui/test_main_window_split.py` の Mixin ごとの所有メソッド一覧（どの Mixin がウィンドウのどの領域を持つかを記録するもの）は約 4 分の 1 が古いままでした。さらに `tests/ui/` 配下の 22 ファイルが、pytest-qt がすでに提供しているセッションスコープの `qapp` フィクスチャを隠す独自の `qapp` を定義していました。いずれも修正し、それぞれに AST ベースのガードテストを追加しました。ガードは違反したファイル名と対処方法を示すメッセージで失敗し、いずれも実際に問題を再導入して失敗することを確認しています。所有メソッド一覧をソースから自動生成しないのは意図的です — それではテストが自明になり、一覧が持つドキュメントとしての価値が失われるため、手書きの一覧と実際のメソッド集合が双方向で一致することを検査します。

- **Qt に依存しないロジックを `ui/` の外へ移動しました。** boundary パッチ用のヘルパー 4 つは `QDialog` のモジュール内に置かれたまま、アンダースコア付きの非公開名としてパッケージをまたいで import されていました。これらは rename-boundary のツリー走査とともに `foam/boundary_patch.py` になり、そのテストはダイアログの import を完全に外せるようになりました。`patched_foam_monitor` と `find_backup_files`（スクリプトの書き換えとファイルシステム走査で、どちらも Qt を含みません）は `services/foam_monitor.py` と `services/backup_files.py` になりました。

- **`ui/panels/block_mesh_renderer.py` が 863 行から 478 行になりました。** そのうち約 360 行は Qt の import も `self` も持たない numpy/pyvista のジオメトリで、`ui/panels/shape_mesh.py` へ移りました。`_make_shape_mesh` はモジュールレベルの `make_shape_mesh()` になり、Export-STL ダイアログがパネルのクラス越しに非公開 staticmethod を呼ぶ必要がなくなりました。長いジオメトリ分岐はそのまま移動しています — 型付きジオメトリ union が用意されるまで手を付けない、と開発者ガイドの Update candidates に記録されているためです。同じパネル側では、素の `tuple` を返していた 218 行のツールバー構築メソッド（呼び出し側を mypy が検査できませんでした）が、`GeometryToolbar` データクラスを返す 5 つのセクション構築メソッドになりました。

- **共有先がすでにあった、あるいは無かった重複を解消しました。** `ui/widgets/_checkable_list.py` はチェック可能リストのパターンのために書かれていたのに、6 つのダイアログのうち 2 つしか使っておらず、残る 4 つは各自のコピーを抱えていました。`schemas/` では `(FOUNDATION_SERIES, OPENCFD_SERIES)` が 7 モジュールでローカル定数として書かれ、さらに 86 箇所でインラインに書かれており、`_entry` は 5 つの異なるシグネチャで 5 回定義されていました — すべて `schemas/_base.py` の 1 つの `BOTH` と 1 つの `entry()` になりました。`app_config/keyword_generator.py` の 4 つのソーススキャナは、4 種類の抽出処理の周りにまったく同じ骨格を持っていたので、その骨格を共有するようにしました。`foam/tree_utils.py` に `find_child_by_type` を、`foam/utils.py` に、3 箇所が長々と書き下していたスクリプト判定・ログ判定のための `is_text_only` を追加しました。

- **Detail ペインがスキーマキーを選択ごとに 4 回ではなく 1 回だけ解決するようになりました。** 4 回の呼び出しはいずれも、解決済みのスキーマが手元にあるにもかかわらず、閉じた名前空間の探索をやり直していました。レジストリの公開アクセサはそのままで、解決済みのスキーマ／選択肢を整形する `supported_in_text()` を追加しています。

- **意図的に残した重複もあり**、再び手を付けられそうな箇所にはその理由をコメントとして書きました。`schemas/` の 4 つの Switch 選択肢タプルは互いのコピーではありません。`turbulence_structure` が `on`/`off` を先頭に置くのはチュートリアルが `turbulence on;` と書くからで、`snappyHexMeshDict` は `true`/`false` を主たる組とし残りを別形として説明しています。並び順も文言も Detail ペインでユーザーの目に触れるため、統合は重複の除去ではなく表示テキストの変更になってしまいます。同様に、BlockMesh パネルの 5 つのオーバーレイメニュー構築は 9 フィールド中 8〜9 が異なり、そのうち 2 つはラムダなので、テーブル駆動にすると置き換える前のコードより大きくなります。

- **`main.py` と `i18n/` が型チェック対象になりました。** どちらも元からクリーンで、問題は `tests/test_lint.py` が `pyproject.toml` の設定より広い範囲をカバーしていると書いていたことのほうでした。

- **v1.14.0 で報告した `v2112` の欠落が解消しました。** foamlore が欠けていた `opencfd-v2112` チェックアウトを追加し、再導出しました。同梱の `schemas/_turbulence_coeffs.py` と `schemas/turbulence_properties.py` を再生成しています。実際にタグが変わったのは 1 件だけです — `SpalartAllmaras.sigmaNut` の旧既定値(`scalar(2)/scalar(3)`。v2212 で `0.66666` に置き換えられた)は明示的な組 `"OpenCFD v2106, OpenCFD v2206"` というタグが付いており、これは「v2112 も確認したうえで外れていた」と読めてしまうものでした。v2112 を測定して存在が確認されたことで、これは正真正銘の閉区間 `OPENCFD_V2106_V2206`(「OpenCFD v2106-v2206」)に畳み込まれます — `schemas/_base.py` に追加した新しい定数で、v1.14.0 ですでに追加した `FOUNDATION_V8_V13` / `OPENCFD_V2212_V2606` と同じ系列ですが、区間の最新リリースでもなお現役という形ではなく、区間の途中で置き換えられたエントリ向けです。生成側の仕様書の項目 6 は完了としました。

- **新しい `ui/icons.py` と、新しいガードテスト群。** `icon(name) -> QIcon` は、9 個の手描きツールバー/メニュー SVG を、色文字列をソースへ置換するのではなくグリフ自身の alpha にティントカラーを合成することで、有効なテーマに合わせて色付けします。これにより黒いアイコンがダークモードで見えなくなることは構造的に起こり得ません。`tests/ui/test_icons.py` には、まさにこのダークモード回帰を捕捉する検査が含まれています。`tests/ui/test_translatable_strings.py` と `tests/test_i18n.py` の拡張は、上の「改善」で述べた 2 つの i18n ガードです。`tests/ui/test_action_toolbar.py` と `tests/ui/test_detail_panel_fit.py` は、それぞれ新しいツールバーと Detail ペインの繰り返しフィット動作をカバーします。

## v1.14.0 — 2026-08-10

### 改善

- **文字サイズがデスクトップのフォント設定に従うようになりました** — エディタ、シンプルターミナル、xterm ターミナルはそれぞれサイズを直接指定していました（10 pt、10 pt、13 CSS px）。そのため、デスクトップのフォントがそれより大きい環境では、本文が置かれている 2 つのペインがウィンドウ内でもっとも小さい文字になり、しかも情報量の多い画面に対応しようとフォントサイズを上げても変化しませんでした。さらに 8 か所のラベルが同様にピクセル固定でした。BlockMesh パネルのマウス操作ヒント行と「⚙ Variable-based」バッジ、および About / Resources ダイアログのアプリ名・バージョン・ライセンス・謝辞・免責事項の各行です。今回、これらすべてがアプリケーションフォントからサイズを導出するようにしたので、デスクトップのフォントサイズを上げればウィンドウ全体が一緒に大きくなります。新たに設定する項目はありません。すでにお使いの設定がそのまま効きます。

- **Settings > UI Scale** を追加しました。デスクトップが Qt に正しい情報を伝えていないディスプレイ向けの設定です — 100 %（既定）、125 %、150 %、175 %、200 % から選べ、`app_config.json` に保存されて次回起動時に反映されます。X11 では、Qt はスケール係数を X リソースの `Xft.dpi` だけから決定します。そのため `GDK_SCALE` で拡大しているセッションや、フラクショナルスケーリングされた XWayland セッションでは、隣の GTK アプリケーションが正しく表示されていても Qt アプリケーションはすべて 1 倍で描画されます。高解像度パネルでウィンドウが極端に小さく見える原因は、たいていこれです。`python3 main.py --ui-scale 150` は保存された設定を変えずにその 1 回だけ適用し、50〜400 の任意の値を受け付けます。環境変数に `QT_SCALE_FACTOR` がすでにある場合は、目の前のマシンに合わせて設定されたものとして、保存された設定より優先されます。コマンドラインのフラグは両者より優先されます。`Xft.dpi`、`QT_FONT_DPI`、丸めポリシーといった逃げ道については、USER_GUIDE_ja.md に新設した「文字サイズと表示スケーリング」を参照してください。

- **エディタの文字だけをズームできるようになりました** — `Ctrl` + `+` / `Ctrl` + `-`、デスクトップのサイズに戻す `Ctrl` + `0`、そして `Ctrl` + ホイールです。キーはウィンドウではなくエディタに割り当てているので、ツリーや 3D ビューアで誤って発動することはありません。ズームは固定サイズではなくデスクトップのフォントサイズからの差分として保持され（後でそのフォントを変えても、2 段階拡大は 2 段階拡大のままです）、セッションと一緒に保存されるため再起動後も維持されます。

- **Apply Text to Tree と Reload from Tree を、ツリーとエディタの境目に移動しました** — この 2 つのコマンドは上部アクションバーで Save File や Reload Case の隣にあり、まったく別物であるディスク操作と同じグループに見えるうえ、実際に操作対象となるエディタテキストからウィンドウの高さいっぱいに離れていました。さらに、この 2 つはよく似た文字列で、注意して読まないと区別できません。今回、下段タブバーの右上隅 — 上のツリーと下のエディタの境界 — に移し、方向が一目で分かる矢印を付けました。**▲ Apply Text to Tree** は上向きにツリーへ解析し、**▼ Reload from Tree** は下向きにエディタテキストを生成し直します。タブページ内の行ではなくタブバーのコーナーウィジェットにしたのには 2 つの理由があります。1 つは縦方向のスペースを消費しないこと（タブバーの行を共有し、スプリッタのハンドルにも干渉しません）。もう 1 つは、どちらの段でどのタブを選んでいても表示され続けることです。後者は見た目の問題ではありません。**BlockMesh** タブを前面にしたまま 3D オーバーレイを更新するのは **Apply Text to Tree** であり、タブ選択の裏に隠してしまうと、まさに必要なときに到達できなくなるところでした。

- **ペインをワンクリックで最小化し、同じ操作で戻せるようになりました** — 対象はファイル一覧、ツリー横の Detail ペイン、下段の Editor / Terminal 行です。スプリッタのハンドルをウィンドウ端まで動かせば従来もできましたが、マウス操作が何度も必要で、戻すときにハンドルを探し直すことになります。今回、同等の方法を 3 つ用意しました。**View** メニュー（**File List** `Ctrl+1`、**Detail Pane** `Ctrl+2`、**Editor / Terminal Pane** `Ctrl+3`。チェックが入っている状態が表示中）、下段用の Editor/Terminal タブバー右端の **▁** ボタン、そして最小化できるペインに隣接する**スプリッタハンドルのダブルクリック**です。

  下段だけは他の 2 つと扱いが異なります。ファイル一覧と Detail ペインは幅ゼロまで畳まれますが、Editor / Terminal 行は**タブバー**の高さで止まります。Editor・Terminal タブはクリックでき、**▲ Apply Text to Tree** / **▼ Reload from Tree** もペインと一緒に消えることなくその場に残ります。そもそもこの行をゼロまで畳むことはできません。この splitter は、ハンドルがスナップせず滑らかに動くよう意図的に collapse を無効化しており、その設定下では高さ 0 の要求が 136 px に黙ってクランプされるためです。

  サイドバイサイドモードに入ると Detail ペインは自動的に退避し、抜けると元に戻ります。3D で確認しているファイルに Detail ペインの出番は少なく、その幅はビューに回したほうがよいためです。先に手動で最小化していたペインは、サイドバイサイドを抜けても最小化されたままになります。どのペインが最小化されているか、および各ペインの復帰サイズはセッションと共に保存されます。だからこそ、常に存在し常に現在の状態を示す View メニューの項目が、ペインが画面外に取り残されないことを保証する要になっています。

- **Apply Text to Tree を Case メニューとショートカットからも実行できるようにしました** — これまではボタンだけが*唯一の*起動手段で、メニュー項目もキー割り当てもありませんでした。ツリー編集のワークフローが依存している操作としては手薄です。今回 **Case > Apply Text to Tree** `Ctrl+Shift+A` を追加しました。**Reload from Tree** もメニューには並びますが、ショートカットは意図的に割り当てていません。この操作は現在のツリーをシリアライズした内容でエディタのテキストを上書きし、未適用の編集を破棄します。そして自然に思いつくキーは **Reload Case** と 1 文字違いです。

- **Help > Keyboard Shortcuts が小さいディスプレイでも収まるようになりました** — 一覧は 5 グループ 31 項目まで増え、デスクトップフォント 11 pt では高さ 1123 px を要求するようになっていました。**Settings > UI Scale** を上げればさらに倍加します。単に縦長というだけでなく、小さくできないことが問題でした。中にスクロールする領域がないダイアログは、内容の大きさがそのまま最小サイズになるため、1366×768 のノート PC では下部のグループと **Close** ボタンが画面の外にはみ出したままでした。今回、グループを 2 列に並べて高さをほぼ半減させ（同じ 11 pt で 1123 px → 631 px）、グリッド全体をスクロール領域に収めたうえで、Close ボタンはその外側に固定しました。ウィンドウは一覧が必要とするサイズで開き、画面の 90 % を上限とし、そこから任意の大きさに変更できます。

### バグ修正

- **`Ctrl+S` を Help > Keyboard Shortcuts に掲載し、Case メニューにも追加しました** — 現在のファイルを保存する `Ctrl+S` は以前から動作していましたが、メニュー項目を持たないウィンドウ直付けのショートカットだったため、メニューバーにもショートカット一覧にも現れませんでした。UI 上の手がかりはファイルリストの右クリックメニューだけで、しかも USER_GUIDE_ja.md は **Case > Save File** と説明していました — 存在しない項目です。今回 **Save Case** の隣に実際の項目を追加し、ショートカット一覧にも掲載しました。あわせて、ウィンドウが登録するショートカットをすべて走査し、ダイアログに記載がなければ失敗するテストを追加したので、同じずれは再発しません。

- **ショートカット一覧の 3 か所の誤りを修正し、3D ビューアの項目を拡充しました** — エディタの **Redo** は `Ctrl+Y` だけと記載していましたが、X11 では Qt が `Ctrl+Shift+Z` もやり直しに割り当てており、一覧はこのキーをツリー専用であるかのように示していました。**Find in Tree** は *Editor* に置かれていましたが、実際にはウィンドウに割り当てられていてフォーカスの位置によらず動作するため、*Application* に移しました。3D ビューアの節は `R` と `F` しか挙げていませんでしたが、VTK と pyvista は等角投影ビューの `V`、ワイヤフレーム / サーフェスの `W` / `S`、ズームの `↑` / `↓`、点と線のサイズの `+` / `-` も受け付けます。これらをすべて記載しました。`P` と `E` / `Q` は意図的に除いています。前者はピック操作で本アプリからは何にも接続しておらず、後者は VTK の終了イベントを発生させますが、それを監視している箇所がありません。

- **ショートカットダイアログの日本語表示が完全になりました** — 10 個のラベル（Find、Find Next、Find Previous、Find in Tree、Undo、Redo、Cut、Copy Value、Paste Value、および *Application* の見出し）に日本語がなく、他が翻訳されているウィンドウの中で英語のまま表示されていました。`tests/test_i18n.py` は `ja.py` のキー重複しか検査していないため気づかれませんでした。この表の全行と見出しが翻訳されたままであることを保証するテストを追加しました。

- **About / Resources ダイアログでテキストが途中で切れる問題を修正しました** — About ダイアログでは謝辞の最終行と、免責事項の 2 段落目全体（「OPENFOAM® is a registered trade mark of OpenCFD Limited.」）が表示されず、デスクトップフォントが大きい環境では区切り線が「Built with PySide6…」の文字の上に重なって描画されていました。どちらのダイアログも幅が固定で、折り返し表示の QLabel が並んでいます。折り返しラベルは、実際に与えられる幅ではなく Qt が推定した幅で測った高さを申告します（176 px 必要なラベルが 102 px と申告する）ため、レイアウトは 102 px しか割り当てず、残りは描画されませんでした。今回、最初のレイアウト完了後に、実際の幅で必要な高さを各折り返しラベルに問い合わせ、それに合わせてダイアログを広げるようにしました。以前から存在した不具合で、11 pt という一般的なデスクトップフォントでも発生していました。

- **Foundation のケースで、改名された辞書が表示されるようになりました** — ファイル一覧は固定の名前リストから作られるため、そのリストにない辞書は「補助が効かない」のではなく、そもそも表示されません。リストには改名前の綴りしかなく、Foundation は OpenFOAM 7 以降 `constant/` をかなり改名してきました。v8 で `turbulenceProperties` が `momentumTransport` に、v9 で `fvOptions` が `constant/fvModels` と `system/fvConstraints` に分割され、v10 で `transportProperties` と `thermophysicalProperties` の双方が `physicalProperties` に統合されています。そのため Foundation v10 以降のケースを開くと、transport 系も thermophysical 系の辞書も 1 つも一覧に出ませんでした。さらに、FoDE が同梱している `schemas/momentum_transport.py` というスキーマモジュール 1 つ分が到達不能でした。記述対象のファイルが表示されないためです。今回、これらの改名後の名前をすべて（`system/functions` と `system/surfaceFeaturesDict` も併せて）、通常のファイル一覧・リージョンごとの一覧・`momentumTransport.air` や `physicalProperties.water` を拾う相変数の glob に追加しました。推測ではなく OpenFOAM-7〜dev と OpenCFD v2106〜v2606 のチュートリアルツリーで実測しています。旧称と後継名を対にして検査するテストを追加したので、片方が欠けた場合は沈黙ではなく失敗になります。

  同じ実測で、改名以外の理由で最初から入っていなかった辞書も補いました。名前が無ければ理由を問わず結果は同じだからです。finite area の 3 点（`faSchemes`、`faSolution`、`faMeshDefinition`）、随伴最適化の 2 点（`system/optimisationDict`、`constant/adjointRASProperties`）、cfMesh の `system/meshDict`、燃焼・化学反応の一式（`combustionProperties`、`chemistryProperties`、`reactions`、`thermo` とその相バリアント）、`phaseProperties`、`MRFProperties`、`pRef`/`hRef`、分子動力学の各辞書など、およそ 40 件を追加し、合計は従来の 32 件に対して 105 件になりました。採用基準は「いずれかのツリーで 4 ケース以上に現れる辞書」であり、希望リストではなく実際のチュートリアルの内容です。7 つのツリーで見ると、アプリが提示する `system/` と `constant/` のファイルの割合は 46〜80% から一貫して **91〜93%** に上がります。除外したものは意図的な除外です。`constant/` に置かれているだけの Chemkin データ（`foam.inp`、`foam.dat`）、README、そして `topoSetDict.1`・`blockMeshDict.m4`・`controlDict.orig` のようなサフィックス付きの派生ファイルです。後者は固定リストで列挙できるものではなく、Add files ダイアログがそのためにあります。粒子クラウドの辞書はケースごとに名前が変わる（`limestoneCloud1Properties` など）ため、代表的な綴りのみを収録しています。

- **OpenCFD のリリース一覧に `v2112` を戻しました** — OpenCFD は年 2 回、`yymm` の `mm` が 06 か 12 でリリースしますが、`schemas/_base.py` は `v2106` から `v2206` へ飛んでいました。この欠落は、バージョン表示が範囲ではなく個別リリースを列挙する箇所で目に見えます。OpenCFD が v2206 まで受け付け v2212 で削除した `minMedianAxisAngle` は「OpenCFD v2106, OpenCFD v2206」と表示され、v2112 では使えなかったかのように読めていました。実際にはそうではありません。同じ欠落は同梱の乱流モジュールにもあります。こちらのタグはリリースごとのチェックアウト一式から生成されており、その一式に v2112 が入っていないためです。したがってこの件は FoDE 側で手編集せず、生成側の仕様書に依頼として記録しました。

### ドキュメント

- **上のファイル一覧の作業から、実測を残す価値のあるドキュメントが 2 本生まれました** — [docs/OPENFOAM_VERSIONS_ja.md](docs/OPENFOAM_VERSIONS_ja.md) はユーザー向けで、改名バグが症状として現れていた本来の疑問に答えます。*なぜ別バージョンの OpenFOAM のケースがここでは動かないのか*。実測した 2 つの表を収録しました。Foundation 7〜dev では 8 で `turbulenceProperties` が `momentumTransport` になり、9 で `fvOptions` が分割され、10 で `transportProperties` と `thermophysicalProperties` が `physicalProperties` に統合されます。一方 OpenCFD v2106〜v2606 では、5 年間でこれらのファイル名は 1 つも変わっていません。加えて、任意の辞書のバナーからフォークとリリースを読む方法、フォーク間で単に名前が違うもの、そして FoDE 側の対応も記載しています。[docs/SCHEMA_CANDIDATES_ja.md](docs/SCHEMA_CANDIDATES_ja.md) は次のスキーマモジュールを書く人向けで、モジュールのない辞書すべてを、チュートリアルでの出現頻度とキー数で順位付けし、ここで手書きすべきものと、値が C++ のクラス階層にあるため foamlore のジェネレータから来るべきものに分けています。どちらも「目的別ガイド」表と開発者ガイドのドキュメント一覧からリンクしており、開発者ガイドの「更新候補」には、この作業が解決したのではなく開いた項目を 4 つ追加しました。

## v1.13.0 — 2026-08-07

### 新機能

- **詳細ペインに、既定値だけでなく OpenFOAM 自身がその係数について書いている内容を表示するようになりました** — 値はソルバが何をするかを教えてくれますが、その係数が何を意味するのか、いつ変更すべきかまでは教えてくれません。OpenFOAM のヘッダにはそれが書かれており、foamlore がその散文を抽出して生成スキーマに取り込むようになりました。引用元のファイルと行番号を伴う逐語引用です。`<model>Coeffs` 辞書を選ぶと、そのモデルのヘッダにある説明が表示され（29 モデルすべてが持っています）、定義論文が Note 行に出ます。`kEpsilonCoeffs` なら Launder & Spalding、`GEKOCoeffs` なら Menter & Matyushenko (2025) です。上流が宣言箇所に記述している係数では、その記述が先頭に来ます。`F3` は機械的な既定値の文の前に "Flag to include the F3 term" と表示され、257 個の係数がこの記述を持っています。

  きっかけになった事例は、これらより稀ですが、より有用です。GEKO の `CFbLam` はソース中で `1.0  // increase to 25 in case of transition simulations (MM:p. 4)` と書かれています。このコメントは係数を**いつ**変えるべきかを述べており、`MM` はそのモデル自身の参考文献ブロックへのタグです。この注記がキーに付き、引用タグは上位の辞書に表示される参考文献で解決できます。長い引用はペイン用に切り詰め、切り詰めた旨を明示します。全文は foamlore の `get_fact` が引用元付きで返します。

- **モデルセレクタが、各モデルの説明を OpenFOAM 自身の言葉で表示するようになりました** — 通常の `constant/turbulenceProperties` は 5 行程度で、係数辞書はどこにも現れません。`simulationType RAS;` と `RAS { RASModel kEpsilon; turbulence on; printCoeffs on; }` だけです。上の項目で述べた内容はすべて `kEpsilonCoeffs` にぶら下がっていました。これは既定値を上書きするケースにしか存在しないキーであり、大半のケースには存在しません。つまり最も一般的なケースでは、そのどれにも到達できませんでした。`RASModel`（`model`、および LES 側の綴りも同様）を選ぶと、選んだ値について、そのモデルのヘッダにある説明と定義論文が表示されるようになりました。`kEpsilon` なら "Standard k-epsilon turbulence model for incompressible and compressible flows including rapid distortion theory (RDT) based compression term" と表示され、Launder & Spalding (1972) を引用します。2 つのセレクタにある 27 個のモデル選択肢すべてが対象です。`laminar` はモデルクラスではないため、従来の短い手書き説明のままです。選択肢のリスト自体と、どのフォークがどのモデルを出荷しているかは変更していません。これらはモデルではなく辞書についての事実であり、引き続き手作業で保守します。

- **乱流モデル係数のヘルプが 3 モデルから 29 モデルに拡大しました** — 姉妹プロジェクト foamlore が生成するスキーマモジュールは `kOmegaSST`・`kEpsilon`・`SpalartAllmaras` だけを記述していました。同プロジェクトがモデルごとに手書きの抽出スクリプトを持つ設計で、モデルを 1 つ増やすたびにスクリプトが 1 つ必要だったためです。これがレジストリ方式(モデル 1 つ = 表の 1 行)に置き換わり、カバレッジは **RAS 16 モデル・LES/DES 13 モデル** — 両フォークが出荷する全モデル — になりました。17 リリース全体で実測しています。対象は `kOmega`・`kOmega2006`・`realizableKE`・`RNGkEpsilon`・`LaunderSharmaKE`・`LRR`・`SSG`・`EBRSM`・`GEKO`・`kEpsilonPhitF`・`v2f`・`kOmegaSSTLM`・`kOmegaSSTSAS`、`Smagorinsky`/`WALE`/`sigma`/`kEqn`/`dynamicKEqn`/`dynamicLagrangian`/`DeardorffDiffStress` の LES 一式、および `SpalartAllmaras`/`kOmegaSST` の DES・DDES・IDDES 系列です。これらの係数を選ぶと、詳細ペインにソース既定値・存在するリリース・読み取り元のコンストラクタが表示されます。

  この規模から 2 つの帰結があります。モデルが自分では宣言せず継承している係数も、宣言元の基底クラスまで辿るようになりました。`kOmegaSSTSAS.C` に記述がなくても `kOmegaSSTSASCoeffs { betaStar 0.09; }` は実在するエントリです。また 134 個の係数名のうち 28 個が複数のモデルに読まれます(`Cmu` は 9 モデル)。従来はどの名前も所有モデルがちょうど 1 つでした。OpenFOAM の `optionalSubDict` が許すとおり `RAS { … }` に直接書いた場合、これらの名前は「読んでいる全モデルを列挙し、モデルごとの既定値を個別に提示する」単一のエントリとして解決します。`<model>Coeffs` 辞書の中に書いた場合は、従来どおりそのモデル自身のエントリが優先されます。

### バグ修正

- **括弧を含むスキーマ値 1 つで数百のキーワードのシンタックスハイライトが止まる問題を修正しました** — 値キーワードのハイライタは、スキーマのキーと選択肢の値を 1000 個ずつ `|` で連結してパターンを作りますが、エスケープをしていませんでした。これまでキーワードはすべて素の識別子だったため表面化しませんでしたが、正規表現のメタ文字を含む最初の選択肢値 — `scalar(2)/scalar(3)`、OpenCFD が v2212 まで実際に `sigmaNut` の既定値をそう書いていたもの — によってそのチャンクのパターン全体が不正になりました。Qt は不正なパターンを黙ってマッチさせないため、症状は「`application` や `PCG` など同じチャンクに含まれるキーワードの色が消える」という形で現れます。各要素をエスケープするようにし、さらに単語ではなく式である選択肢値はキーワードとして収集しないようにしました（`(?<![\w.])` の先読みでは元々マッチし得ないためです）。

- **乱流モジュールのバージョン表示も実測に基づくものになりました** — v1.12.0 では、コアのスキーマと `snappyHexMeshDict` に残っていた一律の Foundation v13 タグを訂正しましたが、同じ過小表示が foamlore 生成の 2 つの乱流モジュールには残っている、と記載していました。Foundation v10 の利用者が `constant/momentumTransport` で `beta1` を選ぶと「Foundation v13」と表示され、OpenCFD v2106 の利用者には「OpenCFD v2512, v2606」と表示される状態です。これらは別リポジトリから取り込んだ生成ファイルであるため、手で編集せず生成側の仕様書に訂正を記録していましたが、その訂正が生成側で実施されました。foamlore はソースの対象範囲を 4 チェックアウトから、いずれかのフォークがバージョン定数を持つ 17 リリースすべてへ広げ、全リリースから係数を再抽出して再生成しました。その結果、`momentum_transport.py` の 111 エントリは `Foundation v8-v13`（このファイル名を読むすべてのリリース）に、`turbulence_properties.py` の共通 109 エントリは `OpenCFD v2106-v2606` になりました。`turbulence_properties.py` の Foundation タグは v7 単独のままです — これは元から正しく、v8 でファイル名が変わったためです。単一リリースでもフォーク全体でもない範囲を表す 2 つのラベル `FOUNDATION_V8_V13` と `OPENCFD_V2212_V2606` を `schemas/_base.py` に追加しました。

  サンプリングではなく全リリースを実測したことで、本リポジトリ側の監査の誤りも 2 点訂正されました。いずれもサンプルから漏れていたリリースに由来します。SpalartAllmaras の `ft2` 項 — `ft2`・`Ct3`・`Ct4`・`ck` — が登場したのは v2306 ではなく **v2212** でした。また `sigmaNut` のソース既定値が v2212 で `scalar(2)/scalar(3)` からリテラル `0.66666` に変わっていました。小さいながら実際の数値の違いであり、以前ディスク上にあった 4 チェックアウトからは見えませんでした（v2512 も v2606 も新しい表記のため）。両方の既定値が、それぞれのバージョンタグ付きの選択肢として提示されるようになりました。

### ドキュメント

- **8 本目のデモ動画: 名前だけでなく、そのモデルが何であるか**（`pitzDaily-turbulence-notes`、約 64 秒） — 上の 2 項目は、このリリースで最も静止画にしにくい内容です。「注記がある」ことではなく「値ごとに*違う*注記がある」ことが主張だからです。そこで動画にしました。同梱の pitzDaily で `constant/turbulenceProperties` を開き、`RASModel` の行を選び、Choices のボックスをリストの下へたどっていくと、その下の Choice Help・Choice Supported In・Choice Note が次々に書き換わります。kEpsilon には Launder と Spalding、kOmegaSST はヘッダが論文を挙げていないので注記は空欄、SpalartAllmaras は 3 本。そこから 11 回下って `laminar` — リスト中で唯一 FoDE 自身が書いている行に着きます。最後のフレームが要点です: ツリーはいまも `kEpsilon` で、ファイルもダーティになっていません。ペインはコンボボックスからヘルプを更新するだけで、ノードを書き換えるのは **Apply Value** のときだけだからです。他の 7 本と同じく `tools/demo_specs.json` に台本があり `tools/demo_driver.py` が再生・収録するので、撮り直しはコマンド 1 つです。ショットごとの台本は `docs/DEMO_SCRIPTS_ja.md` にあります。

- **実際に乱流である同梱ケースを追加しました**（`tutorials/pitzDaily/`） — `tutorials/` のケースはすべて層流でした。`damBreak` と `snappyMultiRegionHeater` の両リージョンは `simulationType laminar` であり、残りのケースには乱流の辞書自体がありません。そのため 2 リリースにわたって拡充してきた乱流モデルのヘルプは、同梱ケースでは試せず、解説を読む人も実物を見られませんでした。`pitzDaily` は RAS の定番チュートリアル（`simpleFoam`・`kEpsilon`・1 秒でメッシュが切れる後ろ向きステップ）で、OpenFOAM v2512 のチュートリアルセットから無改変で再配布します。ライセンスは `tutorials/` の他のケースと同じ GPL-3.0 です。このケースの `constant/turbulenceProperties` を開けば、モデルセレクタ・各選択肢の説明・係数表のすべてがその場にあります。加えて `controlDict` の `#includeFunc` から辿る `system/streamlines` は、インクルード機能の実例にもなっています。

## v1.12.0 — 2026-08-05

### 新機能

- **詳細ペインに乱流モデル係数のヘルプを表示** — 姉妹プロジェクト foamlore が OpenFOAM ソースコードから生成した 2 つのスキーマモジュールが追加され、既定で有効になりました。対象は `constant/turbulenceProperties`（OpenCFD v2512/v2606、Foundation v7）と `constant/momentumTransport`（OpenFOAM 8 以降の Foundation でのファイル名、Foundation v13）です。`kOmegaSST`、`kEpsilon`、`SpalartAllmaras` の係数をクリックすると — `<model>Coeffs` サブ辞書内でも `RAS` 辞書直下でも — どのフォーク・バージョンで定義されるかと、各バージョンの `.C` コンストラクタから機械的に抽出されたソース既定値が適用可能な選択肢として表示されます（例: `beta1` = 0.075、`Cmu` = 0.09。`decayControl` のような OpenCFD 限定の係数はその旨を明示）。スキーマモジュール一覧をカスタマイズ済みの既存環境では、**Settings > Manage Schema Modules > Add Module from File** で `schemas/turbulence_properties.py` と `schemas/momentum_transport.py` を追加できます。

- **スキーマヘルプが対象辞書を実際にカバーするようになりました** — 組み込みの 6 スキーマは内容が薄く、うち 1 つは到達不能でした。OpenFOAM v2606 のチュートリアルに含まれる該当辞書すべてを対象に測定したところ、ヘルプが表示されるエントリの割合は `fvSchemes` が 0% → 99%、`fvSolution` が 28% → 97%、`controlDict` が 40% → 97%、`blockMeshDict` が 5% → 85%、`snappyHexMeshDict` が 58% → 98%、`turbulenceProperties` が 3% → 96% になりました。最初にクリックされるキー（`divSchemes`、`solvers`、`PIMPLE`、`vertices`、`blocks`、`castellatedMeshControls`）にはこれまで項目自体がありませんでしたが、追加されています。値のドロップダウンは OpenFOAM が実際に登録している名前を列挙するようになり、`tolerance`、`relTol`、`endTime`、`deltaT`、そして `divSchemes` カテゴリ全体が初めて説明されるようになりました。

- **ケース依存の名前を持つエントリを認識** — 子の名前がケースのフィールド名やパッチ名に由来する辞書について、スキーマがワイルドカードを宣言できるようになりました。これにより `div(phi,U)`、`solvers { p { … } }`、`relaxationFactors { equations { U 0.7; } }` も、あらかじめすべての名前を列挙することなくヘルプが表示されます。

- **乱流モデルの係数だけでなく構造もカバー** — `simulationType`、`RAS`/`LES`/`laminar` 辞書、フォークごとの全モデル一覧を備えた `model` セレクタ、LES の delta モデルを扱う新しいスキーマを追加しました。スキーマモジュールが複数の対象ファイルを指定でき、また複数のモジュールが同じ辞書に寄与できるようになったため、`constant/turbulenceProperties` と `constant/momentumTransport` の双方に等しく適用されます。

- **キーが旧称なのか、無効なのかを詳細ペインが明示** — OpenFOAM が改名したキーは、現行キーであるかのように見せるのではなく、そのまま残したうえで後継キー名と改名バージョンを併記します。公式チュートリアルに現れるものの、どの OpenFOAM リーダも読まないキーは、実際に読まれるキー名とともに警告色で示されます。最初の該当例は `minMedianAxisAngle`（v1712 に `minMedialAxisAngle` へ改名。Foundation では現在も受け付けられますが OpenCFD は v2212 で削除）と `minFlatness`（motorBike チュートリアルが OpenFOAM-2.3.x 以来、両フォークで出荷し続けている一方、OpenFOAM は `minFaceFlatness` を読むためこのキーは黙って無視されます）です。

### バグ修正

- **`fvSchemes` のスキーマヘルプがそもそも一度も表示されていませんでした** — このモジュールはキーをルックアップとは逆向きに綴っていたため、全エントリが到達不能でした。`ddtSchemes/default` を選んでも説明もスキーム一覧も出ない状態が、モジュール作成時からすべてのケースで続いていました。ユニットテストが通っていたのは、詳細ペインが実際に行う呼び出しではなく内部テーブルの形を検証していたためです。キーを修正するとともに、実際のチュートリアル辞書をパースして詳細ペインと同じ方法で走査するテストを追加したので、モジュールが再び機能停止すればビルドが失敗します。

- **新しい組み込みスキーマが既存環境にも届くようになりました** — 保存済みのスキーマモジュール一覧が唯一の正だったため、**Settings > Manage Schema Modules** を一度でも開いたことのあるユーザーは当時のモジュールに固定され、以後追加されたモジュールを受け取れませんでした。読み込みのたびに組み込みデフォルトが保存済み一覧にマージされるようになり、明示的に削除したモジュールだけが除外されます。本リリース以前に削除していた場合はその意図の記録がないため、一度だけ再表示されます（再度削除できます）。

- **`blockMeshDict` の `mergeType` が無効な値を提示しなくなりました** — ドロップダウンには OpenFOAM が受け付けない `merge` が並び、実際の既定値である `topology` が欠けていました。さらに 2 つの説明文が入れ替わっていました。一覧から選ぶと blockMesh が意図どおりに読めない辞書ができてしまう状態でした。

- **共有キーを Foundation 限定と表示しなくなりました** — `fvSchemes` の全項目と `fvSolution` の大半を含む 61 エントリが Foundation v13 限定とタグ付けされていました。実際には両フォークが共有する `finiteVolume`／`lduMatrix` ライブラリに属するキーであり、OpenCFD ユーザーに対して中核キーが使えないかのように伝えていました。

- **サブ辞書のスキーマヘルプが別の辞書のキーを流用しなくなりました** — 詳細ペインはキーを解決する際、まず外側の辞書名で修飾された項目を探し、見つからなければ同名のプレーンな項目を探します。この 2 段目には「いま自分がどこにいるか」という感覚がなく、あるサブ辞書の中に書かれたキーが、別のサブ辞書に属する項目で解決されることがありました。しかもその説明は断定的で、そこには当てはまらない既定値まで選択肢として提示されていました。ドット区切りの接頭辞は閉じた名前空間として扱われるようになりました。スキーマモジュールがある名前の下にキーを 1 つでも修飾した時点で、そこに修飾されていないキーはその辞書のキーではなく、フラットフォールバックは推測せずに黙ります。どの名前空間にも属さないキーはこれまでどおりどこからでもフォールバックします。これにより、OpenFOAM が `optionalSubDict` 経由で読む辞書のフラットな書き方は従来どおり機能し、`snappyHexMeshDict` のルックアップも一切変わりません（9 つのサブ辞書のいずれもフラットな双子を持つキーがないため、そもそもこのルールは発火しません）。得られたのは「ペインの表示が減ること」ではなく、「あるものの意味を別のものに帰属させなくなること」です。後者はペインを見ているだけでは読み手には見抜けません。

- **`constant/regionProperties` がエントリとして正しく開くようになりました** — マルチリージョンケースのリージョンを列挙しているこのファイルは、そのケースについて唯一ツリーで読めないファイルでした。`regions ( fluid (bottomAir topAir) solid (heater leftSolid rightSolid) );` は、名前のない琥珀色の行 2 つ（一方は `regions` という単語だけ、もう一方は括弧の中身全体）になっていました。`regions` というキーが、`setFieldsDict` に必要な名前付き辞書の解析（`regions ( boxToCell { … } );`）へ無条件に振り分けられており、その形でないものはすべて失敗として扱われていたためです。このキーは `sets` や `surfaces` と同じ先読みで判定するようになりました。名前付き辞書はこれまでどおり `region_block`/`region_entry` として解析されるので setFields のオーバーレイに変更はなく、それ以外は通常のリスト処理に落ちます。これは同じ形のエントリが他のキーの下にあったときに元から得られていた結果と同じです。v2512 のチュートリアル全体では、`regions` を含むファイルのうち解析エラーを報告するものが 45 個から 24 個に減り、`region_block` として解析される 135 個には変化がなく、バイト単位のラウンドトリップにも一切差はありません。

- **解析に失敗していたチュートリアル辞書の大半が開くようになりました** — 値の読み取りにあった 3 つの欠陥が、v2606 チュートリアルの辞書ファイル 4435 個における解析失敗のほとんどを占めていました。エラーを報告するファイルは 288 個・エラー 861 件から、38 個・217 件になりました。1 つ目は、エントリの値の中に書かれた辞書です。`fvSchemes` は `div(phi,Yi_h) Gauss multivariateSelection { O2 limitedLinear01 1; … };` のように書きますが、読み取り側は波括弧の深さを追跡していたにもかかわらず、波括弧の内側の `;` に対する分岐がありませんでした。そのためエントリは名前のない琥珀色の行に退化し、ブロック自身のエントリは本来それを含むはずの辞書の兄弟として取り残され、閉じる `};` が `divSchemes` を早期に終わらせて、以降の `div(...)` はすべてトップレベルのノードとして解析されていました。ツリーはファイルに存在しない構造を表示していたことになります。2 つ目は、値をまったく持たないエントリです。`fluxRequired { p; pcorr; }` や `cache { grad(U); }` のように、キーが意味のすべてを担う書き方で、これらは内容そのものであるフィールド名を失った名前なしの行になり、スキーマヘルプも参照すべき名前を持てませんでした。これらは `valueless` ノードタイプになり、キーは編集可能なまま、Value 列は編集できません（編集すべきものが存在しないためです）。3 つ目は、キーと開き波括弧のあいだにあるコメントです。`thermophysicalProperties` は `{` の 1 行上に `mixture // air at room temperature (293 K)` と書きます。このコメントは保持され、元のファイルと同じくキーの行に書き戻されます。ラウンドトリップの忠実性は終始変わらず、4435 ファイルすべてがバイト単位で同一に書き戻されます。失敗したエントリはもとより逐語的に保存されていたためで、これらの修正が変えるのは画面で見えてクリックできるものであって、ディスク上の内容ではありません。残る 217 件のエラーはロングテールではなく形のはっきりした 4 つの原因で、それぞれ、これまで未着手にしてきたトレードオフとともに `DEVELOPER.md` に記録してあります。

- **`snappyHexMeshDict` のバージョン表示を実測に基づくものにしました** — 上記のコアの 61 エントリの訂正のあとも、snappy のパッケージには Foundation v13 という一律のタグが残っていました。そのため `snap` を選ぶと、少なくとも v7 から存在するキーについて "Foundation v13, OpenCFD v2106-v2606" と表示され、「あなたの環境では使えない」と読めてしまっていました。推測を別の推測に置き換えるのではなく、このパッケージが記述する 107 個のキー名すべてをソース自体（Foundation 7 から dev、OpenCFD v2106 から v2606）に照らして確認しました。うち 97 個はすでに Foundation v7 に存在しており、タグはそのとおりに書き換えられています。この調査では、本当にフォーク固有のキーも見つかり、実測どおりに記録されました。`minEdgeLength`（OpenCFD v2306 以降のみ、Foundation にはどのリリースにも存在しない）、`thicknessModel` と `nSmoothInternal`（OpenCFD のみ）、`radius1`/`radius2`（Foundation では v12 から）、`minMedianAxisAngle`（Foundation は v7〜v13、OpenCFD は v2106/v2206 のみ）です。`appendRegionName` はどちらのフォークにもどのチュートリアルにも根拠がなく、削除しました。`yes`/`no` は `Switch` が両フォークで受け付けるにもかかわらず Foundation 限定とタグ付けされており、`on`/`off` は欠けていました。同じ過小表示は foamlore が生成した 2 つの乱流モジュールにも残っており、Foundation v10 の利用者が `beta1` を選ぶと今も「Foundation v13」と表示されます。ただしこれらは別リポジトリから取り込んだ生成ファイルであるため、手で編集するのではなく生成側の仕様書に訂正を記録してあります。

### ドキュメント

- **7 本目のデモ動画: 論文の実例を端から端まで**（`cavity-full-workflow`）— 上記の 6 本はそれぞれ 75 秒以内に 1 つの問いに答えます。この動画はその 6 本が答えていない問い、すなわち「ケース 1 本は端から端までどう進むのか」に答えるもので、そのために 3 分半を使います。内容は SoftwareX 論文の Appendix A（`icoFoam` で解くリッドドリブンキャビティ）をショットごとにそのまま収録したものです。ケースを何も開いていないウィンドウから始まり、ケースライブラリから `cavity` を複製し、ファイル一覧と、`PDRblockMeshDict` を出す `[+]` マーカーをたどり、`controlDict` をツリーで読みながら `writeControl` に対するスキーマ駆動の詳細パネルを開き、境界条件を Boundary タブに集約してセルから `0/U` の該当行まで戻り、埋め込みターミナルで `blockMesh` と `icoFoam` を実行してファイル一覧にメッシュの行と 5 つの時刻ディレクトリが生えるのを見せ、両方のログを **View Log Summary…** で要約し、最後に ParaView で 0.5 秒の速度場 — 付録の Fig. A.6 そのもの — を、説明ではなく画面として映して終わります。付録を読み終えた人が、想像で補う代わりに実際に見られるということです。この収録のために、ドライバに 3 つのものを足しました。収録ごとのスクラッチ `app_config.json`（ケースの複製が収録機の設定を書き換えなくなり、動画が開くときの機能フラグとケースライブラリがどの機械でも同じになります）、`case_library` シーンキー（ケース選択が、収録シェルに登録されていたものではなく、常に同じ配置のチュートリアルツリーで開きます）、そしてファイル一覧のグループヘッダ・ラベルで指定するフォーム項目・2 つのヘッダで指定する境界条件テーブルのセルを指すターゲットです。FoDE 以外のアプリケーションを操作する初めてのシーンでもあり、ParaView の分だけは名前ではなくピクセル座標で支払っています。

- **6 本のデモ動画と、その撮影台本**（`docs/DEMO_SCRIPTS.md` / `docs/DEMO_SCRIPTS_ja.md`）— YouTube で公開した短い紹介動画で、`README.md` からリンクしています。内容は、`damBreak` での「編集し、見て、実行する」ループ、`topoSetShapes` での `topoSetDict` オーバーレイ、`cavity` での境界条件テーブルとケース比較、`motorBike` での `snappyHexMeshDict`、`samplingShapes` での 3 ファイルにまたがるサンプリングの統合、そして `snappyMultiRegionHeater` でのリージョンごとのファイル分類です。このドキュメントはそれぞれのショットごとの台本で、各ショットで画面に何が映っているか、それに合わせたナレーション、そしてなぜその画角にしたのかを記しています。6 本中 5 本はこのリポジトリに同梱されたケースを使っているので、チェックアウトさえあれば動画と同じものを手元で再現できます。この台本に価値があるのは、動画*について*書いた文章ではなく、動画そのものだからです。各動画は `tools/demo_specs.json` のシーンであり、`tools/demo_driver.py` がそれを再生して収録します。出発点はスクリーンショットギャラリーと同じ `ui/window_state.py` のウィンドウ状態で、そこから実際のマウス・キーボード入力でウィンドウを操作します。撮り直しはコマンド 1 つで済むため、手作業で撮った動画のように古くなっていくことがありません。さらに `tests/tools/test_demo_specs.py` がシーンを UI と突き合わせるので、メニュー項目の改名は収録の途中ではなくテストスイートで失敗します。動画そのものは意図的にバージョン管理していません。spec から再生成できますし、動画は差分圧縮が効かないため、撮り直すたびに履歴へ永久に残ってしまうからです。隣にある `.srt` 字幕ファイルは管理対象です。数 KB のテキストであり、各動画と一緒にアップロードする字幕トラックそのものだからです。

## v1.11.0 — 2026-07-31

### 新機能

- **FoDE が前回の続きから起動するようになりました** — アプリケーションを閉じるときにウィンドウの形と中身を記録し、次回起動時にそれを復元します。ウィンドウのサイズと位置、スプリッタ位置、上部と下部のアクティブタブ、Terminal のモード、ケースディレクトリと開いていたすべてのファイル、画面に表示していたファイル、選択していたツリー行、そして BlockMesh パネルのトグル・ラベルサイズ・カメラアングルが対象です。特に 3-D ビューはデフォルト視点ではなく終了時の角度のまま復元されます。従来は同じケースを開くたびに手で向きを合わせ直す必要がありました。一方、ウィンドウ自身が導き出せるもの — スクロール位置、エディタのカーソル位置と折りたたみ状態、選択行の祖先を超えたツリーの展開状態 — は復元されません。未保存の編集内容も対象外で、これまでどおり終了時のダイアログで処理されます。保存されたセッションが原因でアプリケーションが起動できなくなることはありません。名前が変わったケース、削除されたファイル、存在しなくなったツリー行、より新しいバージョンの FoDE が書き込んだレイアウトは、部分ごとにスキップされて残りが復元され、除外された内容がステータスバーに表示されます。`log.*` の実行ログのような辞書ではない大きなファイルは、意図的に除外対象としています。この種のファイルは読み込み中にウィンドウが応答しなくなるため確認ダイアログが表示されますが、それを起動時に尋ねるのも、ユーザーに代わって答えるのも適切ではないからです。**Settings > Restore Last Session on Startup** はデフォルトでオンです。チェックを外すと次回はデフォルトのウィンドウで起動しますが、保存済みの内容は*残る*ため、チェックを付け直せば同じレイアウトに戻ります。破棄するのは **Settings > Forget Saved Session** で、すべてのバリアントの保存済みレイアウトを破棄し、破棄するものがないときはグレー表示になります。あることを表示しているチェックボックスが黙って別のことをするべきではなく、レイアウトが記録しているケースのパスは意図して消せるようにしておく価値があるからです。`python3 main.py --no-restore` は設定も保存済みセッションも変更せずに、その 1 回だけ復元をスキップします。バリアントによって存在するパネルとタブが変わるため、`--variant` ごとに個別のレイアウトを保持します。

- **コマンドラインの `--theme`** — `python3 main.py --theme dark` はその実行に限って指定した配色テーマで起動し、保存済みの **Settings > Appearance** の設定は変更しません。テーマ設定は従来から反映に再起動が必要で、2 つを見比べるには設定を変えて再起動し、また戻すという手順が必要でした。これからは異なるテーマのウィンドウを並べて置くことができ、そのどちらも次回起動時の見た目を左右しません。Appearance メニューと同じ `system`、`light`、`dark` の 3 つを指定できます。フラグを付けなければ動作は従来どおり、保存済みの設定がそのまま使われます。

- **同梱の例題ケースに `tutorials/samplingShapes` を追加しました** — `topoSetShapes` が `topoSetDict` に対して果たしてきた役割の、サンプリング版です。サンプリングは 4 つのオーバーレイの中で動作を確認しにくいものでした。専用の辞書ファイルを持たず、`controlDict` 内の function object としてもスタンドアロンの辞書としても書け、後者にはメンバーリストの記法が 2 通りあるためです。OpenFOAM のチュートリアルにはこれを網羅するケースがありません。v2512 と v2606 のチュートリアル全体を通しても、サンプリング平面を定義しているケースはわずか 2 件で、そのいずれもプローブやサンプル線とは組み合わせていません。このケースはそのすべてを 1 つの 3×3×3 ブロックに収めています: プローブ点、始点・終点による線、点群、そして平面の 2 通りの記法を、`controlDict` の `functions {}` ブロックと、異なる 2 つの記法で書いた 2 つのスタンドアロン辞書に分散配置しました。さらに、ジオメトリを持たない `patch` サーフェスと、サンプリングではない function object も含めてあり、「ジオメトリなしとして一覧に出る」ものと「完全に無視される」ものの違いも確認できます。各シェイプはあえて整列させず離して配置しています。名前バッジはシェイプ自身の中心に描画されるため、すべてを中央に置いたケースではバッジが重なって判読できなくなるからです。

### バグ修正

- **3D ビューアのクリップ表示マークが再び見えるようになりました** — ブロックメッシュより大きいオーバーレイ形状はメッシュに合わせて切り取って描画され、切り取られた範囲を形状本来の範囲と誤解しないように、名前バッジにマークが付きます。そのマークは `✂ clipped` でしたが、はさみの文字は画面上に何も描画されていませんでした: これらのラベルは Qt ではなく VTK が描画しており、VTK の組み込みラベルフォントにはこの文字のグリフがないため、代替の四角い枠さえ描かず、それでいてバッジは幅だけを確保していました。結果として `midPlane   clipped` という、理由の分からない空白を含んだ表示になっていました。マークは `(clipped)` と `(outside block mesh)` になり、オーバーレイメニュー自身の `(no geometry)` 行と同じく、ASCII のみで括弧を使う形に揃えました。なお角括弧も使えません — VTK のフォントは角括弧を丸括弧として描画します — この点は 3D ラベルに記号を入れる前に知っておく価値があります。

- **3D ビューアの X/Y/Z 範囲表示で、2 つの数値の区切りが見えるようになりました** — 同じグリフ欠落の問題が、もう 1 か所にありました。**Dimensions** オーバーレイは各軸を `X  -5 → 15  (20 m)` と書き出していましたが、矢印は何も描画されないため、表示は `X  -5   15  (20 m)` になっていました。理由の分からない空白で隔てられた 2 つの数値であり、それが範囲の両端なのか、それとも値と個数のようなものなのかも分かりませんでした。現在は `X  -5 .. 15  (20 m)` と表示されます。ハイフンは一見自然な選択ですが不適切です。負の数から始まる範囲では `-5 - 15` と読めてしまうためです。

- **3D ビューアの方位軸がダークテーマで判読できるようになりました** — BlockMesh ビューアの左下隅にある小さな X/Y/Z の軸表示は、3D シーンの中で唯一、配色テーマに追従していない部分でした。そのラベルは 3 つのモードすべてで PyVista の既定である黒のままだったため、ダークテーマのビューア背景 `#2E3238` の上ではほとんど見えませんでした。すでにテーマ対応済みだった、その上に表示される範囲情報と同じ色を使うようになりました。軸の矢印は両テーマとも従来どおりの赤/緑/青のままです — これらは装飾ではなく軸を識別するための色であり、パッチやオーバーレイの色と同じ原則に従います。

- **3D グリッドの目盛りラベルが、下敷きのグリッド線と同じ薄さで描画されなくなりました** — VTK の `show_grid` は線とテキストを 1 つの値でまとめて描画するため、グリッドの数値と軸タイトルはグリッド線に指定した色をそのまま使っていました。この色は意図的に薄くしてあり、グリッド線には適切でも、読ませるための数値には不適切です: ライトテーマの白いビューア上ではコントラスト比 3.95:1 となり、本文テキストに必要な 4.5:1 を下回っていました。両者は別々の設定になり、線はこれまでとまったく同じ薄さを保ったまま、テキストはライトテーマで 7.5:1、ダークテーマで 7.7:1 になりました。ダークモードは 4.58:1 で元々問題なかったため、これはダークテーマ対応の作業によってたまたま表面化したライトテーマの問題です。見逃されていた原因は配色テーブル自身の判読性テストにあります: すべてのビューア色を 3.0:1 で検査していましたが、これは線には正しく、テキストには不適切な基準でした。現在はテキストを 4.5:1 で、グリッド線を 3.0:1 で検査します。

- **ブロックメッシュに合わせて切り取られたオーバーレイ形状が中空にならなくなりました** — 形状メッシュは中身のない曲面であり、切り取った断面は開いたままだったため、両端のふたがクリップボックスの外側にある形状は、中身のない筒として描画されていました。これは例外的な場合ではなく通常の場合です: `setFieldsDict` のリージョンは準 2 次元ケースの奥行き全体をまたぐように書くのが慣例で（damBreak では奥行き 0.0146 m の水槽に対して `box (0 0 -1) (0.1461 0.292 1)`）、そうしたケースを見る角度は正面以外にありません。そのため初期の水柱は、塗りつぶされた領域ではなく、メッシュの上に描かれた中身のないオレンジ色の輪郭として表示されていました。現在は切断面がふさがれ、クリップされた形状も、クリップされていない形状と同じように中身のある立体として表示されます。最も恩恵が大きいのは円柱です: 継ぎ目に重複する点があるために閉曲面の判定そのものに失敗しており、先にその点をマージすることで初めてふたを付けられるようになりました。ふさぐべき体積を持たない形状 — `planeToFaceZone` の平面やサンプリングの円板 — の挙動は従来のままです。クリップの意味自体は何も変わっていません: 辞書ファイルと抽出されたジオメトリはこれまでどおり変更されず、**Export Shapes as STL…** は引き続きクリップ前の完全な形状を書き出します。

- **Reset All Settings がアプリケーション設定を実際にリセットするようになりました** — ダイアログは `app_config.json` を削除し、そのとおりに表示していましたが、アプリケーションはそのまま動作を続け、ウィンドウを閉じた時点でファイルが書き戻されていました。終了処理がセッションのレイアウトとウィンドウサイズを記録するため、リセットで削除したはずの設定が次回起動時に復活していたのです。保存されたセッションはその目に見える側面でした — 設定をリセットして FoDE を開き直すと、同じケースが同じレイアウトで開かれるという、求めていたものと正反対の結果になっていました — が、`window_size` を含めファイル内のすべてのキーが同じ影響を受けていました。現在は削除が維持されます。アプリケーション設定をリセットした後は、ウィンドウを閉じても何も保存されないため、次回起動時はデフォルトの状態から始まります。ダイアログの「アプリケーションを再起動してください」が本来意味していたのはこのことであり、完了メッセージにはその代償 — そのセッションのレイアウトとサイズは保存されないこと — を明示するようにしました。リセット後に自分で変更した設定（テーマの選択など）は従来どおり保存されます。抑止されるのは終了時の自動的な記録だけです。`schema_config.json` は終了時に書き込まれることがないため、スキーマ設定のリセットには影響しません。

- **Reset Settings ダイアログが存在しない設定を説明しなくなりました** — **Application Settings (app_config.json)** のツールチップは「ケースディレクトリ、ウィンドウサイズ、最近のケースをリセット」となっていました。最近のケースという機能は存在せず、一方でこのファイルはその後テーマ、言語、ケースライブラリ、ユーザーリンク、OpenFOAM インストールディレクトリ、保存されたセッションを保持するようになっていましたが、そのいずれも記載されていませんでした。特に保存されたセッションは、リセットで最も気づかれやすい部分であるだけに問題でした。ツールチップは実際にリセットされる内容を列挙するようにしました。確認ダイアログの文言も、日本語訳が `i18n/ja.py` に用意されていながら未使用のまま英語の断片を組み立てていたため、翻訳されるようにしました。

## v1.10.0 — 2026-07-30

### 新機能

- **`#include` で取り込まれるファイルを一覧表示し、閲覧できるようになりました** — `#include "caseSettings"` や `#includeEtc "caseDicts/setConstraintTypes"` と書かれた辞書に対して、FoDE はこれまで何も示せませんでした。ディレクティブは不透明なテキストとして保持されるだけで、名指しされたファイルは見えないまま — ケースを理解するにはその中身を読む必要があることが多いにもかかわらず、です。これらのディレクティブを解決し、参照先のファイルを `↳` マーク付きでファイル一覧に表示するようになりました。ケース内に解決したインクルードは本来所属するグループに入るので、`constant/caseSettings` は **constant** の下に現れ、他のファイルと同じように編集できます。OpenFOAM のインストール先に解決するもの — v2512 チュートリアルの全 1,081 件のディレクティブのうち `#includeEtc` だけで 560 件を占め、全体のおよそ半数がこちらです — は一覧下部の新しい **included files** グループに斜体でまとめられ、取り込み元のファイル名がツールチップに表示されます。取り込み元の辞書に内容が展開されることはありません。インクルードされたファイルはシンボリックリンクされたファイルと同様に独立したファイルのままで、ディレクティブもツリー上の 1 行として残ります。その行を右クリックすると **Open Included File** が使え（Key 列・Type 列のダブルクリックでも同じ）、カーソルを合わせるとディレクティブの解決先、あるいは対象が存在しないこと・オプションの `#sinclude` が単に無いこと・`#includeEtc`/`#includeFunc` を解決する OpenFOAM インストールが見つからないことが報告されます。`<case>`/`<system>`/`<constant>`/`<etc>` のパストークン、`$FOAM_*` 変数、`#includeFunc mag(U)` 形式の引数を含め、5 つのディレクティブ形式すべてに対応します。v2512 チュートリアルの 1,081 件中 1,072 件が解決し、残る 9 件は `Allrun` が生成するファイルで、未実行のケースでは実際に存在しないものです。インクルードの追跡は 1 段階のみで、`#codeStream` 本体に含まれる C++ ヘッダーは辞書として提示せず意図的に無視します。

- **OpenFOAM インストール内のファイルは読み取り専用になり、ワンクリックで自分のものにできます** — `#includeEtc` の対象は OpenFOAM のインストール先にあり、マシン上の全ケースが共有しています。その場で編集してしまうと、気づきにくく取り消しはさらに難しい失敗になります。こうしたファイルは通常どおり開き — ツリーもテキストもシンタックスハイライトも揃っています — その一方でエディタはロックされて `read-only` バッジが表示され、ツリー項目は編集できず、Save・Save All・Create Backup・Duplicate・Delete は書き込むべきでない場所へ黙って書き込む代わりに利用不可となります。ファイルのコンテキストメニューにある **Copy into case...** が通り道です。ファイルをケース内へコピーし、既定の配置先は `system/` — OpenFOAM が自身の `etc/` より先に参照する、まさにその場所です — なので、`#includeFunc` ならディレクティブを一切編集せずにコピーが効きます。コピーは通常の編集可能なケースファイルとしてファイル一覧に登録されるため、後からディレクティブを変更しても残ります。コピー操作をまたいでも、他の開いているファイルの未保存の編集は保持されます。

- **`blockMeshDict` のブロックが Tree ビューで 1 ブロック 1 行になりました** — これまで `blocks ( … );` エントリは、すべてのブロックを 1 本の長い文字列として保持する単一行として表示されていました（`pitzDaily` チュートリアルでは 5 ブロック・約 380 文字）。BlockMesh 3D ビューアは以前から各ブロックを個別に色分け・番号付けして描画していたにもかかわらずです。今回から `block 0`、`block 1`、… と展開され、その並びと番号はビューアが各ブロックの中心に描画するものと一致します。これでようやく 2 つのビューが同じ対象を指すようになりました。各行は個別に編集でき、マウスオーバーすると頂点・セル数・グレーディング・ゾーンの内訳が表示されます。行をクリックすると、Editor タブはリストの先頭ではなく該当ブロックまでスクロールします。副次的にラウンドトリップの忠実性も向上しました: 従来は 1 ブロックを編集するとリスト全体が 1 行に折り畳まれていましたが、他のブロックはバイト単位でそのまま保持されるようになりました。`hex` ブロックが単純に並んだ形でないリスト — 空リスト、マクロで構成されたリスト — は従来どおりの単一行表示のままなので、これまで解析できていたものの解析結果が変わることはありません。`#include` がブロックに混在するリストは展開されます: ディレクティブはブロックの間に独立した 1 行を占め、ブロックの番号付けはそれを飛ばすため、番号は 0 から始まりビューアと一致したままです。1 点注意として、比較モードではブロックを位置で照合するため、5 ブロック中 2 ブロックが異なるファイルのマーカーはこれまでの `≠1` から `≠2` になります。

- **ツリーからブロックの追加・複製・削除ができるようになり、選択中のブロックが 3D でハイライトされます** — `blockMeshDict` のブロックを 1 行ずつ表示する変更で残っていた 2 点です。`block N` 行を右クリックすると、他のエントリと同様に **Add Entry After**・**Duplicate**・**Delete** が使えます。新規追加した行は空のプレースホルダではなく `hex (0 1 2 3 4 5 6 7) (10 10 10) simpleGrading (1 1 1)` という実在するブロックとして始まるため、次の **Apply Text to Tree** でも失われません。削除すると以降の行番号は自動的に振り直されます。ブロック行を選択すると BlockMesh 3D ビューアで該当ブロックが輪郭表示されます。行番号とビューアが各ブロックの中心に描画する番号が同じ番号だからです。この輪郭は **Block edges** と **Solid blocks** の両方をオフにしていても表示され、他の行を選択すると消えます。**Comment Out** はブロック行では引き続き無効です: `blocks ( … )` の中でコメントアウトされたブロックはエントリではなくコメントとして読み戻されるため、行が復元可能な形で残らず消えてしまうからです。

- **配色テーマ設定（Settings > Appearance）** — **Follow System** / **Light** / **Dark** の選択肢を追加しました。`app_config.json` に保存され、再起動時に適用されます。**Follow System**（既定）はプラットフォームのネイティブなウィジェットスタイルとデスクトップのパレットを維持します。**Light** と **Dark** は Fusion スタイルと FoDE 独自のパレットを強制するため、どの OS でもウィンドウの見た目が同一になります。ダークモードでは、エディタのシンタックスハイライト、行番号ガター、差分行の背景、通知バナー、そして BlockMesh 3D ビューアを含む UI 全体が再調整されます。3D ビューアはシーンの背景・軸/範囲のテキスト・頂点およびブロックのラベル・形状名バッジもテーマに追従するようになり、暗いウィンドウの中でビューアだけが白い矩形のまま残ることはなくなりました。UI が描画するすべての色は、十数個のファイルにハードコードされる代わりに、新設した `ui/theme.py` の単一テーブル経由で解決されるようになったため、2 つのテーマが食い違うことはありません — 例えば差分凡例のスウォッチとツリーの差分行背景は、構造上、同じ値を読むようになりました。

- **BlockMesh 3D パネルの Load STL / OBJ ダイアログで複数ファイルをまとめて選択できるようになりました** — ビューア自体は以前から任意の数の STL/OBJ オーバーレイを表示できましたが、ファイル選択ダイアログでは 1 回につき 1 ファイルしか選べませんでした。そのため `constant/triSurface/` にサーフェスが多数ある snappyHexMesh のケースでは、ファイルの数だけ **STL ▾ > Load STL / OBJ…** を開き直す必要がありました。今回からダイアログで複数ファイルを選択でき、選んだものが一度にまとめて読み込まれます（従来どおり既に表示中のオーバーレイに追加されます）。選択したファイルの一部が読み込めない場合も、最初の失敗で読み込み全体が終わってしまうことはなく、残りは読み込まれたうえで失敗したファイル名をまとめた警告が 1 回だけ表示されます。

- **読み込んだ STL/OBJ サーフェスが `STL ▾` メニューにファイルごとの行として表示されるようになりました** — ビューアは読み込んだサーフェスを名前のないメッシュとして保持し、元のパスを破棄していました。そのため複数を表示すると、何が読み込まれているのかを確認する手段も、1 つだけ非表示にしたり削除したりする手段もなく、**Clear STL** で全部消すか何もしないかの二択でした。今回から各ファイルが名前付きのチェック可能な行として表示されます。淡い配色パレットからファイルごとに色が割り当てられるため、3D ビューでもサーフェスを見分けられます。あわせてマスタートグルの **Show loaded surfaces** と **Show all surfaces** / **Hide all surfaces** も用意され、これは topoSet・snappyHexMesh・setFields・sample の各メニューが以前から備えていた構成と同じです。**Unload ▸** サブメニューで個別のファイルを削除でき、既に読み込み済みのファイルを読み込むと重複した行が増える代わりに既存の行へ再読み込みされるため、FoDE の外で編集したサーフェスの更新手段になります。また、`blockMeshDict` を開かずにサーフェス単体を読み込んだ場合も描画されるようになりました。従来は読み込まれて一覧にも載るのに描画されませんでした。表示するメッシュも辞書オーバーレイも無いときはビューアが描画自体をスキップしていたためです。

### バグ修正

- **`${...}` 形式のマクロ参照、および末尾のセミコロンを持たないマクロを含む辞書が解析できるようになりました** — OpenFOAM が受け付ける 2 つの記法が理解されておらず、どちらか一方があるだけでファイル全体が「テキストは読み込まれましたが、ツリーの更新に失敗しました」の警告となり、ツリーが空になっていました。1 つ目は波括弧付きのマクロ参照で、スコープパスを伴うこともあります: `propeller1/system/bladeForces` の `${../_bladeForces};`、`gasMixing` のサンプリング辞書の `${__settings}` など。FoDE のレキサーが `{` で単語を終えてしまうため、裸の `$` がエントリのキーとして残り、パーサが `;` を要求して失敗していました。2 つ目は終端子を一切持たない、文として単独で使われるマクロです — 複数の `pointDisplacement` にある `maxX { $minX }`、4 つの `pimpleFoam` の `fvSolution` にある `"pcorr.*" { $p … }` — こちらはパーサが `;` を必須としていました。この 2 つで OpenFOAM v2512 チュートリアルに残っていた解析失敗の**すべて**を占めていました。`tools/roundtrip_corpus.py` により `system/`・`constant/`・`0/`・`0.orig/` 配下の 9620 ファイルを測定すると、従来は 9501 件の解析に対し現在は 9620 件すべてが解析でき、バイト単位のラウンドトリップ率は 100% のままです。影響を受けていた 119 ファイルは特殊なものではなく、4 つのメッシュチュートリアルの `system/snappyHexMeshDict`、`createBafflesDict`、`fvSolution`、および `system/` にあるヘッダなしのサンプリング断片（`sampling`、`fieldTransfer`、`avg-tracer0`）などが含まれます。`;` は本当に省略可能であるため、エントリの一部としては保存せず、ライタは元ファイルが使っていた形をそのまま再現します。したがって裸の `$p` を編集しても、元々無かったセミコロンが黙って付くことはありません。

- **ファイルの保存時に、触れていない空行が書き換えられる問題を修正** — チュートリアルのケースを開いて何も編集せずに Save を押しただけでファイルが再整形されていました。`// * * *` ヘッダバナーの直後にあった空行がバナーの上に移動し、エントリ間の 2 行以上の空行が詰められ、1 行に書かれた複数エントリ（`x1 14; x2 6;`）が別々の行に分解され、閉じの `// ****` フッタバナーは丸ごと削除されていました。OpenFOAM v2512 チュートリアル内で解析可能な `system/blockMeshDict` 439 件すべてが影響を受けていました。チュートリアル全体で測定すると — `system/`・`constant/`・`0/`・`0.orig/` 配下の全ファイルのうち、この修正の時点では 9501 件が解析可能で（後述のマクロエントリの修正により、のちに 9620 件すべてになりました）— 本リリース以前にバイト単位で同一に保存されていたのは 286 件、現在は解析可能なファイルすべてです。この測定自体が `tools/roundtrip_corpus.py` として同梱されており、任意のインストールに対して再実行できます。原因は、パーサが既に「次のエントリに属する」と記録していた改行をライタが重ねて追加していたこと、およびその二重化を打ち消そうとした 2 つの大雑把な正規表現が実際の空行まで削っていたことです。フッタバナーは最後のエントリより後ろにありどのエントリにも属さないため、解析の時点で単純に破棄されていました。編集済みファイルの保存では実際に編集したエントリだけが書き換えられ、最終改行なしで終わるファイルは最終改行なしのまま保存されます。

- **`divSchemes { … };` と書かれた辞書のセミコロンが独立した行に移動する問題を修正** — 閉じ `}` の後ろの `;` は冗長ですが正当な記述です。FoDE はこれを別のエントリとして解析し、ステータスバーで「認識できないエントリ」として報告したうえ、保存時に次の行へ送り出していました。編集していないのに保存で変化する原因として最後まで残っていたのがこれで、v2512 チュートリアルでは 54 件（大半が `fvSchemes`）が該当します。その時点で解析可能だった 9501 件すべてに到達したのはこの修正によるものです。`;` は実在するテキストで書き戻す必要があるため引き続き独立したエントリですが、解析エラーとして数えられることはなくなり、ライタは元のソースで直前のエントリに接していたエントリを、新しい行を必要とするとみなさずそのままの行に保つようになりました。

- **ブロック内のエントリを編集すると右にずれる問題を修正** — 辞書の中にあるエントリを編集すると、インデントが 2 倍になって書き出されていました。`PIMPLE { … }` の中の `nCorrectors` を変更すると 4 スペースから 8 スペースに動き、ネストが深いほどさらにずれていきました。影響を受けるのはネストしたエントリの最初の行すべてで、通常の `key value;`、サブ辞書の名前、`#includeFunc` ディレクティブ、`$macro`、`regions`/`actions` のエントリ、`fieldValues` ブロックが該当します。そのため、ネストしたキーに対するツリー編集や **Use this value** の実行は、見た目にずれた行を残していました。原因は、ライタがソースから再現済みのインデントの上に、さらに自前のインデントを前置していたことです。`blocks ( … )` 内の個々のブロックだけが例外的に補正されており、それがこの問題が見過ごされていた理由でした。

- **Case Library ダイアログのモジュールが Python 3.10/3.11 でインポートに失敗する問題を修正** — `ui/dialogs/case_library_dialog.py` は、外側と内側（入れ子の `tr(...)`）の文字列リテラルで同じ引用符を再利用する f-string を使っており、これは Python 3.12 でのみ許可される構文緩和のため、3.10/3.11 ではモジュールをインポートした時点（つまり **Case > Manage Case Library…** を初めて開いた時点）で `SyntaxError` になっていました。入れ子の文字列をシングルクォートに変更し、サポート対象のすべての Python バージョンでパースできるようにしました。

- **Detail パネルが選択したフィールドエントリの値を表示するようになりました** — `field_value` 行（例: `0/` フィールドファイルの `boundaryField` 内の `value uniform (0 0 0)` エントリ）を選択すると、ツリーモデル上にはもう存在しない整形メソッドを呼び出しており、Qt スロット内で `AttributeError` が発生していました。アプリは動き続けるものの、Detail パネルの値ボックスは空のままで、コンソールにトレースバックが出力されていました。パネルは `foam/utils.py` にある実際の整形関数を呼ぶようになりました。型チェッカーを `ui/` に拡大した際に発見されたものです。

- **Case > Duplicate Case… で移動先フォルダが再び事前入力されるようになりました** — ダイアログの構築時に、既定の移動先親ディレクトリ文字列を期待する位置引数にメインウィンドウを渡していたため、Qt はそれをテキストフィールドのウィジェット親として解釈していました。その結果、移動先親ディレクトリのボックスはケースの親ディレクトリで事前入力されず空のまま開き、ダイアログ自体も親を持たない状態でした。型チェッカーを `ui/` に拡大した際に発見されたものです。

- **STL/OBJ オーバーレイが次のケースに持ち越されなくなりました** — **STL ▾ > Load STL / OBJ…** で BlockMesh 3D ビューアに読み込んだサーフェスは、別のケースを開いても読み込まれたままでした。そのため、離れたケースに属するジオメトリが新しいケースのメッシュの上に描画され続け、消すには **Clear STL** を使うしかありませんでした。ケースを開くと、パネルが表示する他のもの — topoSet/snappyHexMesh/setFields/sample の各オーバーレイとそのメニュー、頂点テーブル、3D シーン — は既にすべてリセットされていましたが、読み込み済みサーフェスもそれらと一緒にクリアされるようになりました。

- **Windows 11 で選択行が判読できない問題を修正** — Qt は選択行の塗り色をデスクトップから取得しますが（Windows では *設定 > 個人用設定 > 色* のアクセントカラー）、選択行の文字色はシステムパレットから独立して取得し、両者が組み合わせて機能するかは検証されません。Windows 11 ではこれにより、ツリーやファイル一覧で彩度の高い青の選択行の上にほぼ黒の文字が描画されていました。FoDE は 3 つのテーマモードすべてで、この組み合わせを自分で計算し直すようになりました。なお、一見自明な修正 — 黒と白のうちコントラストが高いほうを選ぶ — は問題を修正せずむしろ再現させます。Windows 11 の既定アクセント `#0078d4` は黒に対して 4.64:1、白に対しては 4.53:1 しかなく、数値上は黒が勝ちますが、それこそが読みにくい組み合わせだからです。代わりに適用したルールは、実際のデスクトップの挙動に従います: 塗り色の上で十分に読めるなら白文字、本当に明るい塗り色（黄色やパステル系のアクセント）の場合にのみ暗い文字、それ以外は白文字が読めるようになるまで塗り色をわずかに暗くします — 色相は保つため、ユーザーのアクセントカラーは判別できるままです。Linux と macOS は影響を受けていませんでしたが、同じ経路を通ります。

- **ブロックの間に置かれた `#include` によってブロックが 1 行に潰れる問題を修正** — パッチを別ファイルから取り込む同じ `blockMeshDict` は、ブロックについても同じことをしています（`blocks ( #include "blockMeshDict.caseBlocks" hex … );`）。このディレクティブによってリスト全体が展開不可能な 1 本の長い行にフォールバックしていたため、3D ビューアは 24 個のブロックすべてを描画しているのに、ファイル内に書かれたどのブロックも個別に選択・編集・ハイライトできませんでした。ディレクティブはブロック行の間に独立した 1 行を占めるようになりました。その中身は依然として不可視です — このチュートリアルケースでは、`Allrun` が実行時に作成し 2 つの代替ブロックファイルのいずれかを指すシンボリックリンクであり、編集時点では読むべき実体がありません — したがって番号付けは、3D ビューアが以前からそうしていたのと同じく、このファイルに書かれたブロックのみを数えます。これによりツリーの `block 0` とビューアが描画する `0` が同じブロックを指し続けます。blockMesh が include を解決した後の番号付けとは、両者ともずれる可能性があります。

- **パッチの間に置かれた `#include` によって `blockMeshDict` の境界が失われる問題を修正** — `boundary ( #include "blockMeshDict.caseBoundary" outlet { … } );` のように、パッチの一部を別ファイルから取り込む境界ブロックは境界としてパースできず、ブロック全体が生テキストへフォールバックし、認識できないエントリ 2 件として報告され、Tree ビューで編集可能なパッチ行を失っていました。しかも 3D ビューアは「何も描かない」のではなく「誤って描く」状態でした: フォールバックのテキスト走査が `include` をパッチ名として読み、*次の*パッチの面をそれに割り当てていたため、この書き方をしている v2512 tutorials の唯一の例（`compressible/rhoPimpleFoam/laminar/helmholtzResonance`）では `outlet` パッチがビューアから消え、代わりに `include` という幻のパッチがその 9 面を持っていました。このディレクティブはパッチの間に置かれた独立したエントリとしてパースされるようになり、その前後のパッチは構造的パースを維持します: 上記のチュートリアルはパースエラーなしとなり、`outlet`（patch）と `sides`（symmetry）が正しい面とともに表示されます。`#include` をこの形で使うファイルも従来どおりバイト単位で一致して保存され、上記の round-trip 値も変わりません。

- **同名のサンプリング辞書が 2 つあると 3D ビューアで互いに隠れてしまう問題を修正** — `sample ▾` オーバーレイは読み込んだファイルをファイル名だけで管理していたため、同じベース名の辞書が 2 つ目に読み込まれると（独自の `sample`・`probes`・`surfaces` を持つ追加ディレクトリを加えると到達します）、新しいほうが古いほうのシェイプをメニューから静かに置き換え、残った行も誤った元ファイル名を表示していました。ファイルはフルパスで管理されるようになり、メニュー行の表示はこれまでどおりファイル名のみです。

## v1.9.1 — 2026-07-24

### バグ修正

- **BlockMesh 3D ビューアで、2 軸方向でメッシュを包み込むオーバーレイ領域が「✂ clipped」と表示されたままフルサイズで描画される問題を修正** — `boxToCell`/`setFields` のボックス（や他のオーバーレイ形状）が x・y 方向ではブロックメッシュを包み込みつつ z 方向では表示クリップボックスで切られる場合、VTK のボックスクリップが空でない縮退した結果を返し、その外接直方体は切り取られていない巨大な範囲のままだったため、クリップマークが付いているにもかかわらずシーン全体を覆って描画されていました。クリップ処理は、クリップ結果が実際に表示ボックスの内側に収まっているかを検証するようになり、収まっていない場合はメッシュとボックスの交差範囲で代用します — これはシーン全体を完全に包み込む形状に対する既存の挙動と同じです。

## v1.9.0 — 2026-07-22

### 新機能

- **ツリー編集の Undo/Redo** — ツリー側のすべての変更が、ツリーにフォーカスがある状態の **Ctrl+Z** / **Ctrl+Shift+Z** で取り消し・やり直しできるようになりました（ツリーのコンテキストメニューの **Undo Tree Edit** / **Redo Tree Edit** からも実行可能）: インラインセル編集、Detail パネルの Apply、Paste Value、Add/Duplicate/Delete/Comment Out/Restore from Comment、比較モードの「Use this value」、**Apply Text to Tree**、BlockMesh の頂点ドラッグ、Boundary タブのパッチ操作が対象です。複数ファイルにまたがる境界操作（例: 全 `0/` フィールドでのパッチ名変更）は 1 ステップとして取り消され、触れたすべてのファイルが復元されます。Undo はセッション全体で単一の履歴です（最大 50 ステップ、ケース切り替えでクリア）: Ctrl+Z は場所を問わず最後の変更を取り消し、必要なら対象ファイルへ表示を切り替えます。テキストエディタは自由入力用に自身のネイティブ Undo を保持し、削除の確認ダイアログの「元に戻せません」という文言はなくなりました — 元に戻せるからです。

- **3D ビューアのサンプリングオーバーレイ（`sample ▾`）** — BlockMesh パネルに 4 つ目のオーバーレイソースが追加されました。サンプリング定義がティール色でブロックメッシュ上に描画されるため、実行前にプローブ位置やサンプル線・平面をジオメトリと照らし合わせて確認できます。プローブ点（`probes`/`patchProbes`/`boundaryProbes` の `probeLocations`）はマーカーとして、`sets` タイプのサンプル線（`lineUniform`・`lineCell`・`uniform`・`face` など `start`/`end` の組を持つもの、および `cloud` の点リスト）は細いチューブとして、`surfaces` タイプの `plane`/`cuttingPlane` メンバーは表示専用の広がりを持つ円盤として描画されます。定義は `controlDict` の `functions {}` ブロック**と**スタンドアロンのサンプリング辞書 — `system/sample`、`system/probes`、`system/surfaces`、`system/singleGraph`（トップレベルに `start`/`end` を置く `.org` スタイルを含む）— の両方から読み込まれ、これらのファイルはファイル一覧にも表示されるようになりました。複数ファイルの定義が同時に反映され、メニューの各行には元ファイル名のタグが付きます。メンバーリストは辞書形式 `sets { … }` と従来の丸括弧リスト形式 `sets ( y0.1 { … } … );` の両方に対応し、丸括弧リスト形式もパーサーが実際の編集可能なツリー（不透明な生ブロックではなく名前付きエントリ）として解析するようになり、`regions ( … )` と同様に比較モードでの差分表示も可能です。描画できないエントリ（`patch` サーフェスなど）は **Non-geometric sources (N)** に一覧され、通常のオーバーレイクリッピング（「✂ clipped」）も適用されます。他のオーバーレイ辞書と同様、これらのファイルでも `⊞` サイドバイサイド表示が有効になります。

- **View Log Summary がソルバーログも要約するようになりました** — `Allrun` の実行が書き出す `log.simpleFoam` や `log.interFoam` など（手動実行で `log.*` に tee したものも含む）は、汎用の末尾 20 行フォールバックではなくなりました。Summary タブに、時間ステップ数とシミュレーション時間の範囲、最終的な `ExecutionTime`/`ClockTime`、収束した場合の収束メッセージ、そして最終ステップの残差（フィールドごとの初期・最終残差と反復回数、Courant 数、累積連続性誤差）が表示されます。ソルバーログは実行ファイル名ではなくタイムループの形（`Time = …` のステップと `Solving for …` の残差行）で認識されるため、どちらの OpenFOAM フォークのどのソルバーでも動作します。収束にも `End` にも到達しなかった実行は FAILED として報告されるため、中断された実行が一目で分かります。

- **Tools メニュー「Run *」アクションのオプションダイアログ** — **Run blockMesh… / Run snappyHexMesh… / Run topoSet… / Run setFields… / Run checkMesh…** は即座に実行されなくなり、それぞれ小さなオプションダイアログを開くようになりました。ダイアログには、そのツールでよく使うフラグのチェックボックス・入力欄（blockMesh/topoSet/setFields: ケース相対パスを挿入する **Browse…** 付きの `-dict` と `-region`。snappyHexMesh: 加えてデフォルトでチェックされた `-overwrite`。checkMesh: `-allGeometry`・`-allTopology`・`-writeSets` の形式・`-region`）、それ以外のフラグを自由に書ける **追加オプション** 欄、そして実際に送信されるコマンドラインをそのまま表示するライブの **コマンド** プレビューがあります。プレビューには自動付与される `2>&1 | tee log.<ツール名>` リダイレクトも含まれます — この部分はユーザーが編集できないため、**View Log Summary** や Allrun のプレフライトは引き続きログを見つけられます。**実行** がデフォルトボタンなので Enter だけでデフォルト設定のまま実行でき、以前の確認ダイアログと比べてクリック数は増えません — それらのプレフライトはダイアログ内に統合されました：メッシュ系ツールでは「このケースには既に結果があります…」の警告がダイアログ上部に表示され、setFields の三択「0/ を復元してから実行」は（`0.orig/` が存在するときデフォルトでチェックされる）**先に 0/ を 0.orig/ から復元する** チェックボックスになり、その `rm -rf 0 && cp -r 0.orig 0 && ` プレフィックスもプレビューに表示されます。オプションの値はセッション中ツールごとに記憶され、追加オプション欄が不正な場合（引用符が閉じていない等）は壊れたコマンドを送る代わりに実行ボタンが無効になります。

- **Find OpenFOAM Examples からチュートリアルケースを複製** — Find OpenFOAM Examples ダイアログに **このケースを複製…** ボタンが追加されました（**このケースと比較** と同様に、一致箇所がチュートリアルケース内にある場合に有効になります）。そのチュートリアルをコピー元としておなじみのケース複製ダイアログを開き、「キーワードを検索 → それを使っているチュートリアルを見つける → そこから自分のケースを始める」というワークフローを、インストール先からの手動の `cp -r` なしで完結できます。コピー先の初期値はデフォルトケースディレクトリ（未設定ならホームディレクトリ — 読み取り専用のインストール先が提示されることはありません）で、デフォルトのコピーモードはディレクトリ全体のコピーのため `Allrun` スクリプト・`0.orig/`・`constant/triSurface/` のジオメトリも一緒にコピーされ、コピー完了後はその場で新しいケースを開くかどうかを選択できます。また **Find OpenFOAM Examples…** アクション自体が、新しいケースの出発点にもなることから、他の Duplicate アクションの隣の **Case** メニューにも表示されるようになりました（両方とも同じアクションです）。

### バグ修正

- **名前付き丸括弧ブロックのエントリを再生成しても兄弟エントリの名前が失われなくなりました**: `regions ( … )`（setFieldsDict）、`boundary ( … )`（blockMeshDict）、新しい `sets`/`surfaces` リストでは、エントリの保存済み生テキストが `{` から始まっていたため、ツリーで 1 つのエントリを編集するとファイルテキストの再生成時に*未変更の*兄弟エントリすべての名前が失われ、辞書が壊れていました。エントリの生テキスト範囲は名前トークンから始まるようになりました（ツリー→エディタの行ハイライトも名前の行を含むようになります）。

- **ケース切り替え後も古い比較が残る問題を修正**: Compare-with-Case の比較が有効なまま別のケースを開くと、差分バー・参照ケースの解析キャッシュ・ファイル一覧の差分マークが前のケースの参照先を指したまま残っていました。別のケースを開くと比較はクリアされるようになり、**Reload Case**（同じディレクトリ）では比較を維持したまま差分マークを再計算します。

- 比較モードで**参照ファイルの解析に失敗しても無言で終わらなくなりました**: 従来は比較ビューが空になるだけで「差分なし」と区別がつきませんでした。解析できなかった参照ファイル名をステータスバーの警告で表示し、ファイルごとの差分数を数えるバックグラウンドスキャンも、スキップした参照ファイルの数を報告します。

- **設定ファイルをアトミックに書き込むようになりました**（一時ファイル + リネーム）: 保存中のクラッシュやディスクフルで `app_config.json`、ケースの `.foam-editor-files.json`、生成された `foam_keywords.json` が空・不完全なファイルに壊れることがなくなりました（従来は次回起動時に無言で破棄されていました）。

- **ドットを含む識別子がシンタックスハイライトで分割されなくなりました** — `y0.1` のようなセット名は、キーワードルールが数字とドットの間の `\b` 境界に一致するため、`y0` の部分だけがキーワード色になり `.1` がプレーン表示になっていました。キーワードルールを数値ルールと同様にルックアラウンドでガードするようにしました。構造キーワード（例: `off.1`）やシェルキーワード（例: `config.fi`）にも同じ修正が適用されます。

### 改良

- **BlockMesh 3D ビューアで triSurface ファイルの gzip 圧縮を透過的にサポート** — `snappyHexMeshDict` の `triSurfaceMesh`/`distributedTriSurfaceMesh` ジオメトリが、OpenFOAM チュートリアルが配布する形式（例: `constant/triSurface/motorBike.obj.gz`）そのままに、`.gz` 圧縮されたサーフェスファイルを自動的に解決するようになりました。辞書エントリがプレーンなファイル名を参照していても、ディスク上に `.gz` 版しかなければそちらへ解決され、`.gz` 付きの名前を参照していてもディスク上に非圧縮ファイルがあればそちらへフォールバックします。手動の **Load STL / OBJ…** ファイルダイアログも `.stl.gz`/`.obj.gz`/`.stlb.gz` ファイルを直接開けるようになりました。

- `$FOAM_TUTORIALS` がエクスポートされていなくても、**Case Library がソース済み OpenFOAM 環境の `$WM_PROJECT_DIR` からチュートリアルを見つけられるようになりました**。Find OpenFOAM Examples が既に行っていたインストール検出と同じ動作です。

### ドキュメント

- **新しいスクリーンショットギャラリー**（`docs/SCREENSHOTS.md` / `docs/SCREENSHOTS_ja.md`） — メインウィンドウ、BlockMesh 3D ビューアの `topoSetDict`/`snappyHexMeshDict` オーバーレイ（topoSetShapes・floatingObject・motorBike の各ケース）、Find OpenFOAM Examples ダイアログと Tools メニューを注釈付きで紹介するギャラリーを追加し、`README.md`・`USER_GUIDE.md` の「目的別ガイド」テーブル・`DEVELOPER.md` のドキュメントマップからリンクしました。

### 内部変更

- 3 つのエクストラクタ形状データクラスのフィールド名を 1 つの命名に統一: `label`（表示名）+ `kind`（ジオメトリ/ソースのキーワード）が `TopoShape.source`、`SnappyShape.name`/`geo_type`、`SetFieldsShape.source` を置き換えました。これにより、ほぼ重複していた topoSet/setFields のレンダラメソッドが共有の `_render_source_shapes` に統合され、Export-STL ダイアログの橋渡し用フィールド `source_or_geo_type` も不要になりました。ユーザーに見える変化はありません。

- ソース済み OpenFOAM 環境の読み取りを一元化: `example_search`・`keyword_generator`・`AppConfigManager.foam_tutorials_dir` にそれぞれあった 3 つの `WM_PROJECT_DIR`/`FOAM_*` 読み取り実装が `services/foam_env.py` を共有するようになりました。

- topoSet/snappyHexMesh のボックス形状の解決を `foam/tree_utils.resolve_box_geometry` に、topoSet/sampling の点・法線平面の解決を `foam/tree_utils.resolve_plane_geometry` に統合（既存の共有 sphere/cylinder/cone リゾルバに合流）。Find OpenFOAM Examples と Generate OpenFOAM Keywords で重複していたインストール選択コンボ + Browse… のコードは共有ウィジェット `InstallationSelector` になりました。

- `--variant` プリセットはプライベート属性を直接書き換える代わりに、正式な `AppConfigManager.set_features()` API を通すようになりました。

- ユニットテストを追加: `foam/tree_utils.py` のリゾルバの直接テスト、`services/case_copier.py` のサービスレベルテスト、`services/foam_env.py` の環境検出テスト、`app_config/json_io.py` のアトミック書き込みテスト、ケース切り替え時の比較状態リセットの回帰テスト。

## v1.8.0 — 2026-07-15

### 新機能

- **ケースルートの `All*` スクリプトをファイル一覧に表示** — `Allrun`、`Allrun.pre`、`Allclean` などケースルート直下の `All*` スクリプトが、ファイル一覧の最後に新設された **case root** グループへ自動的に表示されるようになりました。Tools メニューが実行するスクリプトを、アプリを離れずに確認・編集できます。一覧に加わるのは `All*` ファイルのみで、ログ・`*.foam` ファイル・計算結果は従来どおり表示されません。スクリプトはシェルスクリプトであり辞書ではないため、エディタはシェル用のシンタックスハイライト（`#` コメント、文字列、`$変数`、`runApplication`/`runParallel` などの OpenFOAM ヘルパー、および通常のキーワードリストによるユーティリティ・ソルバー名）に切り替わり、ツリービューは空のまま、**Apply Text to Tree** は無効、比較モードの対象外です。編集・保存しても実行権限は保持されます。副次的な効果として、**ケースの複製** / **新しいケースとして保存** にスクリプトが含まれるようになり、複製したケースも **Tools > Run Allrun Script** から実行できます（従来は複製時に暗黙的に失われていました）。関連する 2 つの改良: 隠しファイル（アプリ自身の `.foam-editor-files.json` などのドットファイル）は追加ディレクトリのスキャンおよび Add files ダイアログから常に除外されるようになり、追加ディレクトリ経由で一覧に含まれた `log.*` 実行ログはグレー表示され、サイズにかかわらず辞書パーサーを通さずプレーンテキストとして開かれます（100 KB 超の警告ダイアログの「ツリービューは利用できません」という文言が実際に守られるようになりました）。また **case root** ヘッダーにも他のディレクトリヘッダーと同じ右クリックメニュー（**New file in 'case root'...** / **Add files from 'case root'...**）が表示されるため、実行ログや `README` などの特定のルートファイルを、ルートディレクトリ全体を登録することなく個別に一覧へ追加できます。

- **BlockMesh 3-D パネル: 折り返しツールバーと並列表示の自由なリサイズ** — パネル上部のコントロール（Vertices ▾、Blocks ▾、各オーバーレイメニュー、Refresh、STL ▾、Scale ▾、ラベルサイズ、ビューボタン）が 1 本の折り返し式ツールバーにまとまりました。パネルが広いときは 1 行に収まり、狭いときはボタンが複数行に流し込まれるため、従来のように約 700 px の最小パネル幅を強制しません。並列表示（サイドバイサイド）モードでは、スプリッターをその最小幅より狭くドラッグしても 3-D ペインが折りたたまれて消えることがなくなりました。両ペインとも折りたたみ不可となり、3-D ビューは実用的な最小幅（150 px）まで滑らかに狭められます。マウス操作ヒント行と Preview バナーもリサイズを妨げず折り返して表示されます。

- **BlockMesh 3-D パネルへの setFieldsDict 領域の表示** — `setFieldsDict` を読み込んだり編集したりすると、既存の `topoSetDict`/`snappyHexMeshDict` オーバーレイと並んで、その `regions ( … )` リストが 3-D パネルにオーバーレイ表示されるようになりました。領域ソースは topoSet と同じジオメトリキーワードを使うため、同じシェイプ（box、複数 box、rotated box、sphere、cylinder、cone — `$変数`/`#eval{ 式 }` の解決も同様）が描画されます。各シェイプはオレンジ色で描画され、エントリの `fieldValues` の要約（例: `alpha.water=1`）がラベルとして表示されます。新しい **setFields ▾** メニューは topoSet/snappyHexMesh のメニューと同じ構成（マスタートグル、Show/Hide all、シェイプごとのチェックボックス、`zoneToCell` のようなエントリ用の Non-geometric sources サブメニュー）を持ち、シェイプは 3 つ目のグループとして **Export Shapes as STL…** にも加わります。また、パネルの ⊞ 並列表示トグルは `setFieldsDict` がアクティブなファイルの場合にも有効になります。

- **ブロックメッシュより大きいオーバーレイシェイプのクリップ表示** — damBreak の `box (0 0 -1) (0.1461 0.292 1)` のような `setFieldsDict` 領域はブロックメッシュをはるかに超えて広がり、3-D ビューでメッシュが極端に小さく見えてしまっていました。すべてのオーバーレイシェイプ（topoSet、snappyHexMesh、setFields）は、ブロックメッシュのバウンディングボックスを各軸 10% 拡大した範囲に（表示上のみ）クリップされるようになり、クリップされたシェイプのシーンラベルには「✂ clipped」マークが付くため、切り取られた形状を実際の大きさと見誤ることはありません。ブロックメッシュの完全に外側にあるシェイプはクリップせずに「⚠ outside block mesh」とラベル付けされ、シーン全体を包み込むシェイプ（内側に表面が一切見えないもの）は消えてしまう代わりにバウンディングボックスの重なり部分として描画されます。**Export Shapes as STL…** は常にクリップ前の完全なジオメトリを書き出します。

- **Run setFields / Run checkMesh** — Terminal タブで実行する 2 つの Tools メニューアクションを追加しました。**Run setFields** は `setFields` を実行して `log.setFields` に tee します。setFields は `0/` の場ファイルを直接書き換えるため（設定済みの場に再実行すると値が重ねて適用されます）、`0.orig/` があるケースでは **0/ を復元してから実行**（`rm -rf 0 && cp -r 0.orig 0 && setFields …`）・**そのまま実行**・**キャンセル**の三択を提示し、`0.orig/` がないケースでは通常の確認ダイアログを表示します。**Run checkMesh** はメッシュを検証して `log.checkMesh` に tee します。メッシュを読むだけなので確認なしで実行されます。どちらのログも汎用ログパーサー経由で **View Log Summary** に表示されます。

- **ヘルプ > バージョン情報にバージョン番号を表示** — バージョン情報ダイアログのタイトル下にアプリケーションのバージョンが表示されるようになりました。番号は単一の情報源（`_version.py`）から取得します。git チェックアウトでは、直近のタグからのコミット数とコミットハッシュ（作業ツリーが汚れている場合は `*`、例: `1.7.0 (dev+12, ge75192d*)`）が付加され、バグ報告時にビルドを正確に特定できます。インストール済みのコピーではリリース番号のみを表示します。

- **`constant/polyMesh` の有無・古さインジケーター** — メッシュが存在する場合、ファイル一覧に `constant/polyMesh: N cells` 行が表示されるようになりました（セル数は `owner` ファイルの `FoamFile` ヘッダーの note フィールドから読み取り、メッシュ自体の解析は不要です）。`blockMeshDict` がメッシュ生成後より新しい場合、インジケーターはアンバー色になり「stale (blockMeshDict changed since last run)」と表示されます。

- **BlockMesh 3-D パネルへの `snappyHexMeshDict` ジオメトリの表示** — `snappyHexMeshDict` を読み込んだり編集したりすると、`blockMeshDict`/`topoSetDict` とは独立して、その `geometry {}` ブロックが 3-D パネルにオーバーレイ表示されるようになりました。対応するエントリタイプ: `box`、`sphere`（`radius` を軸ごとのベクトルで指定した場合はイグルー型ドームのようなだ円体として表示）、`cylinder`、`cone`、`triSurfaceMesh`/`distributedTriSurfaceMesh`（参照先の STL/OBJ ファイルを `constant/triSurface/` から自動読み込み）、box ベースの `collection`（searchableSurfaceCollection）メンバー（各インスタンスをスケール・回転・平行移動済みの個別のボックスとして描画）。各シェイプは `castellatedMeshControls.refinementSurfaces`/`refinementRegions`（`"iglo.*"` のような正規表現パターンのサーフェス名キーを含む）と照合して **surface**・**region**・**geometry のみ** のいずれかに分類され、それぞれ色分け表示されます（ティール／紫／グレー）。`locationInMesh` およびマルチリージョン用の `locationsInMesh` はラベル付きの保持点マーカーとして描画されます。**「snappyHexMesh ▾」** メニューでオーバーレイ全体または各シェイプを個別に表示・非表示にできるほか、カテゴリごとの色凡例と、解決できなかったジオメトリ（STL ファイルが見つからない、collection のメンバーが 1 つも box として解決できない、など）のグレーアウト項目も表示されます。トップレベルの `$変数` および `#eval{ 式 }` は `blockMeshDict`/`topoSetDict` と同じ仕組みで解決されます。

- **BlockMesh 3-D パネルで描画できる topoSetDict ソースの拡充と、スケーラブルな topoSet ▾ メニュー** — `topoSetDict` オーバーレイが実際の辞書で使われるソース構文をより広く認識するようになりました（OpenFOAM v2606 のほぼ全ソースを網羅した `pisoFoam/RAS/cavity` チュートリアル辞書では、67 アクション中 6 個だった描画数が 21 個になり、黙って脱落するエントリはなくなりました）。新たに認識されるもの: `boxToCell/Face/Point` の `min`/`max` 形式と複数ボックスの `boxes ( … )` リスト形式、`origin` で宣言された球（OpenFOAM の第一キーワード — 従来は互換エイリアスの `centre` のみ対応）と `innerRadius` による中空球、ラベル付きマーカーとして描画される点を持つソース（`nearestToCell`/`nearestToPoint` の `points`、`regionToCell` の `insidePoints`/`insidePoint`、`regionToFace` の `nearPoint`）、そしてシーンの大きさに合わせた円板として描画される `planeToFaceZone`。60 以上のアクションがあっても **topoSet ▾** メニューが使いやすいままであるように、**Show all shapes** / **Hide all shapes** アクションを追加し、描画可能なジオメトリを持たないエントリは長いグレーアウトリストの代わりに **Non-geometric sources (N)** サブメニューにまとめるようにしました。**snappyHexMesh ▾** メニューにも一貫性のため同じ 2 つの改善を適用しています。点マーカーと平面のシェイプは意味のある STL サーフェスを持たないため **Export Shapes as STL…** の対象外です。同梱の `tutorials/topoSetShapes` デモケースは新しい形式をすべて含むようになりました。

- **Run snappyHexMesh / Run topoSet** — 既存の「Run blockMesh」と同様の Tools メニューアクションを追加しました。それぞれコマンドを Terminal タブに送り、出力をケースディレクトリの `log.snappyHexMesh` / `log.topoSet` に tee します。ケースに既に数値の結果ディレクトリがある場合は、再実行がそれらを無効にする可能性があると事前に警告します。

- **Run Allrun Script / Run Allclean Script / Clean Case (foamCleanTutorials)** — Terminal タブで実行する 3 つの Tools メニューアクションを追加しました。**Run Allrun Script** はケースの `./Allrun`（ソルバーを含むワークフロー全体）を実行します。OpenFOAM の `runApplication`/`runParallel` ヘルパーは `log.*` ファイルが既に存在するステップをスキップするため、実行済みのケースでは何も起きないように見える代わりに、**クリーンしてから実行**（`foamCleanTutorials && ./Allrun` — 標準的な `./Allclean && ./Allrun` イディオム）・**そのまま実行**・**キャンセル**の三択を提示します。**Clean Case** は `foamCleanTutorials` を実行します。ケースに独自の `Allclean`/`Allwclean` スクリプトがあればそれを使い、なければメッシュ・時刻ディレクトリ・`processor*/`・`postProcessing/`・`log.*` ファイルを削除し、さらに `0.orig/` が存在する場合は `0/` も削除します（標準の `-auto` チュートリアル慣例）。確認ダイアログにはこれらのうちどれが起きるかが正確に表示されます。**Run Allclean Script** は、スクリプト自体を明示的に実行したい場合のために `./Allclean` を直接実行します。3 つとも破壊的な操作の前に必ず警告し、ケースまたはターミナルパネルがない場合は無効になります。

- **形状を STL としてエクスポート** — BlockMesh 3-D パネルに新しい **STL ▾ → Export Shapes as STL…** アクションを追加しました。読み込み済みの `topoSetDict`/`snappyHexMeshDict` の描画可能な各シェイプ（パラメトリックな box/sphere/cylinder/cone/rotated box に加え、`triSurfaceMesh`/collection 由来のものも含む）をチェックボックス付きで一覧表示し、初期状態は 3-D ビューでの現在の表示/非表示に対応します。出力先フォルダを選ぶと、チェックした各シェイプがそれぞれ個別の `.stl` ファイルとして名前（衝突時は重複排除）付きで書き出されます。描画可能なジオメトリを持たないシェイプはエラーにせずスキップとして報告されます。

- **Find OpenFOAM Examples** — キーワードや設定の実際の使用例を `find`/`grep` なしで探せる、新しい非モーダルの Tools メニューダイアログを追加しました。ローカルの OpenFOAM インストールのチュートリアルケース（`tutorials/`）と `etc/caseDicts/` 配下の厳選テンプレート（`#includeFunc` で利用できる `postProcessing/` の function object テンプレートを含む）の両方を検索します。インストールは環境変数（`WM_PROJECT_DIR`/`FOAM_TUTORIALS`/`FOAM_ETC`）が設定されていればそこから、なければ一般的なインストール場所（`/usr/lib/openfoam/`、`/opt/`、`~/OpenFOAM/`）から自動検出され、バージョンセレクタで切り替えられます。任意のディレクトリを参照して指定することもでき、その選択はセッションをまたいで記憶されます。検索結果はチュートリアルケース／テンプレートフォルダごとにグループ化され、ファイル名（`controlDict`、`fvSchemes` など）で絞り込みでき、シンタックスハイライト付きの読み取り専用プレビューが最初の一致行までスクロールして表示されます。そこからファイル全体または選択した部分をクリップボードにコピーできるほか（**ファイルをコピー** / **選択範囲をコピー** の 2 つのボタン）、一致箇所がチュートリアルケース内にある場合は **このケースと比較** をクリックすると、そのケースが既存の「ケースと比較」ビューにそのまま読み込まれ、右クリックの **この値を使用** で個々の設定を自分のケースに取り込めます。

- **View Log Summary** — `log.*` ファイル（blockMesh、snappyHexMesh、topoSet）を生テキストの代わりに短いレポートに要約する、新しい非モーダルの Tools メニューダイアログを追加しました。フェーズごとの最終メッシュ統計と反復回数、最終的なパッチ別レイヤー厚テーブル、topoSet のセル・面・点セットごとの 1 行要約、件数付きで重複排除された警告（致命的エラーは常に全文表示）を表示します。「Raw Log」タブでは加工前のテキストもすぐ確認できます。非モーダルであるため、辞書の設定内容とログの結果を見比べながら、ツリーやエディタの横に表示したまま作業を続けられます。ダイアログを閉じても次回開いたときに表示されなくなる不具合を修正し、別のケースを開くとアプリの他のパネルと同様にそのケースの最新ログへ自動的に切り替わるようになりました。

- **シンタックスハイライトのキーワード抽出を強化し、インストールを選択可能に** — **Settings > Generate OpenFOAM Keywords…** の背後にあるキーワードスキャナが、`src/` と `applications/` のソースに含まれる辞書読み取り呼び出し（`lookup("…")`、`get<…>("…")`、`readEntry("…")` など）も grep するようになりました。`controlDict` の `application`、`writePrecision`、`timePrecision` のような自由形式の辞書キーが実際に名前として現れるのはここであり、これまでスキャンしていた caseDicts テンプレートや `TypeName` マクロには一切登場しませんでした。同梱のキーワード一覧も新しいスキャンで再生成した（約 3,100 → 約 5,200 キーワード）ため、これらのキーは最初からハイライトされます。ダイアログには Find OpenFOAM Examples と同じ自動検出付きの **Installation** セレクタ（記憶されるディレクトリも共有）が追加され、OpenFOAM 環境を source しなくても任意のインストール済みバージョンからキーワード一覧を構築できます。CLI では `tools/generate_foam_keywords.py --dir <インストールルート>` が対応します。生成される JSON には来歴情報（`source`、`version`、`generated`、ライセンスに関する `note` — ファイルにはキーワード識別子名のみが含まれます）が記録されるようになりました。

- **同梱デフォルトのキーワード一覧をユーザー生成分から分離** — 同梱ベースラインは `app_config/foam_keywords.default.json`（git 管理、現在は OpenFOAM v2512 から生成）に移り、**Generate OpenFOAM Keywords…** は従来どおり `app_config/foam_keywords.json` に書き込みますが、このファイルは gitignore 対象になり、存在する場合にデフォルトより優先されるだけになりました。一覧を再生成しても git チェックアウトが汚れることはなく、ユーザーファイルを削除すれば同梱デフォルトに戻ります。

### 不具合修正

- 擬似 2D ケース（例: damBreak）で **Boundary faces** をチェックしても何も起きないように見える問題を修正しました。このようなケースは薄い側面の壁だけを `boundary` に列挙し、大きな前面・背面は blockMesh の暗黙の `defaultFaces` パッチに属するため、これまで一切描画されていませんでした。どのパッチにも割り当てられていない外部ブロック面が `defaultFaces` として抽出され、**Boundary faces** がチェックされているときに薄いグレーの面として描画されるようになったため、2D ケースでもチェックボックスの効果が目に見えるようになりました。

- **View Log Summary…** が **Tools** メニューに加えて **View** メニューにも表示されるようになりました（両方とも同じアクションです）— 「View」で始まる項目は View メニューで探されがちなためです。

- Tools メニューの欠けていた日本語訳（メニュー名、各アクションのラベルとツールチップ、Restore 0/・Run blockMesh/snappyHexMesh/topoSet・Open Mesh in ParaView の確認/警告ダイアログ）に加え、**Settings > Generate OpenFOAM Keywords…** と BlockMesh 並列表示トグルのツールチップの訳を追加しました — これらは日本語モードでも英語で表示されていました。

- 比較モードの **Use this value** が `#includeFunc …` ディレクティブのような名前を持たないエントリを正しく扱えない不具合を修正しました。ディレクティブにはキー名がないため、このアクションは囲むブロック自体を対象にしてしまい、`functions {}` 辞書全体を 1 行のディレクティブで上書きする可能性がありました。名前のないエントリは内容で照合して正しいブロックの末尾に追加され、同一のエントリが既にある場合は複製せずスキップされます。また、エントリを囲むブロックが現在のケースに存在しない場合に適用を拒否しなくなりました。不足している親辞書（例: `functions {}`）は自動的に作成されるため、参照ケースや例のケースで見つけた設定を、そのブロックを持たないケースにも取り込めます。

- `save_file()` / `save_all_files()` がファイルをその場で書き換えた後、ディスク由来のファイル一覧インジケーター（新しいメッシュ staleness フラグを含む）を更新しない不具合を修正しました。`QFileSystemWatcher.directoryChanged` はディレクトリエントリの追加・削除にのみ反応し、既存ファイルのその場での書き換えには反応しないため、両方の保存パスが `_reload_file_list()` を直接呼び出すようにしました。

- BlockMesh 3-D パネルの **⊞** サイドバイサイド切り替えボタンが `blockMeshDict` がアクティブなファイルのときしか有効にならない不具合を修正しました。パネルはすでに `topoSetDict`/`snappyHexMeshDict` のジオメトリも同じビューにオーバーレイ表示しているため、これら 2 つのファイルタイプでもボタンが有効になるようにしました。

- 別のケースを開いたときに `topoSetDict`/`snappyHexMeshDict` のオーバーレイ状態（シェイプ、シェイプ単位のメニューチェックボックス、保持点マーカー、**Export Shapes as STL…** アクションの有効/無効状態）がクリアされない不具合を修正しました。新しいケースディレクトリを開く処理は、パネル自身の `clear()` を呼ぶ代わりに内部のシェイプ一覧だけを直接リセットしていたため、新しいケースの `topoSetDict`/`snappyHexMeshDict` がたまたま読み込まれるまで、前のケースのメニュー項目・`locationInMesh`/`locationsInMesh` マーカー・3-D オーバーレイが表示されたままになっていました。

- エディタの数値ハイライトが識別子の内部にはみ出す不具合を修正しました。単語にくっついた数字 — `wall0` のようなパッチ名の `0`、`inlet-1` の `1`、`0wall` の先頭の `0` — が、独立した数値であるかのようにティール色に着色されていました。数値ルールは前後両側で境界判定されるようになり、本来の数値（`0.05`、`-1e-05`、ベクトル成分）には影響ありません。

- **Settings > Generate OpenFOAM Keywords…** をただ開いて閉じるだけで、現在のファイルが誤って編集済み（タイトルの `*`）とマークされる不具合を修正しました。ダイアログの後に実行される再ハイライトは、書式しか変わっていなくても Qt の `textChanged` を発火させます。この処理はエディタパネルのプログラム的更新ガード（Highlight ツールバートグルが既に使っていたものと同じ）を通るようになり、実際の編集だけがファイルを編集済みにします。

- xterm モードから Simple ターミナルに切り替えた直後、作業ディレクトリがケースディレクトリではなくアプリケーション自身のディレクトリになる不具合を修正しました。差し替え後のターミナルウィジェットにケースディレクトリが伝えられる時点ではシェルプロセスがまだ起動中で、プロセスが `Running` 状態でないために `cd` コマンドが黙ってスキップされていました。プロセスが `NotRunning` でない限りコマンドを送るようにしました — `QProcess` は起動中の書き込みをバッファし、シェルの起動完了後に流し込みます。

- **Apply Text to Tree** が `snappyHexMeshDict` の 3-D オーバーレイを更新しない不具合を修正しました。適用パスはファイル名→ビューアのディスパッチを独自に複製しており、そこに snappyHexMesh のケースが欠けていたため、テキストエディタで編集したジオメトリはファイルを保存するかツリー経由で編集するまで 3-D ビューに反映されませんでした。読み込み・保存・ツリー編集・適用のすべてのパスが単一のディスパッチヘルパーを共有するようになりました。

### ドキュメント

- **マニュアルでの機能の見つけやすさを改善** — `USER_GUIDE.md` の「目的別ガイド」テーブルを 27 行から 41 行の目的別の項目に拡充しました（例: `topoSetDict` ジオメトリの 3-D 表示、フィールドファイルを開かずに全境界条件を一覧、形状の STL エクスポート、パッチの一括リネーム、foamMonitor での残差プロット）。目次にも、これまで抜けていた Tools 各セクション、**Generate OpenFOAM Keywords**、**同梱サンプルケース**、BlockMesh パネルのサブセクションを追加しました。`README.md` の「主な機能」は、各グループ見出しがユーザーガイドの対応セクションへディープリンクするコンパクトな概要に整理し、ターミナルを必要としない Tools アクションを「統合ターミナル」から独立した **ツールメニュー** グループへ移動、抜けていた Allrun/Allclean/Clean Case も追加しました。`DEVELOPER.md` にはドキュメントマップ（各ドキュメントの役割と、ユーザーに見える機能を追加したら「目的別ガイド」と目次にも追加するというルール）を追加し、プロジェクト構成ツリーに `tutorials/` と開発者ガイド・リリースノートのファイルを載せました。

- **SoftwareX 掲載論文の書誌情報を追加** — README の引用セクションとトップの告知が、掲載済み論文の正式な書誌情報 — *SoftwareX*, Volume 35, 2026, 102852, [doi:10.1016/j.softx.2026.102852](https://doi.org/10.1016/j.softx.2026.102852) — を記載するようになり、「採録済み・詳細は近日追記」のプレースホルダーを置き換えました。

### 内部変更

**リファクタリング: JSON 設定ヘルパーの共通化**

- `app_config/json_io.py`（`load_json`/`save_json`）を追加し、`AppConfigManager` と `CaseFilesConfig` がそれぞれ個別に実装していた読み込み・破損時のフォールバック・書き込みロジックをこれに置き換えました。機能変更はありません。

**リファクタリング: `snappyHexMeshDict` スキーマをパッケージへ分割**

- `schemas/snappy_hex_mesh_dict.py`（776 行、70 キー）を `schemas/snappy_hex_mesh_dict/` パッケージへ変換し、サブドメイン別（geometry、castellated mesh、snap controls、layers、mesh quality）のサブモジュールを `__init__.py` で統合するようにしました。スキーマレジストリが使用する `schemas.snappy_hex_mesh_dict` のインポートパスは変わりません。機能変更はありません。

**リファクタリング: `AppState` の diff フィールドを `DiffState` サブデータクラスへ**

- `AppState.diff_case_dir` と `diff_parsed_roots` を、既存の `FoamMonitorState` と同様のパターンで `DiffState` サブデータクラス（`AppState.diff`）にまとめました。機能変更はありません。

**`ruff`/`mypy` のスコープを拡大**

- `ruff` と `mypy` の対象に、既存の `foam/` と `model/` に加えて `app_config/`、`schemas/`、`services/`、`ui/app_state.py` を追加しました。クリーンな状態にするため、これらのディレクトリに残っていた既存の lint/型の問題（スキーマの説明文字列の行長調整、型注釈の不足2件）を修正しました。`ui/` の残りの部分は引き続き対象外です。

**リファクタリング: UI 周りの重複排除と、抽出・整形ヘルパーの共通化**

- BlockMeshPanel の `topoSet ▾` / `snappyHexMesh ▾` メニュー — 従来は約 25 個の属性が 2 系統並列に存在し、再構築・可視判定メソッドも鏡写しに重複していました — を、単一の `_ShapeOverlayMenu` ヘルパークラスの 2 インスタンスに置き換えました。挙動の違いは 1 点のみ: snappy の保持点トグルの有効/無効が、次回の再構築時ではなくマスタートグルの切り替えに即座に追従するようになりました。
- Tools メニューの実行系アクションは `_run_in_terminal()` / `_confirm_rerun_over_results()` ヘルパーを共有し、メニュー構築はループ化され、繰り返されていた `QMessageBox.question` ブロックは共通の `_confirm()` はい/いいえヘルパーに置き換えました。
- `ui/mixins/_boundary_ops.py` に `_find_boundary_field` / `_append_new_patch` / `_set_patch_children` ヘルパーを追加し、5 箇所に複製されていた boundaryField 検索と、繰り返されていたパッチ作成・置換ブロックを置き換えました。
- リーフ値の整形を共通化しました。`foam/utils.py` の新しい `format_leaf_value()` をライター（直列化）とツリーモデルの Value 列（表示）の両方が使うため、表示と直列化が食い違うことはなくなりました。
- `foam/topo_set_extractor.py` と `foam/snappy_hex_mesh_extractor.py` の間で重複していた sphere/cylinder/cone のジオメトリ解決を、`foam/tree_utils.py` の共有リゾルバ（`resolve_sphere_geometry`、`resolve_cylinder_geometry`、`resolve_cone_geometry`）へ移動しました。
- カプセル化: 他モジュールから呼ばれている `FoamTreeModel.index_of_node`/`attach_parents` と `BlockMeshPanel.init_plotter` をパブリックにし、`FileListPanel` に `file_paths()` アクセサを追加して、diff の事前計算が内部のリストウィジェットへ直接アクセスしないようにしました。
- `i18n.tr()` は、呼び出しのたびに言語モジュールを再インポートする代わりに、`set_language()` 時にキャッシュした変換テーブルを参照するようになりました。

## v1.7.0 — 2026-07-04

### 新機能

- **`topoSetDict` の `actions` ブロック**を、不透明な生テキストではなく構造化ツリーとしてパースするようになりました。各無名 `{ … }` エントリが `action_entry` ノードになり、その子ノード（`name`、`type`、`action`、`source`、`box`、`set` など）をツリービューで個別に表示・編集できます。差分比較はアクションエントリを位置ベースで照合します。

- **テキストエディタのシンタックスハイライト** — プレーンテキストエディタで OpenFOAM 辞書ファイルのトークンが色付けされるようになりました: `//` および `/* */` コメント（グレー・斜体）、文字列リテラル（緑）、`#ディレクティブ`（紫・太字）、`$マクロ` 参照（オレンジ）、`FoamFile`・`true`/`false`・`uniform`・`nonuniform` などの予約済みキーワード（青・太字）、数値（ティール）。PySide6 組み込みの `QSyntaxHighlighter` を使用しており、新たな依存関係はありません。

- **テキストエディタのコード折りたたみ** — 複数行にまたがる `{ … }` ブロックに加え、2 行以上連続するコメント行（複数行の `/* … */` ブロックや連続する `//` 行）ごとに、行番号ガターにクリック可能な ▾/▶ 三角形が表示されるようになりました。三角形をクリックすると領域の折りたたみ・展開を切り替えられます。ファイルを読み込むと `FoamFile { … }` ヘッダーとファイル先頭のコメントバナーはどちらも自動的に折りたたまれます。折りたたみは表示のみの操作で、テキスト本文は変更されません。

- **BlockMesh 3-D パネルへの `topoSetDict` ジオメトリの表示** — `topoSetDict` を読み込んだり編集したりすると、各アクションのジオメトリソースが 3-D パネルにオーバーレイ表示されるようになりました。対応ソースタイプ: `boxToCell/Face/Point`（ボックス）、`sphereToCell/Face/Point`（球）、`cylinderToCell/Face/Point/AnnulusToCell`（円柱）、`coneToCell/Face/Point` および `coneAnnulusToCell`（円錐台。`radius2` が 0 のときは真円錐）。円柱・円錐では、新しい `point1`/`point2` と旧来の `p1`/`p2` の両方の軸点キー名を受け付けます。シェイプはアクション種別で色分けされます（`new` = スチールブルー、`add` = 緑、`subtract`/`delete` = 赤、`subset` = 紫、`invert` = ゴールド）。半透明で描画されるため、下地のメッシュも視認できます。パネルツールバーの **「topoSet ▾」** メニューでオーバーレイ全体の表示・非表示を切り替えられるほか、各シェイプ（アクションごとに `name · source` で一覧表示）を個別に表示・非表示できます。`topoSetDict` のトップレベルに定義した `$変数` および `#eval{ 式 }` は抽出前に解決されます（`blockMeshDict` と同じ仕組みです）。

### バグ修正

- キーワードリストを拡張スキャンで再生成した後、シンタックスハイライトがサイレントに失敗する問題（`QRegularExpression` が無効 — "regular expression is too large"）を修正しました。ハイライターが値キーワードの交替パターンを 1,000 キーワードずつのチャンクに分割し、それぞれを独立した `QRegularExpression` オブジェクトとしてコンパイルするようになりました。これにより、キーワードリストの規模にかかわらず PCRE2 のコンパイル済みコードサイズ制限に達しなくなります。

- `ui/mixins/_boundary_ops.py` の `_on_patch_edit_requested`、`_on_patch_paste_requested`、`_on_patch_delete_all_requested`、`_on_patch_add_all_requested` が存在しない `_extract_boundary` を `ui.panels.boundary_view_panel` からインポートしていた問題を修正しました。正しくは `model.boundary_model` の `extract_boundary` をインポートします。

- ケース比較の差分バーにある **Side by side** チェックボックスが、比較を開いても効果がなかった問題を修正しました。チェックボックスがシグナル接続前に checked 状態で初期化されていたため、常に `True` のまま固定されていました。`_compare_with_case` が `setChecked(True)` を呼び出しても既に checked であるためシグナルが発火せず、比較パネルが表示されませんでした。

- **OpenFOAM キーワード生成**で、`CoProcessor()` や `Pipeline:` などの正規表現特殊文字を含むトークンがリストに混入し `QRegularExpression` パターンが無効になる問題を修正しました。ジェネレーターは完全な識別子パターン（`^[A-Za-z]\w+$`）にマッチするトークンのみ保存するようになりました。ハイライターのローダーにも同じフィルターを追加したため、既に生成済みのファイルは次回起動時に自動的にクリーンアップされます（再生成は不要です）。

- BlockMesh 3-D パネルの **「Vertices ▾」**・**「Blocks ▾」**・**「Scale ▾」**・**「topoSet ▾」** ドロップダウンメニューが、クリックのたびに閉じてしまい、複数のチェックボックスを続けて切り替えにくい問題（例: topoSet の複数シェイプを個別に表示・非表示する場合）を修正しました。チェック可能な項目をクリックしてもメニューは開いたままになり、メニュー外のクリック・Escape キー・ツールバーボタンの再クリックで閉じる従来の挙動は維持されます。

- **ツリーパネル**で値を直接編集(インラインセル編集)しても、ファイルが未保存としてマークされず、**「ツリーからテキストを再読込」**を実行してもテキストエディタが更新されない問題を修正しました。詳細パネル経由で同じ値を編集した場合は正しく動作していました。ツリーモデルの `dataChanged` シグナルを、詳細パネル編集と同じ後処理(テキストエディタの再生成・未保存マーク)に接続したことで、インラインでのツリー編集も詳細パネル編集と同じ挙動になりました。

### 改善

- **topoSet シェイプの個別表示** — BlockMesh 3-D パネルの topoSet オーバーレイ操作を **「topoSet ▾」** メニューに変更しました。マスターの **Show topoSet geometry** トグルに加えて、読み込んだ `topoSetDict` の各シェイプ（`name · source` で表示）を個別のチェックボックスで表示・非表示できるため、特定のボックス・球・円柱・円錐ソースだけを表示したり隠したりできます。新しい `topoSetDict` を読み込むと一覧はすべて表示状態にリセットされます。メニューには **アクション色の凡例**（`new`・`add`・`subtract`・`subset`・`invert`）も表示され、各シェイプ行にはそのアクションに対応する色見本が付くため、メニューと 3-D ビューの色が直接対応します。

- **topoSet の中空環状体・`rotatedBox`・非ジオメトリソースの一覧表示** — BlockMesh 3-D パネルで、`cylinderAnnulusToCell` と `coneAnnulusToCell` が（`innerRadius` / `innerRadius1` / `innerRadius2` を反映して）中実ではなく本当に中空のチューブとして描画されるようになりました。`rotatedBoxToCell/Face/Point`（`origin` + `i`/`j`/`k` のスパンベクトルで定義される斜め方向のボックス）もオーバーレイ表示されます。描画対象のジオメトリを持たないソース（`cellToFace`、`zoneToCell`、`fieldToCell`、`surfaceToCell` など）は、**「topoSet ▾」** メニューに *"(no geometry)"* としてグレーアウト表示され、辞書内に存在することが分かるようになりました。

- **シンタックスハイライトのトグル** — エディタツールバーに **Highlight** ボタンを追加しました。ワンクリックでシンタックスカラーリングのオン／オフを切り替えられます。設定は `app_config.json` に保存され、次回起動時に復元されます。

- **シンタックスハイライトのキーワードリストを拡充** — **OpenFOAM キーワード生成** スキャナーが、C++ ヘッダーの `ClassName("…")` マクロと実装ファイルの `addNamedToRunTimeSelectionTable` / `addNamedToMemberFunctionSelectionTable` ルックアップ名も抽出するようになり、ベースラインのキーワード数が約 2,200 から約 3,100 に増加しました。辞書のキー名（`vertices`、`blocks`、`edges`、`boundary`、`dimensions`、`internalField`、`boundaryField`、`solvers` など）もダーク・シアンでハイライトされるようになりました。caseDicts スキャンがノード値に加えてノード名を収集するようになり、またハイライターが起動時にスキーマレジストリからキーセグメントを抽出するため、キーワードファイルを再生成しなくても一般的なキーがカバーされます。

- **パーサ／モデル層の型安全性強化** — `FoamNode.node_type`（`foam/nodes.py`）が単なる `str` ではなく、パーサ／ライタ／ツリーモデルが実際に生成するすべての値からなる `Literal` 型になりました。開発依存として追加した `mypy`（`foam/` と `model/` にスコープ）がこれを検証し、`tests/test_lint.py` によって通常のテスト実行の一部としてチェックされます。lint 用の `ruff` も同様です。機能・挙動の変更はありません。

**`tutorials/` の同梱チュートリアルセットを拡充**

- `tutorials/cavity/` を OpenFOAM v2512 標準チュートリアルの 3 つの icoFoam サブケースをまとめるコンテナに変更しました: `cavity/`（単一リージョンのワークフロー解説）、`cavityGrade/`（非一様 `simpleGrading`）、`cavityClipped/`（クリップ形状、`mapFieldsDict`）。コンテナレベルに `Allclean`/`Allrun` スクリプトも含まれます。
- OpenFOAM v2512 標準チュートリアルから `tutorials/damBreak/`（interFoam、層流）を追加しました。`defaultFieldValues`/`regions` ブロックを持つ `setFieldsDict`、`0.orig/`、および `sampling` 関数オブジェクト辞書のテストに使用します。
- cavity をベースにしたカスタム icoFoam `blockMeshDict` ケースを 4 件追加しました: `oneBlocks`（3-D 単一ブロック）、`oneBlocks-vars`（変数置換とコンパクト `(blockId faceId)` 面記法）、`nineBlocks`（3×3 マルチブロック、正規表現境界パッチ）、`nineBlocks-vars`（変数置換 + コンパクト記法）。
- `tutorials/topoSetShapes/` を追加しました。単一の 3×3×3 ブロック上に、BlockMesh 3-D パネルがオーバーレイ表示できるすべてのジオメトリソース（`boxToCell`、`rotatedBoxToCell`、`sphereToCell`、`cylinderToCell`、中空の `cylinderAnnulusToCell`、切頭円錐の `coneToCell` フラスタム、`radius2 0` の真円錐 `coneToCell`、中空の `coneAnnulusToCell`）に加えて非ジオメトリの `cellToFace` アクションを、`$変数` および `#eval` 解決とあわせて配置した `topoSetDict` のデモです。`system/topoSetDict` を開いて **「topoSet geometry」** にチェックを入れると、アクション別に色分けされた形状が表示されます。

---

## v1.6.1 — 2026-06-19

### バグ修正

- `tutorials/` の README を修正しました。GPLライセンスも追加しました。

## v1.6.0 — 2026-06-19

### 改善

**サンプルケースを `tutorials/` に同梱**

- 2 件の OpenFOAM ケースを `tutorials/` ディレクトリに収録しました: `cavity`（icoFoam、単一リージョンのワークフロー解説）と `snappyMultiRegionHeater`（chtMultiRegionFoam、マルチリージョン）。
- 各ケースは OpenFOAM v2512 の標準チュートリアルセットから取得し、GPL-3.0 でライセンスされています（FoDE ソースコードの AGPL-3.0 とは別）。詳細は `tutorials/tutorials_README.md` を参照してください。

**マルチリージョン：`0/<region>/` 配下のフィールドファイルをファイル一覧と境界条件パネルに表示**

- `0/<region>/` および `0.orig/<region>/` 内のフィールドファイル（例: `0/heater/T`、`0/bottomWater/p`）が、`0/heater` や `0/bottomWater` のようなグループヘッダーのもとにファイル一覧へ自動的に追加されます。
- **Boundary** パネルのディレクトリセレクターに、`0` と `0.orig` に加えてリージョンごとのサブディレクトリ（例: `0/bottomWater`、`0/heater`、`0/topAir`）が表示されるようになりました。リージョンを選択すると、そのリージョンのフィールドファイルに絞り込んだ境界条件テーブルが表示されます。
- 以前は `0/` および `0.orig/` の直下ファイルのみがスキャン対象で、リージョンサブディレクトリは無視されていました。

**BlockMesh ビューア：コンパクト境界面記法 `(blockIndex, faceIndex)` のサポート**

- OpenFOAM の新しいコンパクト境界面記法 — `(blockIndex faceIndex)` — が、従来の 4 頂点インデックス記法 `(v0 v1 v2 v3)` と並んでサポートされるようになりました。
- コンパクト記法は標準的な六面体ブロック面テーブル（面 0 = −x、1 = +x、2 = −y、3 = +y、4 = −z、5 = +z）を使用して 4 頂点インデックスリストに展開されます。両方の記法を同じファイルで混在させることができます。
- 以前は、コンパクト面エントリは `len < 3` ガードによって 2 整数タプルが除外されて無視されていたため、新しい記法を使用した境界パッチが 3D ビューに表示されませんでした。

**GUI：トップバーの再配置と Tools メニューの追加**

- トップバーに **Reload Case** ボタンを追加しました。**Save All Files** とセパレーターの間に配置されており、**Case > Reload Case** と同じ操作をワンクリックで実行できます。
- **foamMonitor…** ランチャーをトップバーから新しい **Tools** メニュー（**Settings** と **Help** の間）に移動しました。foamMonitor はファイル編集機能ではなくランタイム監視ツールであるため、編集系ボタンと分離することで役割の違いを明確にしました。実行中の状態表示（メニュー項目が **■ foamMonitor** に変わる）およびクリックで停止する動作は従来どおりです。

### バグ修正

- **BlockMesh ビューア：否定マクロ変数定義の解決** — `xMin -$xMax;` のような形式（`$参照` の前にマイナス符号）の変数定義は、パーサーがそれらを `macro` ノードではなく `word` ノードとして分類するため、`_build_var_map` で無視されていました。マクロ解決パスは `macro` 型のノードのみを処理していたため、`($xMin $yMin $zMin)` のような頂点が 3D ビューに表示されませんでした。新しい word/compound 算術評価パスを追加し、`xMax` が既知になった時点で `-$xMax` が正しく解決されるようにしました。

- **Compare Cases：サイドバイサイドパネルが即座に表示されるように修正** — "Side by side" チェックボックスはデフォルトでオンになっています。比較を開始したとき、チェックボックスの値がすでに `True` だったため Qt の `toggled` シグナルが発火せず、比較パネルが表示されませんでした。ユーザーが手動でチェックを外してから再度オンにするまで表示されないという問題がありました。`setChecked` の直後にトグルハンドラーを直接呼び出すことで修正しました。

### 内部変更

**リファクタリング: `ui/` をサブディレクトリへ整理**

- ダイアログ 14 ファイルを `ui/dialogs/`、パネル 7 ファイルを `ui/panels/`、低レベルウィジェット 3 ファイルを `ui/widgets/`、8 つの `_*_ops.py` Mixin ファイルを `ui/mixins/` へ移動しました。オーケストレータのコア（`main_window.py`、`app_state.py`、`layout_constants.py`）は `ui/` 直下に残ります。機能変更はありません。

**リファクタリング: `tests/` をソース構造に合わせてサブディレクトリへ分割**

- `tests/` 直下にフラットに置かれていた全テストファイルを、ソース構造に対応したサブディレクトリへ移動しました: `tests/foam/`、`tests/model/`、`tests/ui/`、`tests/services/`、`tests/app_config/`、`tests/schemas/`。`conftest.py` は `tests/` 直下に残ります。機能変更はありません。

**リファクタリング: 共有状態を `AppState` データクラスへ集約**

- `ui/app_state.py` を追加し、`MainWindow` の bare `self.X` 属性として散在していた 18 個の共有可変フィールド（現在のファイル・ツリー、ファイルバッファ、ダーティ追跡、内部フラグ、ケース設定、差分状態、foamMonitor 状態、パネル状態）を `AppState` データクラスにまとめました。`MainWindow.__init__` では `self.state = AppState()` の 1 行で初期化され、8 つの Mixin すべてが共有データを `self.state.<field>` としてアクセスします。これにより Mixin 間の依存関係が明示的かつ grep 可能になりました。機能変更はありません。

## v1.5.0 — 2026-06-10

### 改善

**BlockMesh ビューア：サイドバイサイドモード**

- `blockMeshDict` がアクティブなファイルで BlockMesh パネルが利用可能なとき、上部タブウィジェットの右上に **⊞** トグルボタンが表示されます。
- クリックするとツリーの右隣に 3D ビューが水平スプリッターで並列表示され、ツリーの編集と 3D ビューを同時に確認できます。サイドバイサイドモード中は独立した **BlockMesh** タブが非表示になり、モードを切ると復元されます。
- 3D ビューは自動更新されません。ツリーを編集した後は BlockMesh パネルの **Refresh** をクリックしてください。
- xterm ターミナルがアクティブな間はボタンが無効化されます（GPU/OpenGL 競合のため）。

**比較パネル：比較モード以外では非表示に**

- 参照ケースが読み込まれていないときは、参照ケースのツリーペインが完全に非表示になりました。従来はスプリッターの非表示スロットとして存在し、メインツリーと詳細ペインの間に細いスプリッターハンドルの隙間が残っていました。
- ペインは差分バーで **Side by side** にチェックを入れたときに表示され、チェックを外すか **Clear** をクリックすると非表示になります。

**BlockMesh ビューア：変数ベースメッシュのプレビューモード**

- `vertices` ブロックに変数参照（`$varName`）が含まれる場合、頂点テーブルの X/Y/Z セルはデフォルトで読み取り専用になりました（直接書き込みは無言で失敗しており、誤解を招いていました）。
- Vertices パネルの上部（グループボックス内、メインツールバーではない）に **⚙ Variable-based** チップと **Preview** トグルボタンが表示されます。**Preview** をクリックするとプレビューモードになり、テーブルセルが編集可能になって各変更が即座に 3D ビューへ反映されますが、ツリーとファイルは変更されません。プレビューモード中は黄色いバナーが表示されます。
- **Refresh** をクリックするとプレビューモードを終了してツリーの頂点座標に戻ります。
- 変数参照を含まないメッシュでは従来どおり、テーブルへの編集がツリーとエディタに即座に反映されます。

**BlockMesh ビューア：多段変数チェーンの解決**

- `blockMeshDict` 内の任意の深さの変数チェーンを完全に解決できるようになりました。従来は `z001 $z1;` のようなマクロ参照が `z1 #eval{$z0+$dz0};` で定義されていた場合、マクロパスが `#eval` 評価より先に実行されるため、数値ではなく生の式文字列が残っていました。
- 固定の 3 パス方式を、マクロ参照の解決と `#eval` 式の評価を交互に繰り返す反復ループに置き換えました。反復上限はトップレベルの変数定義の数であり、これは非循環依存グラフの理論的な最大深度に等しいです。
- 循環参照（`a $b; b $a;` など）は、無限ループやクラッシュを起こすことなく未解決のまま静かにスキップされます。

---

## v1.4.0 — 2026-06-09

### 新機能

**foamMonitor ランチャー**

- トップバーの **foamMonitor…** ボタンからダイアログを開き、`foamMonitor` を起動して残差などのデータを gnuplot でプロットできます。
- ダイアログではファイル（ソルバーログや `postProcessing/` の出力ファイル）の選択、オプション（対数スケール、グリッド、リフレッシュ間隔、アイドルタイムアウト）の設定、追加オプションの自由入力ができます。
- foamMonitor の実行中はボタンが **■ foamMonitor** に変わり、クリックするとプロセスを停止します（foamMonitor シェルと gnuplot ウィンドウの両方を終了）。新しいケースを開いたときも実行中のインスタンスは停止されます。
- 新しい gnuplot バージョンで非推奨となった `reread` コマンドを起動時に自動的にパッチし、モダンな `load ARG0` 相当に置き換えます。gnuplot のバージョンに関係なくウィンドウが正しくリフレッシュされます。
- 指定したファイルが存在しない場合や foamMonitor がエラーで終了した場合は警告ダイアログを表示します。

**BlockMesh ビューア：変数と `#eval` 式のサポート**

- `blockMeshDict` 冒頭の変数定義（例：`xMax 0.5;`、`nCell 20;`）が、3D ジオメトリ抽出前に `vertices` および `blocks` セクションで置換されるようになりました。`$varName` および `${varName}` の両参照形式に対応しています。
- 他の変数を参照するマクロ変数（`nx $nCell;`）を 1 レベル深まで解決します。
- 算術式（`zMax #eval{ $length / $nCell };`）は変数置換後に評価されます。対応演算子：`+`、`−`、`*`、`/`、括弧。
- 従来は未解決の `$variable` 参照により `float()` 変換が失敗し、頂点が欠落したり、最悪の場合は範囲外の頂点インデックスによる VTK クラッシュが発生していました。
- **レキサー修正：** 内部にスペースのない `#eval{expr};` 形式が、セミコロンを含む単一のディレクティブトークンとして解析され、パーサーが直前の変数の値として `vertices` ブロック全体を消費してしまう問題を修正しました。レキサーは `{` でディレクティブトークンの読み取りを停止するようになり、深さ追跡が正しく機能します。

**ドラッグ＆ドロップによるケースオープン**

- ファイルマネージャからケースディレクトリをウィンドウ上の任意の場所にドロップするとケースを開きます。ツリービュー、エディタ、ファイル一覧など、すべてのパネルがドロップ対象として有効です。
- 現在開いているファイルに未保存の変更がある場合は、新しいケースを読み込む前に確認ダイアログが表示されます。

---

## v1.3.0 — 2026-06-01

### 改善

**ケース比較の高速化・ノンブロッキング化**

- ファイル一覧の差分件数をイベントループ 1 ティックごとに 1 ファイルずつ段階的に計算するようになりました。UI はブロックされず、すべてのファイルの解析完了を待たずに `≠` マーカーが順次表示されます。
- `FoamFile` ヘッダーを持たない大きなファイル（`log.simpleFoam` や残差出力など）は、差分計算および境界条件パネルの読み込み時に自動的にスキップされます。100 KB 未満のファイルは常に解析を試みるため、`FoamFile` ブロックを持たない小さなカスタムソルバー辞書は引き続き正常に動作します。
- 大きな非ディクショナリファイルを開く際に確認ダイアログが表示されるようになりました。ツリービューが利用できないこと、および読み込み中にアプリケーションが応答しなくなる可能性があることを警告します。確認後はステータスバーにメッセージが表示されます。

### 新機能

**UI 言語選択（英語 / 日本語）**

- **Settings > Language** サブメニューを追加。**English** または **日本語** を選択するとアプリケーションが選択言語で再起動します。
- 言語設定は `app_config.json` に保存され、起動時に適用されます。デフォルトは英語です。
- 新規 `i18n/` モジュール: `tr()` 関数がアクティブな言語辞書を参照し、未翻訳のキーは英語文字列へ自動フォールバックします。
- 新しい言語（例: イタリア語）の追加は `i18n/<コード>.py` 翻訳ファイルを1つ作成するだけで完了し、他のコード変更は不要です。

---

## v1.2.0 — 2026-05-30

### 新機能

**Boundary ビュー — テーブルのコピー**

- Boundary パネルのツールバーに **Copy Table** ボタンを追加。ドロップダウンメニューで 2 つの形式を選択できます:
  - **Copy as Markdown** — GitHub Flavored Markdown のパイプテーブル。Lines per cell が 2 以上の場合、複数行セルは `<br>` タグで結合され GitHub 上で改行として表示されます。
  - **Copy as CSV** — RFC 4180 準拠の CSV。複数行セルは引用符付きフィールドとして保持され、Excel・LibreOffice Calc で正しく表示されます。
- どちらの形式も行ヘッダー列を含み、転置状態を反映します。

**ケース比較 — サイドバイサイド表示**

- 差分バーの **Side by side** トグルで中央パネルを左右に分割します。左ペインは編集可能な現在のケースのツリーを表示し、右に新しい **Reference** ペインが開いて対応するファイルを読み取り専用で表示します。
- 参照ケースのツリーでは、参照にしか存在しないキーを**薄緑色**でハイライト表示します。
- 参照ペインの任意のリーフノードを右クリックして **Use this value** を選択すると、対応するノードの値を現在のケースに即座に適用できます。適用後、差分ハイライトは自動的に更新されます。
- ファイル一覧の **Changed files only** チェックボックス: 差分のないファイルを非表示にし、参照ケースと異なるファイルのみを表示します。
- 差分マーカー（`≠N` / `≠0`）は、比較開始時に全ファイルに対して即座に計算されるようになりました（従来はファイルを開くたびに遅延計算していました）。

### 改善

**BlockMesh パネル**

- ツールバーを 1 行にコンパクト化しました。
- STL に加えて OBJ ジオメトリファイルの読み込みとオーバーレイ表示に対応しました（`Load STL / OBJ…` / `Clear STL`）。

**ファイル一覧**

- 追加ファイルのボタンを常に表示するようにしました（追加ファイルが登録されている場合は件数を表示）。

**ケース比較**

- 差分バーに **Side by side** トグルとカラーレジェンドを追加しました。

---

## v1.1.0 — 2026-05-23

本リリースでは、インタラクティブな BlockMesh 3D ビューア、差分オーバーレイによるケース比較、ケース全体にわたる境界パッチのリネーム、Boundary パネルからエディタへのナビゲーション、ツリー ↔ エディタのソース行同期など、複数の主要機能を追加しました。また、Windows 向けの起動バリアント（no-terminal）、追加ディレクトリの再帰スキャンオプション、snappyHexMeshDict スキーマの拡張、各種エディタおよびユーザビリティの改善も含まれています。

### 新機能

**BlockMesh 3D ビューア** *（`pyvista` および `pyvistaqt` が必要）*

- 新しい **BlockMesh** タブで `blockMeshDict` のジオメトリをインタラクティブに 3D プレビュー。頂点（赤い球）、ヘックスブロックのエッジ（ワイヤーフレーム）、境界パッチ面（パッチ種別で色分け: wall、inlet、outlet、symmetry など）を表示。
- 頂点、頂点ラベル、ブロックエッジ、ブロックラベル、境界面、軸、グリッド、寸法テキストの表示切替チェックボックス。
- **Block labels** — 各ヘックスブロックの重心にブロックインデックスを表示（濃青テキスト）。頂点ラベルと同様の機能。
- **Color blocks** — 各ヘックスブロックを定性的パレット（tab10）の異なる色で表示。ワイヤーフレームとソリッド面の両方に適用。
- **Solid blocks** — 半透明のソリッドヘックスブロック面（opacity 0.25）をワイヤーフレームに加えて表示。Color blocks のパレットを共有。
- **Label size** — 第 2 ツールバー行の共用スピンボックス（範囲 6–32、デフォルト 10）で頂点ラベルとブロックラベルのフォントサイズを一括調整。
- 視点方向ボタン（+X / −X / +Y / −Y / +Z / −Z / Iso）でカメラを素早く切り替え。
- パネル下部のマウス操作ヒントバー（ドラッグ = 回転、Shift+ドラッグ = 平行移動、スクロール = ズーム）。ホバーで詳細ツールチップ表示。**Help > Keyboard Shortcuts…** でも確認可能。
- 3D ビューと並んで表示される頂点テーブル（インデックス | X | Y | Z）。行クリックで頂点をハイライト、座標セルをダブルクリックで値を編集 — 変更は FoamNode ツリーとテキストエディタに即座に反映。
- STL ジオメトリファイルの読み込みとオーバーレイ表示（`Load STL…` / `Clear STL`）。
- **View > BlockMesh 3-D Panel**（チェック式）でターミナルモードとは独立してタブを表示・非表示に切り替え可能。xterm が有効なときはアクションが無効化され、GPU コンフリクトの理由を示すラベルに変わります。
- `no-terminal-blockmesh` バリアントでは常時表示。`standard` バリアントではターミナルがシンプルモードのときのみ表示。

**ケース比較**

- **Case > Compare with Case…** — 参照ケースのディレクトリを選択し、現在のケースと比較。
- アクションバー下の差分バーに参照パスとカラーレジェンドを表示。**Clear** をクリックすると比較モードを終了。
- ツリーオーバーレイ: 薄黄色 = 値の変更、薄青 = このケースのみに存在するキー。ハイライト行をホバーするとツールチップに参照値を表示。
- ファイル一覧のマーカー: `≠N`（アンバー）= 差分あり、`≠0`（グレー）= 確認済みで一致、マーカーなし = 未訪問。50 件超は `≠50+` に丸め。ファイルを開いたタイミングで遅延更新。

**境界パッチのリネーム（Rename Boundary）**

- **Boundary** パネルのセル・パッチ列/行ヘッダー、またはツリービューのパッチノードを右クリックして **Rename Boundary…** を選択。
- ダイアログにパッチ名が含まれるすべての読み込み済みファイル（`blockMeshDict` の境界エントリおよびフィールドファイルの `boundaryField` パッチキー）が一覧表示されます。更新するファイルを選択し、新しい名前を入力して **Rename** をクリック。選択したすべてのファイルが一括でメモリ上に反映され、ダーティマークが付きます。

**Boundary パネルのシングルクリックナビゲーション**

- Boundary パネルのセルをクリックすると、対応するフィールドファイルをエディタで開き、該当パッチエントリにスクロールしてアンバーハイライトで表示。**Auto-scroll editor** チェックボックスでこの動作を切り替えられます。
- クリックしたセルが現在開いているファイルと異なる場合、ファイル一覧とエディタが自動的に更新されます。

**ツリー ↔ エディタ ソース行同期**

- ツリーノードをクリックすると、対応するソース行がテキストエディタでアンバー背景でハイライトされ、スクロールします。
- **Find in Tree** ボタン（または `Ctrl+Shift+T`）で現在のエディタカーソル行のソース範囲をカバーする最も深いツリーノードを選択します。
- エディタテキストが未保存の間（行番号が古い状態）はジャンプが無効化され、チェックボックスのラベルとツールチップで状態を表示します。

**ツリーキーフィルター**

- ツリー上部のフィルターバーで大文字小文字を区別しないキーワード絞り込みが可能。子孫ノードが一致する場合は親ノードも表示されます。

**ケースの再読み込み（Case Reload）**

- **Case > Reload Case** — メモリ上の編集をすべて破棄し、ディスクからケースを再読み込み。未保存ファイルがある場合は件数を示す確認ダイアログを表示してから実行します。

**`0/` ディレクトリの削除**

- ファイル一覧の `0` グループヘッダーを右クリックして **Delete '0' directory…** を選択すると、`0/` ディレクトリをディスクから完全削除。`0.orig` が存在するときのみ表示されます。削除前にフルパスを示す確認ダイアログを表示します。

**追加ディレクトリの再帰スキャン**

- 追加ディレクトリをフラットスキャン（直下ファイルのみ、デフォルト）または再帰スキャン（`rglob` でサブディレクトリ含む）に設定できるようになりました。
- **Settings > Manage Extra Files & Directories…** > **Extra Directories** タブでエントリを選択し **Toggle Recursive** をクリックすると再帰スキャンを有効・無効に切り替えられます。有効なエントリは `[recursive]` と表示されます。
- サブディレクトリにファイルを含む結果ツリー（例: `validation/`）などに便利です。

**アプリケーションバリアント**

- `--variant` フラグで起動設定を選択: `standard`（ターミナル + BlockMesh）、`no-terminal`（Windows 向け）、`no-terminal-blockmesh`（BlockMesh 常時表示・ターミナルなし）。選択したバリアントは `app_config.json` に保存され、次回以降の起動時に自動適用されます。
- ターミナルモードの切替（xterm ↔ シンプル）は Terminal タブ内のチェックボックスで行います。シンプルモードに切り替えると BlockMesh タブが表示されます。

**ツリーのコピー＆ペースト**

- ツリーパネルで `Ctrl+C` / `Ctrl+V` によるツリーエントリのコピー＆ペーストが可能。ショートカットはツリーウィジェットにスコープされており、下部のテキストエディタとは干渉しません。

### 改善

**View メニュー**

- **View > Show Type Column** — ツリービューの Type 列を表示・非表示に切り替え。
- **View > BlockMesh 3-D Panel** — パネルの表示切替（上記参照）。

**エディタツールバー**

- **Find Prev**（Shift+F3）と **Find Next**（F3）ボタンを追加。
- **Find in Tree** ボタン（`Ctrl+Shift+T`）— 現在のエディタ行から対応するツリーノードにジャンプ。
- ツールバー右側に **Line: N** として現在のカーソル行番号を表示。

**キーボードショートカットダイアログ**

- **Help > Keyboard Shortcuts…** ですべてのキーボードショートカットを一覧表示。
- 新しい **BlockMesh 3-D viewer (mouse)** セクションで回転・平行移動・ズーム・カメラリセット・フライトゥーポイントの操作方法を確認できます。

**snappyHexMeshDict スキーマ**

- `snappyHexMeshDict` のスキーマカバレッジを拡張: `snapControls`、`addLayersControls`、`meshQualityControls` のキーを追加。
- ユーザー定義名のサブ辞書（ジオメトリエントリ、細分化サーフェス/リージョン、レイヤーパッチグループ）に対して祖父キーコンテキストを使ったスキーマ検索に対応。ユーザー定義名に関わらず正しいスキーマが表示されます。

**パーサー**

- 括弧ブロック内（頂点リストなど）のインライン `//` コメントが無音で破棄されていたバグを修正。コメントはブロック内の `unknown_raw_entry` ノードとして保持されるようになりました。

**Boundary パネル**

- セルおよび列/行ヘッダーの右クリックメニューに **Rename Boundary…** を追加。
- パッチ列/行ヘッダーを右クリックして全フィールドファイルへの一括パッチ追加・削除が可能。

**ファイル管理**

- パーサーが完全に解釈できなかったエントリ（`unknown_raw_entry` ノード）をツリーでアンバー色でハイライト表示し、通常エントリと区別しやすくなりました。

---

## v1.0.0 — 初回公開リリース

初回公開リリース。主要機能:

- OpenFOAM 辞書ファイルの構造化ツリービューおよびテキストエディタ。
- `TARGET_FILES`、`0/`、`0.orig/` を自動スキャンするファイル一覧。追加ディレクトリの登録も可能。
- 境界条件テーブル（閲覧・編集・作成・削除・コピー・ペースト・全ファイル一括追加/削除）。
- `controlDict`、`fvSchemes`、`fvSolution`、`blockMeshDict`、`snappyHexMeshDict` の組み込みスキーマヘルプ。
- フル PTY xterm.js ターミナル（Linux/macOS）。ケースを開くと自動でそのディレクトリへ移動。
- ケースライブラリ（**Case > Duplicate from Case Library**）でチュートリアルを作業ディレクトリにコピー。
- 現在のケースを新規ケースとして保存 / ケースの複製。
- 個人用参照リンク（**Help > Resources…** > **My Links** タブ）。
- AGPL-3.0-or-later ライセンス。
